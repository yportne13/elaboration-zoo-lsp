# A5 Round2 — L11_macro / L12_canonical

## 1. 结论（verdict）

- P0: 1（A8 已修 L11/L12；L13 共享副本残留，记录）
- P1: 4（A2/A3/A4/A5 全部已修，两版同步）
- P2: 1 已修（A6）；另 5 项 needs-verify（ROUND2 §B，按指示记录不盲改）
- P3: 0（顺带清理 SAFETY 注/过期 TODO/过期模块注释）
- needs-verify: 5
- 本轮是否收敛：**否**。自审可修项（P0/P1/P2）已全部处理，但 ROUND2 §B 明列的高风险
  残留（Stacked Borrows 别名 UB、canonical IDDFS 完备性、`vals_eq_ground` Flex 等）
  需 Miri/重构，静态推理下无法保证正确，按 §B 要求**记录不盲改**，故严格按 BRIEF §2
  判据本轮未收敛。

## 2. 发现列表

### [P0] A8 自递归宏无展开深度上限 → 栈溢出（已修）
- 位置：`src/L11_macro/parser/mod.rs`、`src/L12_canonical/parser/mod.rs` 宏展开
  重解析递归点（L11 `:1184/:1449/:1992`；L12 `:1237/:1574/:2117`）。
- 证据/机制：`macro_rules m { () => { m } } def x = m` 展开后重 lex 首 token 仍是 `m`，
  无限递归 `p_raw`/`p_decl`。
- 处置：**已修**。两副本各加 `thread_local! MACRO_DEPTH: Cell<u32>` +
  `MAX_MACRO_EXPANSION_DEPTH = 256` + RAII `MacroDepthGuard`（`Drop` 含 panic 展开安全），
  在三处重解析递归点前 `enter()`；超限：`p_raw`/块重解析返回 `Err(macro_depth_error)`，
  `p_decl` 推 IError 后退化（`is_cut` → Hole，否则试下一条规则），不递归。只计宏展开/
  块重解析路径，普通解析不受影响。探针：
  `tests/l11_fast_parity.rs::probe_macro_self_recursion_depth_limit`、
  `tests/l12_fast_parity.rs::probe_macro_self_recursion_depth_limit`（8MiB 小栈线程，
  断言不栈溢出 + 返回解析错误）。
- 残留：`src/L13_namespace/parser/mod.rs` 同构副本未改（共享文件，ROUND2 明确不改），
  记入 §4。

### [P1] A2 L11/L12 η 臂缺 applicability 守卫 → 可达 panic + parity 裂缝（已修）
- 位置：参考版 `src/L11_macro/unification.rs` η 臂（原 `:647/:653`）、
  `src/L12_canonical/unification.rs` η 臂（原 `:681/:688`）；快版
  `src/L11_macro/bump_spine_iter.rs`（原 `:2418/:2433`）、
  `src/L12_canonical/bump_spine_iter.rs`（原 `:2469/:2484`）。
- 证据：`get_global "f"` 的类型 = f 的登记值（可为 λ）；`def g : String = get_global "f"`
  让 `LiteralType ≡ λ` 落 η 臂 → `v_app(LiteralType)` 命中
  `panic!("impossible apply")`（L11 `mod.rs:321`、L12 `mod.rs:431`）。
- 处置：**已修**。移植 L06 `v_applicable` 语义：
  - 参考版新增 `v_applicable`（L11 `unification.rs:17` = Flex/Rigid/Decl/Obj；L12 `:18`
    另含 `Call`，与各自 `v_app` 可处理形态逐一对齐），η 两臂加 `if v_applicable(&t/&u)`。
  - 快版新增 `vapp_ok`（L11 `bump_spine_iter.rs:721`、L12 `:772`）：tag7 仅 Obj/Decl 可、
    tag3/4/6 不可、其余可——与 `vapp1` 不 panic 的形态精确对应；η 两臂加守卫。
  守卫失败后落 `_ => Err`/`return false`，两版同判定。探针：
  `probe_eta_applicability_guard`（L11/L12）。

### [P1] A3 `intersect_go` 长度失配 `unreachable!()` → `None`（已修）
- 位置：`src/L11_macro/unification.rs:605` 区（原 `unreachable!()`）、
  `src/L12_canonical/unification.rs:620` 区。
- 处置：**已修**。改 `_ => None`，与 L05–L10 及快版 `intersect_bump`（`n1 != n2` → false）
  一致，回落 `unify_sp` 判失败。

### [P1] A4 超大整数字面量 `parse::<u64>().unwrap()` panic（已修）
- 位置：`src/L11_macro/parser/mod.rs`（原 `:506`）、`src/L12_canonical/parser/mod.rs`
  （原 `:549`）。
- 处置：**已修**。数字臂改为状态可访问的闭包 parser，`parse::<u64>()` 失败时
  `state.push_error(IError{ ErrMsg::Custom("integer literal does not fit in u64") })`
  并退化为 `Raw::Hole`；正常数字路径逐字等价（同一 span、同一 Church 链）。
  两副本同步；双版共用各自 parser。探针：`probe_u64_literal_overflow_no_panic`。

### [P1] A5 trait 求解 `.unwrap()` panic / L12 快版判定分叉（已修）
- 位置：`src/L11_macro/elaboration.rs:292`（原 `.unwrap()`）、
  `src/L11_macro/bump_spine_iter.rs:5365`、`src/L12_canonical/bump_spine_iter.rs:5443`。
- 处置：**已修**。
  - L11 参考版：`solve_multi_trait(...).map_err(|e| Error(name.to_span().map(|_| format!("{:?}", e))))?`
    （对齐 L12 参考版 `elaboration.rs:313-314`；L11 `Error` 单字段）。
  - L11/L12 快版：`.map_err(|e| Error(span, vec![]))?`（L12 Error 双字段），文案
    `format!("Trait({:?})", e)` 以复刻参考版 `UnifyError::Trait` 的 Debug 形态。
  - L12 快版 `:5443` 不再 `.unwrap()`，与参考版同为 Err，判定分叉消除（无 l12_fast_parity
    头部登记需求）。
  探针：`probe_solve_multi_trait_recoverable`（L11）、
  `probe_solve_multi_trait_recoverable_parity`（L12）。

### [P2] A6 `prune_ty` 未反转 pruning 掩码（已修）
- 位置：`src/L11_macro/unification.rs:142`、`src/L12_canonical/unification.rs:144`
  （`prune_ty` + `prune_ty_go`）。
- 证据：Pruning 链头 = 最内层槽位，旧实现直接按链头配对最外层 Π；L05–L10 与快版
  `prune_ty_bump` 均反转。多层 telescope + 混合掩码时与快版错位。
- 处置：**已修**。改为收集 `Vec<Option<Icit>>` 后 `reverse()`，`prune_ty_go` 用
  `split_first` 逐层配对（与 L08/L10 逐字同构），并删除已过期的 `//TODO:revPruning`
  注释（`unification.rs`、`elaboration.rs` 各 2 处）。探针：
  `probe_prune_ty_mask_reversal_parity`（L11/L12，源取自 L06
  `pruning_nonpalindrome_dependent_masks` 的 `U→Type 0` 改写）。

### [P2·N/A] A7 `1919810`/`GLOBAL_BASE` 边界 — 本切片不适用
- 证据：L11/L12 参考版**没有** global 大下标机制——全局走 `Cxt.decl`（名字键）+
  `Tm::Decl`/`Val::Decl`（模块头 `bump_spine_iter.rs:13-19` 自述），全切片
  `grep 1919810` 仅命中注释，无 `x - 1919810`/`GLOBAL_BASE` 算术，故无 0 号全局下溢。
- 处置：**仅文档修正**。快版 `Machine` 文档注释是从 L09 误拷的（称"L09 增量：
  global 表 / GLOBAL_BASE 大下标"），已改为 L11/L12 实际机制
  （`bump_spine_iter.rs:3465-3468` / L12 同）。无代码行为变更。

### [P2] canonical IDDFS 完备性 `basic_target_limit < target_limit`（needs-verify，记录）
- 位置：`src/L12_canonical/canonical.rs:19-24`。
- 机制：`basic_target_limit` 从 1 步进 +2，`< target_limit` 永不取等、跳过偶数预算；
  `avoid_recurse` 另带 `//TODO: this is incorrect`。可能漏解（不完备）。
- 处置：**needs-verify / 仅报告**（ROUND2 §B4）。构造"恰需上限 6 预算"的实例需运行时
  验证，本轮不盲改。

### [P2] `vals_eq_ground` 把 `Val::Flex` 视为等于一切（needs-verify，记录）
- 位置：`src/L12_canonical/typeclass.rs:130`、`:197`（`(Flex,_)|(_,Flex) => true`）；
  `Match` 分支比较 `:227-231` 忽略 case 表（TODO）。
- 机制：实例匹配/缓存可能接受错误实例或误命中；当前依赖"求解时参数已 ground"。
- 处置：**needs-verify / 仅报告**（ROUND2 §B4）。构造含未解 meta 的 trait goal 需运行
  时判定，静态无法给期望。

### [P2] L12 快版 `v_to_ref_val` 链头 `unreachable!`（needs-verify，记录）
- 位置：`src/L12_canonical/bump_spine_iter.rs:5771`（tag==2 链头仅接受 Flex/Rigid，
  其余 unreachable）。trait 实参若为已应用的构造子（链头 XCell，如 `List A`）即触发。
- 处置：**needs-verify / 仅报告**（ROUND2 §B）。需 HK 类型实参实例，静态难构造。

### [P2] L11 旧 `Synth::unify` 无 occurs check（needs-verify，记录）
- 位置：`src/L11_macro/typeclass.rs:370`（`fn unify` 用可变 `Subst` 首匹配，无 occurs）。
- 机制：可能选错实例（ROUND2 §B2：L12/L13 已重写为 `val_match`）。移植属语义重写、风险高。
- 处置：**needs-verify / 仅报告**。探针需构造"能区分两实例"的 trait 源，静态无法保证
  期望，按 §B 不盲改。

### [P0·记录] 快版 `&mut *mach_ptr` Stacked Borrows 别名 UB（记录，不盲改）
- 位置：`src/L11_macro/bump_spine_iter.rs:2385/2501/2524`、
  `src/L12_canonical/bump_spine_iter.rs:2436/2552/2575`。
- 机制：`unify` 把 `metas` 等以 `&mut` 字段借出后，`unify_iter` 内 `&mut *mach_ptr`
  重建整个 `Machine` 可变引用并调 `&mut self` 的 `solve_multi_trait_ref`（写 `metas`）——
  严格 Stacked Borrows 下别名 UB。SAFETY 注释（L11 `:3968` 区）声明不相交，但 `metas`
  同时以字段 `&mut` 存活。
- 处置：**仅报告**（ROUND2 §B1 明列：修复需把整机重借改成不相交字段自由函数，高风险
  大重构且本环境无 Miri）。建议独立任务：将 `solve_multi_trait_ref` 改为
  `fn(&mut Vec<MetaEntry>, &mut TraitState, …, cxt)` 形式，彻底消除整机重借。

### [P3] SAFETY 注补齐 / 过期注释（顺带修）
- `v_clo_of`/`v_pi_of`/`v_xcell_of` 补 `// SAFETY:`（L11/L12），点明依赖 ≥8 对齐。
- 删除过期 `//TODO:revPruning`（见 A6）。
- 修正误拷的 L09 `Machine` 文档（见 A7）。

## 3. 本轮改动清单

- `src/L11_macro/bump_spine_iter.rs` → `XCell`/`CloCell` 加 `#[repr(align(8))]` + 4 个
  编译期对齐断言 + SAFETY 注；新增 `vapp_ok` 并守卫 η 两臂；trait 求解改 Err；
  `Machine` 文档修正。
- `src/L11_macro/unification.rs` → 新增 `v_applicable` + η 守卫；`intersect_go` 失配改
  `None`；`prune_ty` 掩码反转 + 删过期 TODO。
- `src/L11_macro/parser/mod.rs` → 数字 `u64` 解析失败推 IError + Hole；宏展开深度守卫
  （`thread_local` + RAII）覆盖 3 个重解析递归点。
- `src/L11_macro/elaboration.rs` → `solve_multi_trait` 可恢复化；删过期 revPruning 注释。
- `src/L11_macro/{mod,pattern_match,pretty,typeclass}.rs` → 第 1 轮未使用导入/死代码清理
  （本轮复核无回归）。
- `src/L12_canonical/bump_spine_iter.rs` → 对齐断言 + SAFETY；`vapp_ok` η 守卫；
  `solve_multi_trait_ref` 改 Err（双字段 Error）；`Machine` 文档修正。
- `src/L12_canonical/unification.rs` → `v_applicable`（含 `Call`）+ η 守卫；
  `intersect_go` 改 `None`；`prune_ty` 掩码反转 + 删过期 TODO。
- `src/L12_canonical/parser/mod.rs` → `u64` 溢出处理 + 宏展开深度守卫（同 L11）。
- `src/L12_canonical/{canonical,elaboration,mod}.rs` → 第 1 轮清理复核；删过期 revPruning。
- `tests/l11_fast_parity.rs`、`tests/l12_fast_parity.rs` → 各加 5 个 Round-2 探针
  （eta / u64 / 宏递归 / trait 求解 / prune_ty parity）。

## 4. 需要 orchestrator 处置的事项

1. **运行 §C 探针**（上表 5×2 个 `#[test]`）。其中 prune_ty parity 与 solve_multi_trait
   探针的**期望待运行时确认**：若源未真正落到 `prune_meta`/`solve` 路径，断言退化为
   通用 parity（仍应通过）；若分叉即为真实发现，请回传我修正。
2. **L13 共享副本残留（P0）**：`src/L13_namespace/parser/mod.rs`（及 `macros.rs`）与
   L11/L12 同构，同样缺宏展开深度上限。按 ROUND2 本轮不改，建议统一移植三处
   `MacroDepthGuard`（常量/结构可提为共享 `parser_lib`，但那是共享文件）。
3. **Stacked Borrows（P0 级）**：见 §2 记录，建议列入独立重构任务并跑 Miri。
4. L01–L03 交叉审计结论见 `a5-cross-r2.md`（0 个 P0/P1/P2）。

## 5. 设计决定讨论（不属本轮 bug）

- `L11/mod.rs:110` `Tm::Meta(Solved) => None`（"will be fixed in L12"）与
  `L11 unification.rs:69 Err(Basic)` vs `L12:70 Err(Stuck)`：分层刻意分歧，测试锁定，
  非双 oracle 裂缝。
- L11 `solve_multi_trait` 扫描起点用 `MetaVar(0)`（L12 用 `this_meta`）：属既有分层差异，
  本轮只做 `.unwrap()`→Err，不改扫描起点（避免超出 A5 范围的行为变更）；若后续要严格
  对齐 L12，应单列任务并补 parity。
- `canonical.rs` 的 `avoid_recurse` 名字黑名单 + `//TODO: this is incorrect`：被测试锁定
  的近似实现，记录为设计债。
- L11/L12 快版 `Vec<W<'static>> → Vec<W<'a>>` 等生命周期擦除：依赖 `bump.reset()` 前
  句柄消亡的全局不变式，本轮未找到反例（Stacked Borrows 项另计）。
