# A5 Round1 — L10→L11 / L11→L12 跨章节继承连贯性（补跑轮，含 A4/A6/A7 转交处置）

> 切片：`src/L11_macro/**`、`src/L12_canonical/**`、`tests/l11_*`、`tests/l12_*`。
> 本轮为第一轮补跑（前轮限流未执行），并叠加处置 A4-r1 §4-2、A6-r1 §4-1/2/3/4、
> A7-r1 §2.1/§4-A5-2/§4-A5-4/§4-A5-5 转交事项。
> 方法：`git diff --no-index` 逐 hunk 分类（参考版 + 快版孪生全文件）、
> `git log --oneline --stat -- src/L11_macro src/L12_canonical` 单层 commit 扫描、
> 静态推理。硬约束遵守：未运行 cargo、未动 git 状态、只改本切片文件。

## 1. 连贯性判定（verdict）

### 继承对结论

- **L10→L11：有裂缝（本轮已全部修复后收敛）。** 参考/快版 diff 除宏机制
  （owned-token 宏 + 展开信息，`a322a60`）、decl 表世界（`Val::Decl` 名字键取代
  L09/L10 数值哨兵——架构演进，非缺口）、可变全局（let 形态 `create_global`/
  `change_mutable`）、`Val::to_typ` 的 Decl 臂等 L11 增量外，发现 4 类继承裂缝
  （§2-1/2/5/6），全部为 L09/L10 或 L13 已有修复未同步到 L11，本轮补齐。
- **L11→L12：演进为主，裂缝已清零。** 参考/快版 diff 逐 hunk 分类：
  - 参考版：typeclass.rs 整体重写（`Typ` 桥移除 → `val_match`/`vals_eq_ground`
    Val 级求解器、断言表 Vec 化、实例尝试顺序"从后往前"→"从前往后（more
    specific first）"）= 演进；`Val::Call`（d8d817c HDL regNext）、MetaEntry
    三元（创建时快照 + origin）、unify 带 `fuel`、`meta_contrains` Stuck 挂账、
    SmolStr 化、Error 携带重试闭包表（canonical Err 重试）= 演进。
  - 快版：MetaSnap/三元 MetaEntry、`v_to_ref_val` 桥、`new_meta` 快照化、
    `constraints`、Error 二元、SmolStr = 演进（快版与参考版逐点同构）。
  - 裂缝见 §2-3/4/7/8，本轮修复。
- **上轮修复（f51a0e4/d4c05ea）落位核对：全链在位。** MacroDepthGuard
  （L11 parser:134/1184/1449/1992；L12 parser:136/1237/1574/2117）、u64 可恢复化
  （L11:556 / L12:599）、η 守卫（参考 `v_applicable` L11:17 L12:18；快版
  `vapp_ok` L11:721/2434/2449、L12:772/2485/2500）、单向 trait 匹配（L11
  `match_typ`；L12 `val_match` 同语义——A4 r1 已交叉核对）、`prune_ty` 掩码
  反转（L11:147 L12:149 reverse + 快版 `iter().rev()` L11:3368 L12:3427）、
  `intersect_go` None 化（L11:587 L12:602）、`to_typ` 字面量/Prim None 化
  （L11 typeclass:34）——参考版与快版两侧逐一确认。
- **FINAL §3.1 SB 别名 P0 行号刷新（仍不盲改）**：L11 孪生 `mach_ptr` 字段
  `:2159`、取得 `:3959`、重借回调 `:2425/:2541/:2564`；L12 孪生 `:2210/:4035/
  :2476/:2592/:2615`（本轮行号因插入偏移与上轮报告不同，机制刻画不变，
  与 A3/A4/A5 前轮三重结论一致：Miri + 自由函数化专项立项）。

### FINAL §3.4 needs-verify 四项结论

| 项 | 结论 |
| --- | --- |
| canonical IDDFS `basic_target_limit < target_limit` 跳偶数预算 | **记录，不修。** 证据：调度 `1,3,5,...` 来自作者 perf 调优 `1c7e1aa`（初版 `*= 3` 改 `+= 2`）；调用方固定 `depth=5, target_limit=6`（elaboration.rs:368），实际尝试 {1,3,5}——最小预算 b≤5 的实例均能解出（预算单调 → 首解一致），仅 b 恰为 6 一角不完备；改调度会改重试成功路径的合成结果字节（违反硬约束 3，无触发例佐证当前是 bug）。锚点：`canonical.rs:19-26`、git `55d2e00`/`1c7e1aa`、本层 README。 |
| `vals_eq_ground` 把 Flex 视为等于一切 | **刻意语义，保留 + 注释登记。** 与 `val_match` 的 Flex 宽放（:129 有注释锚定）同一策略：实例匹配容忍未解实参靠后续 refine；收紧会破坏既有可解实例选择。已补注释（typeclass.rs vals_eq_ground_impl Flex 臂）。 |
| Match 分支忽略 case 表（TODO） | **已修**（L13 同款修复移植）：pattern 逐结构比较 + 分支体 Rc 指针判"同源"（L13 typeclass.rs:409-421 的修复注释与修法），`Rc` 导入已在本文件使用面内。参考版与快版共用 `Synth`，一处修复双 oracle。 |
| 快版 `v_to_ref_val` 链头 `unreachable!` | **已修（panic → 宽放）**：链长恒 ≥1，`acc` 初值必被实参循环覆写为 `Flex(u32::MAX)` 宽放（既有口径），tag-7 链头不可观察——`unreachable!` 改为同款占位，与 rigid 链路径同判定（L12 bump_spine_iter.rs `v_to_ref_val`，doc 同步）。 |

### 计数

- **P0: 0  P1: 5（全部已修）  P2: 4（已修 3：B3 flex 短路、Match case 表、
  v_to_ref_val panic；记录 1：L12 孪生 Decl 展开燃料臂缺口，注释失实已订正）
  P3: 3（已修 2：L12 go_ix 文案对齐、L10 孪生过期注释【转交 A4】；补 1：两份 README）**
- needs-verify 剩余：3（canonical 调度【已定性记录】、solve_multi_trait 上下文
  策略、no_metas quote 版——均为"无已知触发例 + 需专项/重构"的登记项）
- **本切片 0 个未处置 P0–P2：是**（唯一记录态 P2 为机制缺口登记，不盲补属
  BRIEF §2 判据内的正确处置）。

## 2. 裂缝/分歧清单（逐条）

| # | 位置 | 证据 / 机制 | 分类 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | L11/L12 快版 unify 的 (Obj,Obj) 合同臂位次（修前 L11:2657 / L12:2708） | A4-r1 §4-2 转交。臂在流程末尾，tag2×tag2 Obj 头链被链分派"同头 Obj→false（修前 :2367）/异头非 flex→false（修前 :2376）"提前判败；参考版臂（L11 unification.rs:780 / L12 :829）对同字段投影可判等 → 参考 Ok/快版 Err parity 裂缝；`:1936/:2076` 处"参考版无 (Obj,Obj) 臂"注释过期 | 裂缝（P1，parity） | **已修**：照抄 A4 修 L10 的同款——臂前移到中性链分派之前（含注释文案与尾注），is_objheaded 文档、同头/异头注释贴齐 L10 修后口径；修后两文件臂区域逐行一致（diff 验证） |
| 2 | L11/L12 参考版 pattern_match.rs 探测隔离丢失（修前 :174-178 / :176-180） | A7-r1 §2.1 转交（P1）。`165313d` 注释掉 `temp_infer = infer.clone()`，`filter_accessible_constrs` 的 check_pm 探测直接跑主 infer：fresh meta 泄漏 + 探测期求解无回滚；同层快版走 `run_pure_probe` 整表快照/回滚（:4681），L09:204/L10:182 保留克隆——全链唯一失去隔离的两层；`?N` 归一化掩盖泄漏 | 裂缝（P1） | **已修**：恢复 `let mut temp_infer = infer.clone();` 并以 `temp_infer.check_pm` 探测（贴齐 L09/L10 现状与快版隔离语义），加三行注释锚定"探测不得泄入主 infer"；L13 窄快照优化仍由 A4-r1 §4 转交线处理（perf，非本轮正确性范畴） |
| 3 | L12 快版 trait 求解 flex 短路 `v_tag(*f) == 5`（修前 :4120，本轮 :4133） | A6-r1 §4-2 转交（40199ef 同族）。`is_flex` 才同时覆盖裸 meta（tag5）与 meta 头链（`?m x`，tag2+HK_FLEX）；参考版 `Val::Flex(..)` 含 spine 本就两者都拦（unification.rs:514）→ 快版对 meta 头链实参提前放行进求解器 | 裂缝（P1→按本切片口径 P2） | **已修**：`is_flex(&self.spine, *f)` + 注释（L12 bump_spine_iter.rs solve_trait_ref）。L10/L11 孪生走 `val_to_typ` Option 桥已天然拦截，结构不同不适用（grep 证实无同形位） |
| 4 | L12 `vals_eq_ground_impl` Match 分支忽略 case 表（修前 typeclass.rs:227-231 TODO） | FINAL §3.4；L13 已修（typeclass.rs:409-421，注释明言"只比 scrutinee 与 env 长度不健全"） | 裂缝（P2，不健全判等 → 可能混选实例） | **已修**：`c1.len()==c2.len()` + pattern `==` + `Rc::ptr_eq(t1,t2)`（L13 同款），原 TODO 落地 |
| 5 | L11/L12 pattern 可达性记录顺序 + checked_ret 键型 | A6-r1 §4-1 转交（2a0eb6e 同族）。`reachable.insert` 在 `check_pm_final` 之后：走查中 check_pm_final 失败的臂漏记可达 → 误报 unreachable pattern（L13 的 13-adder-tree 实测触发模板）；`checked_ret` 按模式 Raw 键：两个模式相同的不同臂被混同，第二个臂体漏检 | 裂缝（P1） | **已修（4 处）**：L11/L12 参考（pattern_match.rs，`HashSet<usize>` + 先记可达 + 按臂下标）与孪生（bump_spine_iter.rs，`FxHashSet<usize>` 同序），注释贴齐 L13；2a0eb6e 的第三子项 `patcon.to_raw()` 传参 **不适用**——L11/L12 的 `PatternDetail` 无 L13 的隐式槽字段（Any(Span<()>) vs Any(name,param,icit)），patcon 不携带隐式信息，to_raw 只能复原用户模式本身，L13 的越界/复用触发类不存在 |
| 6 | L11 pretty.rs `go_ix` 越界 `panic!`（:46） | A4 已修 L08/L09/L10（`@{ix}` 退化），L11 漏同步——错误文案对浅上下文项 pretty 可达 | 裂缝（P1，用户可达 panic） | **已修**：退化 `format!("@{}", ix)` + 注释贴齐 L10 |
| 7 | L11/L12 pretty.rs SumCase 非 Sum 头 `panic!("Sum case must be applied to a sum")`（L11:219 / L12:241） | 同上族：构造子 `-> ret` 原样存储可为 App 链；A4 已修 L09/L10（`sum_head_name` 降级） | 裂缝（P1） | **已修**：移植 `sum_head_name`（保留本层既有 `头[impls]::case` 显示格式不变，仅 panic 臂退化 `?`），注释贴齐 |
| 8 | L12 pretty.rs `go_ix` 越界返回整句 `"Variable index out of bounds"` 作为变量名（:48） | 不 panic 但与 L08-L11 的 `@{ix}` 位置占位族不对齐（名字位出现整句英文） | 裂缝（P3，文案对齐） | **已修**：`format!("@{}", ix)` + 注释 |
| 9 | L12 快版 unify_iter 头注释"无燃料（参考版无 fuel）"失实（:2190） | 本轮 L11→L12 快版逐 hunk 审计发现：**L12 参考版 unify 带 `fuel` 参数**（Decl 头 `quote+eval` fuel-1 重试臂 + SumCase 重锚 100 配额，unification.rs:553-568/752-756）——A7-r1 §2.3 对 L12 的"参考版无 fuel"表述需订正；孪生无 Decl 展开臂（`constraints` Stuck 挂账已移植） | 裂缝（P2 记录 + P3 注释） | **注释已订正**（unify_iter 头注）；**机制缺口登记不盲补**（Decl 头展开在孪生 decl 存根世界无对应口径、无已知触发例；补齐属语义机制移植，需专项验证）——见 §5 |
| 10 | L12 `v_to_ref_val` 链头 `unreachable!` | FINAL §3.4（见 §1 表） | 裂缝（P2，可达 panic → 宽放） | **已修**（见 §1 表） |
| 11 | L10→L11 快版 diff 其余（宏 elaborator、decl 表、可变全局、Prim 类型臂 12/12.5 替代 L10 宽松臂 12、`declb_of`/MatchStruct、`use super::typeclass::Synth` 共用求解器等） | 逐 hunk 均为 L11 特性演进，双 oracle 同构 | 演进 | 无动作 |
| 12 | L11→L12 参考/快版 diff 其余（Val 级求解器重写、MetaSnap、Val::Call、canonical 重试闭包、SmolStr、`Either` icit 等） | 逐 hunk 均为 L12 演进，快版与参考版逐点同构；实例尝试顺序变化（L11 从后往前 → L12 从前往后 more-specific-first）为 L12 语义演进、层内 parity 锚定 | 演进 | 无动作 |

## 3. 本轮改动清单

- `src/L11_macro/bump_spine_iter.rs` → (Obj,Obj) 合同臂前移 + 三处过期注释纠正（#1）；
  孪生 leaf 可达性先记 + checked_ret 按臂下标（#5）
- `src/L12_canonical/bump_spine_iter.rs` → 同上（#1/#5）；solve_trait_ref flex
  短路 `is_flex`（#3）；unify_iter 头注 fuel 失实订正（#9）；v_to_ref_val 链头
  unreachable → 宽放占位 + doc（#10）
- `src/L11_macro/pattern_match.rs` → 探测隔离恢复（temp_infer 克隆，#2）；
  可达性先记 + checked_ret `HashSet<usize>`（#5）
- `src/L12_canonical/pattern_match.rs` → 同上（#2/#5）
- `src/L11_macro/pretty.rs` → go_ix 越界退化 + `sum_head_name`（#6/#7）
- `src/L12_canonical/pretty.rs` → go_ix 越界 `@{ix}` 对齐 + `sum_head_name`（#7/#8）
- `src/L12_canonical/typeclass.rs` → vals_eq_ground Match case 表比较（#4）；
  Flex 宽放刻意语义注释（§1 表）
- `src/L11_macro/README.md`、`src/L12_canonical/README.md` → 新增（已知分歧登记，
  BRIEF §2）
- 测试：**未改任何测试断言**。本轮修复全部是"panic/漏记/位次不可达 → 参考版
  既有语义对齐"或"探针隔离恢复"，Ok 输出与既有 Err 判定不变；现有
  `l11_fast_parity`/`l12_fast_parity` 套件已覆盖 match 编码路径（含
  filter_accessible_constrs 与卡住 match），由 orchestrator 门禁裁决。
  A7 建议的"探测不改变 ?N 序列"不归一化探针：需实际运行钉字面输出，
  本环境无 cargo，登记为下轮补齐项（§4-4）。

## 4. 需 orchestrator 转交邻居章节的事项

1. **A4（L10）**：`src/L10_typeclass/bump_spine_iter.rs:1959-1961`
   `unify_sp_lockstep` 内注释"参考版无 (Obj,Obj) 臂，交回完整分派后走
   `_` → Err"已过期（参考版有该臂；A4 自己修的合同臂使交回分派后按同字段
   判等）。建议改文案与 L11/L12 本轮口径一致："交回完整分派，由 (Obj,Obj)
   合同臂接管判定（同字段可判等，非恒败）"。
2. **A4（L09/L10）**：pattern 可达性记录顺序 + checked_ret 键型（2a0eb6e
   同族）——L10 `pattern_match.rs:236-250`（`reachable.insert` 在
   check_pm_final 后、`HashSet<Raw>` 按模式 Raw 键）与孪生
   `bump_spine_iter.rs:5913-5928` 同族；L09 形态更异（`HashMap<Raw,Tm>`、
   :251/:265），需按 L09 自身结构评估。本轮只修了 L11/L12。
3. **A7（l11bench/l12bench）**：**church 排除理由不成立**——A7 疑虑引的
   natadd_src 注释（L11 twin:7682-7684）指的是"λ 体 + Type N 注解"组合，
   而 church 的 `def Nat : Type 1 = (N : Type 0) -> ...` 是 **Pi 体** +
   Type 注解；该形态在现有绿测中有直接反例
   （`tests/l11_fast_parity.rs:402` `def String : Type 0 = Type 0`、
   `:628` / `l12:628` `def Eq [A : Type 0] ... : Type 0 = (P : A -> Type 0) -> P x -> P y`）；
   church 的 λ 体 def（`add`/`p0`）用的恰是 natadd 注释认可的完整类型注解。
   建议下轮 cargo 可用时实测后把 church 补回 l11bench/l12bench（生成器现成）。
   **gadt 排除理由成立**：证据链 `0242720`（test9/test10 基线即挂：Vec 长度
   依赖消解失败 / `prune_vflex` unreachable）+ `tests/l11_fast_parity.rs:474-476`
   快版 walk 缺陷区注释。
4. **A7（测试建议备案）**：A7-r1 §2.1 的"探测不改变 ?N 序列"不归一化回归
   探针，因需运行钉字面输出，本轮未加；隔离修复本身由现有 match 套件
   （经 `run_pure_probe` vs temp_infer 双口径一致）保障，待下轮补探针。
5. **（同步信息，无需行动）**：A7-r1 §2.3 燃料矩阵中 L12 一行需按本报告 §2-9
   订正（L12 参考版有 unify fuel）；L09/L10 的 fuel 缺失论证仍归 A4。

## 5. 刻意分歧登记（锚点）

1. **L11 起为 decl 表/名字键世界**（无 L09/L10 数值全局哨兵）：`Val::Decl`、
   `Cxt.decl`、可变全局 let 形态——架构演进，哨兵边界纪律不适用。
   锚点：L11/L12 twin 模块头；A7-r1 §2.2 表。
2. **L12 快版孪生不移植 canonical/`iddfs`**：Err 重试路径专属。
   锚点：`src/L12_canonical/bump_spine_iter.rs:29-30`、本层 README。
3. **`iddfs` 预算调度 1,3,5,...（步进 2）**：作者调优（55d2e00 初版 `*= 3` →
   1c7e1aa `+= 2`）；b == target_limit == 6 一角不完备，未盲改。
   锚点：`canonical.rs:19-26`、本层 README、本报告 §1。
4. **`vals_eq_ground`/`val_match` 的 Flex 宽放**：实例匹配容忍未解实参。
   锚点：`src/L12_canonical/typeclass.rs` 两处注释、本层 README。
5. **快版 `v_to_ref_val` 链降级为宽放占位**：解码只用于求解边界非热路径；
   参考 `Val::Flex(..)` 含 spine 同为宽放，观察面相同。
   锚点：`src/L12_canonical/bump_spine_iter.rs` v_to_ref_val doc、本层 README。
6. **快版 unify 无 Decl 头展开燃料臂（L12 参考 vs 孪生机制缺口，登记待专项）**：
   参考版 `(Decl(..), _)` quote+eval fuel-1 重试无孪生对应口径，无已知触发例；
   `constraints` Stuck 挂账已移植。锚点：L12 twin unify_iter 头注（本轮订正后）、
   本层 README、L13 参照（18-utils 分叉闸先例）。
7. **`solve_multi_trait` 用调用方上下文（L12 参考 unification.rs:488 + 孪生透传，
   needs-verify 承接自 A6-r1 §4-3）**：L13（d3df88e）改创建处快照依赖其
   MetaSnap/重命名基础设施；L12 的 def 级求解（`solve_multi_trait(0)`）确有
   "创建层深于求解层"，但 L11/L12 求解以实例名 `infer_expr` 在调用方 cxt 重
   elaborate、不经旧值 rename，无已知越界触发例；对齐属引擎级语义变更
   （BRIEF §2 代价判据），登记不盲改。锚点：本报告；A6-r1 §4-3。
8. **`no_metas` 为 quote 版（L11/L12 参考+孪生，承接 A6-r1 §4-4）**：
   L13 值图遍历 + 指针去重（71e11ae，`L13 mod.rs:519-565`、twin:12063）未
   下沉；触发类（模块/bundle 链巨型/自引用解图、深上下文 NM_QUOTE_LVL）
   为 L13-only；且 L13 版结果类型/遍历器为架构耦合改造，不满足最小 diff。
   锚点：`src/L11_macro/mod.rs:98`、`src/L12_canonical/mod.rs:102`、
   `L12 bump_spine_iter.rs no_metas`、本层 README。
9. **`declb_of` 无缓存（承接 A7-r1 §4-A5-2，评审判定：不做）**：指针键缓存
   可行（Decls 为 Rc 地址稳定）但缓存条目持有 bump 分配（`bump.alloc(Tm::Decl)`
   /`alloc_str`），与 bench 口径 `bump.reset()` 每轮复位耦合（悬空风险），
   需 reset 钩子才安全；热点仅定理证明/复杂模式负载，无 A/B 收益数据。
   锚点：`src/L11_macro/bump_spine_iter.rs declb_of`、本层 README。
10. **(Obj,Obj) 合同臂位次（修后口径，跨 L08-L12 快版统一）**：位相等捷径对
    tag7/Obj 头链关闭、臂先于链分派。锚点：各层 twin `is_objheaded` doc +
    本轮修后注释。

## 6. 残余 / needs-verify（诚实记录）

1. **[P2·记录] L12 孪生缺 Decl 头展开燃料臂**（§5-6）：无已知触发例；
   补齐需以 L12 参考版 Decl-头 unify 行为构造回归例后专项移植。
2. **[needs-verify] canonical IDDFS 调度**（§5-3）：已定性为作者调优 +
   一角不完备；如未来出现"grant 6 预算仍 Err"的实测例，改 `<=` 并连带
   评估重试结果字节。
3. **[needs-verify] solve_multi_trait 上下文策略**（§5-7）与 **no_metas
   quote 版**（§5-8）：均无本层触发例，保持登记。
4. **[P3] 本切片无 blackbox golden 套件**（A7-r1 §2.6/§4-A5-6）：维持 A7
   建议，待与"探测 ?N 序列探针"（§4-4）一并下轮补齐。

## 7. Round 2 补遗（与 A3/A4/A6 的 r2 并行产生的交叉项处置）

> 全部在本切片所有权内；硬约束同前（未运行 cargo、未动 git、最小 diff）。

### R2-1 【阻塞门禁·已修】l12_fast_parity 探针 E0277（A6-r2 §1 同族）+ l11 同款

- 位置：`tests/l12_fast_parity.rs`（修前 :370/:384，即
  `probe_u64_literal_overflow_no_panic` / `probe_macro_self_recursion_depth_limit`）。
  **复核发现 `tests/l11_fast_parity.rs` :571/:587 有逐字同款缺陷**（同
  `spawn(move || L11_macro::parser::parser(src, 0))`——闭包返回
  `Option<(Vec<Decl>, Vec<IError>)>`，`Decl→Raw→Rc<Tm>` 为 `!Send`，E0277
  同样阻塞 `cargo check --all-targets`），一并修复。
- 修法 = A6 修 l13 的同款（a6-r2.md §2-A）：`Some`/错误条数在子线程内解包，
  线程只回传 `(bool, usize)`；外层保留原三条断言及文案（join Err = "不应
  panic"、是否 Some、错误条数 > 0）；8MB 大栈形态不变；各加一行 `!Send`
  原因注释。
- 静态自查（编译由 orchestrator 复跑裁决）：`(bool, usize)` 全 Send；
  `join()` 返回 `Result<(bool, usize), Box<dyn Any + Send>>`；`parser` 的
  `pub` 可见性不变；闭包捕获 `src: &'static str` Send。其余 spawn 点
  （l11:31/:42/:497、l12:38/:49/:300）为既有已编译 harness（`run`/`run_input`
  返回 String 输出行），无需改动。

### R2-2 【A3→A5·已修】alloc_xname 缺口补齐（L11/L12 快版孪生）

- 位置：L11 `bump_spine_iter.rs` lams_from_ty（修前 :3439 的
  `bump.alloc_str(&format!("x{}", lp))`，r2 后 :3460）/ L12 同构（修前
  :3504）。A3 的五方样板（L05/L06/L07/L08 helper 主体 md5 一致）未落到
  L11/L12。
- 已修：按样板逐字补入 `alloc_xname`（栈上 11 字节缓冲格式化 `x{n}`，省每
  binder 一次 system-heap String）+ 调用点替换；六层 helper 主体窗口 md5
  复核：L06=L07=L08=**L11=L12**（L05 窗口差异仅为下一函数的文档注释文字，
  helper 本体六方逐字节一致）。
- 参考侧宿主确认（对齐 A3 对 L07/L08 的"参考版无宿主"口径）：L11 参考
  `unification.rs:386`、L12 `:394` 的改名走 `format!("x{}")`（L12 再包
  `SmolStr::new`，短串内联存储）——参考版定位可读优先、该处非密集热点，
  **L11/L12 参考侧均无移植宿主**。另核 L09/L10/L13 孪生无
  `alloc_str(&format!)` 同形位（L09/L10 的 solve/lams 管线结构不同），
  样板无需再落第四、五层之外。

### R2-3 【A4→A5·已贴齐】SumCase 显示臂与 L09/L10 的 r2 口径核对

- 核对结论：**r1 时不同款**——A4 r2 把 L09/L10 贴齐 L08（`{head}.mk` 前缀
  去重不再叠 `::` + 实参 ATP 前 precedence、隐式 `[名]` 显式裸名、空格
  连接），而我在 r1 只给 L11/L12 做了 panic 降级、保留了旧显示
  （`{head}[impls]::{case}({expls 逗号连接})`）。
- 已贴齐：L11/L12 的 SumCase 通用臂整体替换为 L10 同款（L12 保留其
  `pretty_nat` Nat 特化守卫臂在通用臂之前，indent 版实参打印
  `pretty_tm_indent(ATP, indent, …)`）；`sum_head_name` 降级语义不变。
  L11 与 L10 臂逐行 diff 仅注释换行差异（层级名单加长所致）。
- 行为影响评估：pretty 为参考版/快版共用手（双 oracle 同步变化，fast_parity
  互检不敏感）；L09–L12 无字面 golden 套件（A7-r1 §2.6），无断言锁定旧
  显示格式。

### R2-4 【A6"待 L12 复验"·自验结论】r1 的 (Obj,Obj) 臂位次修复 L12 侧

- 结论：**L12 侧修复自洽，复验通过。** 证据（r2 行号）：
  - 合同臂位于中性链分派**之前**：L12 twin :2352（臂）→ 链分派内防御注释
    :2449/:2466（"已被上文合同臂接管"），序正确；L11 同构（:2295 →
    :2392/:2409）。
  - :2754 尾注（"合同臂已前移…否则永远到不了臂本身"）在位；is_objheaded
    doc（:1987）与 lockstep 注释（:2129）无"参考版无 (Obj,Obj) 臂"残留。
  - 参考版臂在位：`L12_canonical/unification.rs:829`
    `(Val::Obj(a1,b1,sp1), Val::Obj(a2,b2,sp2)) if b1.data == b2.data`。
  - L11/L12 两文件臂区域逐行 diff 一致（r1 已验，本轮行号漂移后复核锚点
    不变）。A6 的 L12 边界复验标记可据此解除。

### R2-5 改动清单（Round 2 增量）

- `tests/l12_fast_parity.rs` → 两枚 spawn 探针 E0277 修复（R2-1）
- `tests/l11_fast_parity.rs` → 同款两枚探针 E0277 修复（R2-1 复核发现）
- `src/L11_macro/bump_spine_iter.rs` → alloc_xname helper + 调用点（R2-2）
- `src/L12_canonical/bump_spine_iter.rs` → 同（R2-2）
- `src/L11_macro/pretty.rs`、`src/L12_canonical/pretty.rs` → SumCase 显示臂
  贴齐 L08/L09/L10 口径（R2-3）
- 测试断言改动仅为 E0277 编译修复（断言语义与文案逐字保留），符合
  "改测试断言必须有独立证据"豁免口径——证据 = E0277 本身（Send 不满足
  属编译错误，非行为断言变更）。
