# A2 Round1 — L03→L04 / L04→L05 / L05→L06（src/L04_implicit、src/L05_pruning、src/L06_string）

## 1. 连贯性判定（verdict）

- **L03→L04：连贯**。参考版 `mod.rs` 全部 41 个 hunk、`parser/` 两文件全部 hunk
  逐条分类为**演进**（Icit 穿线、隐式插入 insert/insert_t/insert_until_name、
  命名隐式、NameOrigin/Inserted binder、上游 04 示例与测试换装）；L03 时代
  的全部修复（env_ext_defs tip 判定、受控内联环 + 链长 fail-fast 移除、
  quote 链 β 防御 83521bf、align8）在 L04 双版本同源位置齐备。快版差异
  （RenBuf 换代缓冲、eval_fresh、AppBds 跳段）均为 readme §性能版要点
  记录在案的演进。
- **L04→L05：连贯**。参考版 34 个 hunk 全部为 pruning 特性演进（typed
  metas、Locals/close_ty、AppPruning、非线性 invert + 掩码、prune_ty
  revPruning、prune_vflex foldr 外先序、intersect/flex_flex、lams 改沿类型、
  Cxt 重构 src_names/pruning、displayMetas 带类型）；η 守卫原样带过；无 L04
  内容丢失。快版 RenBuf 哨兵（NONE_MARK + has_mark）与掩码内先序
  （4aaddd7）、intersect 直败（dba0fb8）、quote 链 β 修复（3ca3a42）齐备。
- **L05→L06：有裂缝（1 处，已修）**。单文件拆分为 syntax/cxt/elaboration/
  unification/pretty/parser 的组织变化逐文件对位忠实；port-audit 补齐项
  （递归 def fake_bind 三处 ×双版本、pretty 越界退化两处）核验落地且注释
  与参考版同源；η 守卫（含 Decl 头）、tag 7 位相等守卫、intersect 直败、
  prune_ty revPruning、掩码内先序全部在位。唯一裂缝：**a806ff0 的
  alloc_xname 未继承**（见 §2.1，已修）。
- 计数：**P0: 0  P1: 0  P2: 1（已修）  P3: 4  needs-verify: 0**
- 本切片是否收敛（0 个未处置 P0–P2）：**是**。

## 2. 裂缝/分歧清单（逐条：位置、证据、分类、处置）

### 2.1 [P2·裂缝·已修] a806ff0「lams 的 x{n} 名改栈上格式化」未继承到 L06 快版
- 位置：`src/L06_string/bump_spine_iter.rs` `lams_from_ty`（修前 `bump.alloc_str(&format!("x{}", lp))`）。
- 证据：a806ff0（2026-09-10，`--stat` 仅 L03/L04/L05 三文件 +21）晚于 L06 的
  移植 commit 58109c8（2026-09-04），L06 fork 后未跟进；L05 同函数用
  `alloc_xname(bump, lp)`，L06 退化为每匿名 binder 一次 system-heap String
  分配——正是该 commit 要消除的形态（solve/prune 密集负载 16384 层累积
  上万次）。产出字符串不变，纯性能回归。
- 处置：**已修**。按 L05 逐字移植 `alloc_xname` 助手 + 替换调用点（与 L05
  版本 diff 逐字节一致，注释补一行来源说明）。参考版不涉（a806ff0 本就
  只动快版；L06 参考版 `unification::lams_go` 与 L05 参考版同为 `format!`）。
- 同族外溢：L07/L08/L09 快版同源位置同样缺失（见 §4 转交）。

### 2.2 [P3·刻意分歧·登记] L06 的 pretty 优雅退化不回灌 L04/L05
- 位置：L06 `pretty.rs:33-48`（go_ix 越界固定文案）、`pretty.rs:54-110`
  （go_app_pruning 掩码/名字表错位退化）；L04/L05 同源位置：参考版 `go`
  的 Var 直索引 / L05 `go_pr_i` 的 `_ => panic!("impossible")`。
- 论证：L06 的修复动机是「hover 类调用方拿错名字表」与
  string_to_global_type 制造的深签名 meta 显示——均为 L06 特有调用面；
  L04/L05 的 pretty 只消费当前 cxt 名字表下的 nf/elab/show 输出，AppPruning
  的掩码长度恒 ≤ 打印上下文名字表长度，无可达触发路径（上游 05 Haskell
  同为 impossible）。移植属无锚点的防御性改动，保留并记录。
- 锚点：本轮在 `src/L04_implicit/readme.md`、`src/L05_pruning/readme.md`
  「已知限制」各补一句（见 §3）。

### 2.3 [P3·演进·登记] L06 快版测试换装（相对 L05）
- L05 的 `unify_failure_message`/`icit_error_parity`/`display_metas` 随
  **程序形态变化**消失：L06 顶层是 decl 序列（def/println），无 elab 模式与
  全文错误比对（readme「已知限制」已文档化：判定一致 + icit/命名 λ 两类
  无 Span 消息例外，由 `named_lambda_matches_by_name` 等钉住）；
  `dup_nf/dup_node_counts/dup_memo_shares_forced_subtree`（quote 记忆化
  共享锚点）未随迁，`deep_church_65536`→`deep_workloads`、错误判定→
  `error_parity`。机制本体（QuoteMemo 常驻 92e7039、稳态复用、记忆化
  屏障）经逐 hunk 核对无缺失；纯覆盖面变薄，记 P3 不补测（L06 已有
  blackbox v3 68 例 + steady_state_reuse 覆盖同族路径）。

### 2.4 [P3·演进·登记] L03 的 shadowing 互检测试在 L04 起折入组合用例
- L03 `name_map_shadowing_matches_basic` 在 L04/L05 折入
  `inserted_binder_invisible`（L04 bump:2767、L05 bump:3509 注释明言
  shadowing + inserted 不可见二合一）；L06 侧由 blackbox v3
  `binder_shadows_builtin_name` 钉住。机制（源码 binder 才入 name_map/trail）
  逐层在位，无裂缝。

### 2.5 不适用项（BRIEF §3.1 清单中与本切片无关者）
- MacroDepthGuard（L11/L12 宏机制，L04–L06 无宏）；trait 单向匹配
  `match_typ(goal, pattern)`（L10+）；`to_typ`/u64 字面量（L09+）；
  `GLOBAL_BASE` 哨兵边界（L09+ 全局表按名/按址索引，L03–L06 无该机制，
  grep 0 命中）。FINAL §3.1 的 SB 别名 P0（`solve_multi_trait_ref` 整机
  重借）在 L04–L06 无同构代码（无 typeclass 求解路径），unsafe 仅
  packed-word 解码与已文档化的 workbuf 洗白。

## 3. 本轮改动清单（文件 → 一句话）

- `src/L06_string/bump_spine_iter.rs` → 补齐 a806ff0：新增 `alloc_xname`
  助手（与 L05 逐字一致），`lams_from_ty` 匿名 binder 名改栈上格式化。
- `src/L05_pruning/readme.md` → 「已知限制」补 pretty 越界保留 panic 的
  刻意分歧一句；「怎么跑」补 blackbox v2/v3 两行（文档漂移 P3）。
- `src/L04_implicit/readme.md` → 「已知限制」补 pretty Var 越界保留 panic
  一句；「怎么跑」补 blackbox_v2 一行（文档漂移 P3）。
- `src/L06_string/readme.md` → 「怎么跑」补 blackbox_v3 一行（文档漂移 P3）。

## 4. 需 orchestrator 转交邻居章节的事项

1. **转 A3（L07/L08）**：a806ff0 同源缺口——
   `src/L07_sum_type/bump_spine_iter.rs:4014`、
   `src/L08_product_type/bump_spine_iter.rs:4079` 的
   `bump.alloc_str(&format!("x{}", lp))`（lams_from_ty），按 L05/L06 现版
   `alloc_xname` 同款补齐（本轮已把 L06 修为样板）。
2. **转 A4（L09 起）**：`src/L09_mltt/bump_spine_iter.rs:3127` 同款缺口；
   A4 顺带核对其余快版层（L10–L12）是否有同函数。
3. **转 A1（L03 侧）**：`src/L03_holes/parser/mod.rs` 的 `kw`/`string` 仍是
   7ff008d 风格清扫前的旧形（`input.get(1..).map(...)` 三层嵌套）——纯
   风格（P3），7ff008d 只落了 L02/L04/L05，是否对齐由 A1 定夺。
4. **FYI 转 A3**：L06 参考版 `unify` 的 (String, Decl) 宽松臂以
   `decls.contains_key` 把关（`unification.rs:495-503`）；L07 的 decl 表若
   有同族「卡住头 vs 具体类型」臂，核对把关面是否一致（只读提醒，非
   断言有裂缝）。

## 5. 刻意分歧登记（锚点：测试名/文档行）

| 分歧 | 锚点 |
| --- | --- |
| L04/L05 pretty 越界保留 panic（L06 优雅退化不回灌） | 本轮新增 `src/L04_implicit/readme.md`、`src/L05_pruning/readme.md`「已知限制」各一句；上游 05 Main.hs 同为 impossible |
| L06 错误消息只比判定不比全文（快版 span 全零） | `src/L06_string/readme.md`「已知限制·错误消息内容」；`tests/l06_blackbox_v3.rs` `error_parity` 系测试 |
| L06 无 elab/display_metas（decl 序列程序形态） | `src/L06_string/mod.rs:338-352` `run()` 只有 def/println 消费；readme「语法要点」 |
| L06 (Lit,Lit) 恒败、同名 Decl 逐参（无字面量臂） | `src/L06_string/readme.md`「unify 的 L06 臂」第 5 条；`tests/l06_blackbox_v3.rs` `bug1_*` 族 |
| intersect 长度失配直败零比较（非共同前缀比较） | `src/L05_pruning/readme.md`「已知限制」；`intersect_bump` 文档注释 |
| 低层 `?N` 洞显示契约（不移植 L13 no_metas） | `docs/l13-to-low-layer-port-audit.md` §不移植；`tests/l06_blackbox_v2.rs:194-195/270` |
| unify/force fuel 不移植（L06 无可触发失控类） | `docs/l13-to-low-layer-port-audit.md` §fuel |
| 枚举裸名「后注册者覆盖」（L07+，本切片无涉） | port-audit §枚举构造子裸名别名查重（转 A3 参考） |

## 附：核验过且无需改动的继承链要点（备查）

- align8 + 静态断言：L04 bump:248/260、L05 bump:273/285、L06
  bump:209(XCell)/302/314，`align_of >= 8` 断言各层 3-4 处齐备（b621789）。
- η applicability 守卫：L04 mod:140/437/441 + bump:163/1008/1020；L05
  mod:180/939/943 + bump:181/1152/1164；L06 unification.rs:16/476/481（含
  Decl 头，层内正确泛化）+ bump:833/1737/1756。
- prune_ty 掩码反转（revPruning 外→内）：L05 mod:589-604、L06
  unification.rs:131-147；快版掩码内先序产出（L05 bump:1443-1468、L06
  bump:2103-2128）+ `prune_ty_bump` 的 `mask_inner_first.iter().rev()` 消费
  约定（L05 bump:1846、L06 bump:2547）两侧同源一致（4aaddd7/1b836c7）。
- intersect_go 长度失配优雅化：L05 mod:871-889（None 回落）、L06
  unification.rs:427-431；快版直败（L06 bump intersect_bump `if n1 != n2
  { return false }`）。
- port-audit 补齐项（a9d0322）：参考版 `elaboration.rs:179-196`（redefine +
  递归占位）、`:226-234`（Var decl 回退）、`ty_precheck:126-141`；快版
  三处 `bump:3311-3346`/`3381-3410`/`3580-3609`，注释与行为同源。
- quote 链陈旧函数部分 β 修复：L04 bump 二叉 fallback≈786-805 + ChainRun
  bail≈885-903（防御性，注释明言潜伏）；L05 bump≈813-837/900-938；L06
  bump≈1377-1410/1464-1510（真可达，实测复现）。
- env_ext_defs tip+binds 判定（a483331/985390b）：L03:208/L04:232/L05:248/
  L06:277 四层逐字一致。
- 轮清空清 spine（c7d5164）、QuoteMemo 常驻（92e7039）、fresh_meta 两级
  快路径（3c30b40）：git log --stat 证实 L04/L05/L06 三层同 commit 落位，
  现码在位。
