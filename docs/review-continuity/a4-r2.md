# A4 Round2 — L08→L09 / L09→L10（转交处置 + 复验）

- 切片：`src/L09_mltt/**`、`src/L10_typeclass/**`、`tests/l09_*`、`tests/l10_*`
- 输入转交：A2→A4（a806ff0 `alloc_xname` 缺口 + 核对 L10 同位）、
  A7→A4（fuel P2 needs-verify、L10 探测整克隆 Infer P2、golden 套件 P3）、
  A3→A4（a3-r2 §4-1 alloc_xname 再转交 + §4-4 η 守卫裁决回执）。
- 硬约束遵守：未运行 cargo、未动 git 状态、只改本切片文件。
- 禁 cargo 说明：golden 预期值全部为**静态推导**（推导链见 §2-5），orchestrator
  集中跑测试时若 golden 失败，失败差异本身即本轮显示改动的裁决数据。

## 1. 连贯性判定（verdict）

- **L08→L09：连贯**（本轮补齐 a806ff0 `alloc_xname`、`SumCase` 显示贴齐 L08、
  探测隔离对齐 L13/快版口径后收敛；"再定基层"时代缺口族维持刻意分歧，
  README 登记本轮补全——新增"替换面"条目：decl 表→global 哨兵表、meta
  快照/回滚、pm 状态、Match pending splice）。
- **L09→L10：连贯**（两版同步做 §2-1/2-3/2-4 三项，参考版↔快版无新分叉）。
- **A7 转交的 fuel 项最终裁决：不适用、不补**（§2-2 逐失控类论证，落
  两层 README 固定）。与 A4-r1 "不补、已登记"结论一致，但本轮把一句话
  登记升级为**带文件:行号的六类逐条论证**，A7 的 P2就此销项。
- P0: 0  P1: 0  P2: 2（probe 窄化已修；fuel 裁决不适用登记）  P3: 2
  （SumCase 显示贴齐已修；卡住 match 显示简形登记） needs-verify: 2
- **本切片是否收敛（0 个未处置 P0–P2）：是**

## 2. 裂缝/分歧清单（逐条：位置、证据、分类、处置）

### 2-1 [P2-perf·裂缝·已修] a806ff0 `alloc_xname` 未继承（A2/A3→A4 转交）
- 位置（修前）：`L09/bump_spine_iter.rs:3144`、`L10/bump_spine_iter.rs:3289`
  的 `bump.alloc_str(&format!("x{}", lp))`（`lams_from_ty`，`"_"` binder 改名）。
- 处置：**已修**。按 L05/L06/L07/L08 四方同款样板：`alloc_xname`（[u8;11]
  栈缓冲右到左写数字）插在 `lams_from_ty` 文档注释前 + 调用点替换。现位：
  `L09:3122/3164`、`L10:3267/3309`。产串与 `format!("x{}", lp)` 逐字节相同
  （"x"+u32 十进制），纯 perf、输出不变。参考版不涉（`lams_go` 用 `format!`
  生成 `x{n}`，非 bump 世界，与 L06 参考版同立项场）。
- L11/L12/L13 同缺口仍开放（本轮实测 `L11:3443`、`L12:3508`、`L13:5301`），
  转交清单见 §4。

### 2-2 [P2·最终裁决：不适用·已登记] L09/L10 无 unify/force fuel（A7 §2.3）
- **结论：不补。** L08 fuel 的 6 个烧点（`L08/mod.rs:376/388/397/420/435/448`
  + unify 递归烧点 `L08/unification.rs:581-593`）所防的失控类在 L09/L10
  逐一**无同构触发面**：
  1. meta 解链间接环——L08 无跨 meta occurs check 才需 fuel 兜底
     （`L08/mod.rs:361-363`）；L09/L10 solve 经 `occ: Some(m)` 的 rename 做
     逐 meta occurs check（`L09/unification.rs:419-425`、`L10:429`），解链
     无环。快版 force 注释同证（`L09/bump:675-676`、`L10/bump:702`）。
  2. pm_defs 精化展开——L09/L10 无 pm_defs/pm_solvable（grep 0 命中）。
  3. Match 重选——force 只有 Flex 臂（`L09/mod.rs:232-241`、
     `L09/bump:670-682`），卡住 match 恒卡住，pending splice 未继承。
  4. decl 展开——全局表存终值；递归自引用在 def/enum 检查期预注册为卡住
     Rigid 占位（`L09/elaboration.rs:289/374`），值层无 unfold 算子。
  5. Prim 归约——`Val::Prim` 为无 spine 裸标记，`v_app` 对它 panic，永无
     Prim 头链（`L09/bump:25-27`）。
  6. unify 深度失控（`L08/unification.rs:581` 的"索引槽互相嵌入"）——其
     失控源是比较途中 force 展开 decl 使结构度量增长；L09/L10 比较两侧为
     已终值，unify 递归结构性递减（Match 臂另有 `avoid_recursive` 封口，
     `L09/unification.rs:338-345`）。
  L10 自有的新失控类（实例求解依赖子目标搜索）已自带护栏：effort≤1000
  上限（`typeclass.rs:212-217`）+ 子目标答案缓存（`:316-345`），与 L08
  fuel 无关。**盲加 fuel 属不可触发的死护栏**，且 L08 fuel 耗尽有可见行为
  （`(fuel exhausted)` 诊断 `L08/mod.rs:946-953`、unify fuel=0 判 Err），
  有 Ok→Err 行为变更风险，违反 parity 约束。
- 处置：**已写进两层 README 固定**（`L09/README.md` 时代缺口第 2 条全文、
  `L10/README.md` 指针 + typeclass 护栏说明）。A7 P2 销项。

### 2-3 [P2-perf·裂缝·已修] 探测整克隆 Infer → L13 同款窄快照（A7 §2.4）
- 位置（修前）：`L10/pattern_match.rs:182` `let mut temp_infer = infer.clone()`
  （每构造器一次**整 Infer 克隆**：meta + global + trait_solver + 两张注册表）；
  `L09/pattern_match.rs:204` 同族（meta + global 两字段克隆）。
- 处置：**已修（L09+L10 同步，参考版+快版口径一致）**。L13 同款：快路径
  之后的探测循环包进闭包，循环前 `infer.meta.clone()` 整表快照、循环后
  **无条件**回滚（Success/Error 皆然）；现位 `L09/pattern_match.rs:184`、
  `L10:164`。字段可分性论证（写进代码注释）：
  - `meta` 是唯一探测面（Hole 精化的 fresh 分配 + unify/探测求解）；
  - `global`/`trait_definition`/`trait_out_param` 是注册表，探测只做表达式级
    infer/check 无 decl 登记，探测期只读；
  - `trait_solver` 每次真查询先 `clean()`（`L10/unification.rs:476`），
    瞬态残留不跨查询生效，无需回滚。
- **与快版口径的一致性**：两层快版的探测本就统一走 `run_pure_probe`
  （metas 整表快照换入换出、整循环一回滚：`L09/bump:4187/5086`、
  `L10/bump:4498/5773`）——参考版修后与快版**同粒度同机制**，也与 L13
  参考版（`L13/pattern_match.rs:266-335`）同构。两层孪生头部注释同步纠正
  （"参考版克隆 temp_infer 的对应物"已过期）。
- **两项行为收敛（如实登记，均为向快版/L13 口径收敛）**：
  a. 探测期 meta 变化（含 params 循环里 Hole 的 fresh 分配）现在随探测
     回滚；修前参考版 params 循环的分配**滞留**主 meta 表。`?N` 编号差异
     属已文档化归一化偏差，parity 套件不受影响。
  b. 回滚粒度从"每构造器"变"整循环"（探测 1 的解对探测 2 可见）——与
     快版 `run_pure_probe`、L13 完全一致；修前快版即此行为且 parity 全绿，
     故参考版向其收敛不会翻转任何 parity 判定。

### 2-4 [P3·~~裂缝·已修~~ **R3 作废回滚**] SumCase 显示格式未继承 L08（本轮 golden 推导时发现）
> **Round 3 裁决：本条方向贴反，已整体回滚**——L08 的空格格式是 L08 本地
> 形态；L09 起"再定基层"重写的显示血统即 comma 格式并延续到终点章
> （`src/L13_namespace/legacy_tests.rs:604/716`、`src/L12_canonical/mod.rs:1479/1591`
> 锁定 `Vec[Bool]::cons(1, Bool::false, …)`）。回滚与 golden 重钉见文末
> "Round 3 补救"。

- 证据：L08 的 SumCase 臂做 `.mk` 前缀去重 + 实参空格连接 + 隐式槽
  `[名]`（`L08/pretty.rs:151-176`），并有 blackbox 钉值锁定
  （`tests/l08_blackbox.rs:1543` `Point.mk(Nat::zero Nat::succ(Nat::zero))`、
  `:1555`）；L09/L10 保留旧上游形 `{head}::{case}` + ", " 连接、忽略 icit，
  对 struct 脱糖的 `Point.mk` 分支名渲染成 `Point::Point.mk(...)`（双重
  前缀）——L09 fork 后未跟进 L08 的显示演进，属继承裂缝（a4-r1 #2 只
  移植了 panic 降级、漏了格式本体）。
- 处置：**已修**。L08 臂逐字移植（L09 数据形状与 L08 完全同构
  `datas: Vec<(Span<String>, Tm, Icit)>`；L10 适配 Rc/is_trait）：现位
  `L09/pretty.rs:212-241`、`L10/pretty.rs:214-243`。两层快版共用同一
  `pretty.rs`（`L09/bump:59`、`L10/bump:77`），一处修复双 oracle。
  **字节变化面**：仅构造子值显示（两层无任何既有字面锚测试；inline
  `#[test]` 均为 println-only；`recursive_0` golden 不涉）。parity 为
  参考版↔快版互检、共用文件，仍逐字节一致。
- 残留：L11/L12 仍为旧形（`L11/pretty.rs`本轮已由 A5 移植 go_ix/SumCase
  panic 降级，格式本体未动）→ §4 FYI 转 A5；L13 的 pretty 为 namespace
  重写版，不可直接比较，不转交。

### 2-5 [P3] L09/L10 无字面输出 golden（A7 §2.6）→ **已补**
- 新增 golden 只钉参考版 `run` 的 Ok 输出（双 oracle 一致性由既有 parity
  套件另行锁定），预期值全部静态推导：`pretty_tm` 各臂（`LiteralIntro` 裸
  内容 `pretty.rs:201`、`U` `Type N`、匿名 Π `→`、`SumCase` 新格式、
  `Match` `(unsolved match …)`、`Var` 哨兵 `recursive_N`）+ parser 事实
  （`->` 的 Π binder 为 `"_"`：`parser/mod.rs:497`；string_concat binder
  为 `x`/`y`：`cxt.rs:20-40`）+ 已验证 parity 用例的程序形态。构造子值
  字节与 L08 blackbox 已钉字面同族互证（§2-4 引）。
- `tests/l09_fast_parity.rs` +6 条（:1077-1247）：字符串/concat、Nat/Bool
  构造子值、struct .mk 值 + 投影、宇宙 + 卡住 Prim（`y => Prim Func`）、
  递归 add/even、卡住 match 显示——共 13 行字面断言。
- `tests/l10_fast_parity.rs` +4 条（:805-947）：ToString/Say 实例合成
  （`true`/`false`/`hello`）、outParam Add 链、L09 血统值、宇宙——共 9 行。
- **刻意不钉**：泛型构造子值（`cons`/泛型 struct 的隐式槽是否入 datas 未
  静态定论，不做无依据断言）；Err 文案（既有 parity 已锁判定与归一化正文）。

### 2-6 复验（任务 5）
- **A3 r2 回灌同链无新分叉**：`alloc_xname` 四方样板（L05/L06/L07/L08）
  与本轮 L09/L10 五方同码（来源注释按层改名）；η 守卫转交经 A3 §2.2
  六条对质裁决为 L07/L08 不可达（其 `v_app` 有 Match pending 臂吸收、
  `string_to_global_type` 已改返回登记类型），**接受裁决**，转交销项；
  L09/L10 已落地的守卫保留（L09 `v_app` 确无 pending 臂，A3 §2.2-4 同证）。
- **A2 的 L06 修复在链上无漂移**：L06:2593/2636 与 L09/L10 新码同构。
- **README 登记完整性**：a4-r1 §1 的丢失清单逐项核对——decl 表→哨兵表、
  meta 快照/回滚、pm 状态、Match pending splice 此前未入 README，本轮
  已补（`L09/README.md` "再定基层的替换面"条目）；显示形态条目两层各补。

## 3. 本轮改动清单

- `src/L09_mltt/bump_spine_iter.rs` → `alloc_xname` + 调用点（#2-1）；
  `filter_accessible_constrs` 头注释纠正（#2-3）
- `src/L10_typeclass/bump_spine_iter.rs` → 同上（#2-1/#2-3）
- `src/L09_mltt/pattern_match.rs` → 探测改 L13 同款 meta 窄快照（#2-3）
- `src/L10_typeclass/pattern_match.rs` → 同上（#2-3）
- `src/L09_mltt/pretty.rs` → SumCase 臂贴齐 L08 格式（#2-4）
- `src/L10_typeclass/pretty.rs` → 同上（Rc/is_trait 适配）（#2-4）
- `src/L09_mltt/README.md` → fuel 六类论证 + 替换面登记 + 显示形态（#2-2/#2-6）
- `src/L10_typeclass/README.md` → fuel 指针 + typeclass 护栏 + 显示形态（#2-2/#2-6）
- `tests/l09_fast_parity.rs` → +6 golden（#2-5）
- `tests/l10_fast_parity.rs` → +4 golden（#2-5）

## 4. 需 orchestrator 转交邻居章节的事项

1. **→A5（L11/L12）**：`alloc_xname` 缺口（本轮实测 `L11:3443`、
   `L12:3508`）——样板已五份（L05-L10）；FYI：L11/L12 的 SumCase 显示
   仍为 `::` + ", " 旧形，对 `.mk` 分支名会双重前缀，L08/L09/L10 已对齐
   （#2-4），是否贴齐由 A5 按其 golden/钉值情况定夺。
2. **→A6（L13）**：`alloc_xname` 缺口 `L13/bump_spine_iter.rs:5301`
   （A3 r2 §4-3 同项，仍未落地）。
3. **→orchestrator**：本轮 golden（§2-5）与 SumCase 显示改动（§2-4）需
   `cargo test` 裁决；golden 失败时的实际输出请回贴，若与推导不符即为
   显示移植或推导的偏差，A4 下轮修正。
4. **→A5/A6 同步信息**：探测窄化（#2-3）与 L11/L12 的 165313d 修复方向
   一致性已核对——A5 若按 L13 窄快照实现，粒度应为**整循环一回滚**（与
   快版 `run_pure_probe` 同），而非逐构造器。

## 5. 刻意分歧登记（锚点）

| 分歧 | 锚点 |
|---|---|
| L09/L10 无 unify/force fuel（六类失控面不存在，不补） | `src/L09_mltt/README.md` 时代缺口第 2 条（全文论证）；`L10/README.md` 指针；`L09/bump:675-676`、`L10/bump:702` |
| 卡住 match 显示保留 L09 简形 `(unsolved match n)`（L08 完整分支渲染未继承） | 两层 README 显示形态条目；golden `golden_stuck_match_display`（l09:1224）钉字面 |
| η 守卫链中段空洞（L06✓/L07✗/L08✗/L09✓/L10✓/L11✓/L12✓） | A3-r2 §2.2 裁决（L07/L08 不可达不修）；L09/L10 守卫保留（`L09/mod.rs` 头注 + `vapp_ok`） |
| 探测回滚粒度 = 整循环（参考版=快版=L13 同口径；params 循环 fresh 分配随探测回滚，修前滞留） | `L09/pattern_match.rs:184`、`L10:164` 注释；本报告 §2-3 |
| 泛型构造子值不做字面断言（隐式槽入 datas 与否未静态定论） | 本报告 §2-5"刻意不钉" |

## needs-verify（诚实记录）

1. **golden 与 SumCase 显示的字节未经运行验证**（禁 cargo）：构造子值
   格式有 L08 blackbox 钉值互证，置信度高；`y => Prim Func`、
   `Type 1 → Type 0`、`recursive_0` 族由源码逐臂推导。若 orchestrator
   跑测失败，以实际输出为准修正断言（预期值错≠被测代码错）。
2. **r1 遗留两条 needs-verify 维持**：L09/L10 η 守卫无端到端触发例
   （hardening 性质，A3 裁决佐证其邻居层同样不可达）；L10 fast
   (Obj,Obj) 臂位次修复无专门探针（参考版臂语义机械对齐）。

---

# Round 2 补遗（A5 并行转交处置）

输入：A5 与本轮并行完成后的三项转交——lockstep 过期注释、可达性记录
顺序 + checked_ret 键型（L13 2a0eb6e 同族）、窄快照与 A5 克隆隔离的
口径核对。全部处置完毕，遵守禁 cargo/禁 git 写/最小 diff。

## 补-1 [P3·注释过期·已修] L10 快版 lockstep 注释（:1959-1961）
- 位置：`L10/bump_spine_iter.rs:1959-1961`（位相等后缀免比门控的注释）
  仍写"参考版无 (Obj, Obj) 臂，交回完整分派后走 `_` → Err"——A4-r1 #5
  已裁定 L10 参考版**有**该臂（unification.rs:723）且快版臂已前移到中性
  链分派之前，本轮该处漏校准。
- 处置：**已修**。改为"交回完整分派后由前移的 (Obj,Obj) 合同臂接管
  （L10 参考版有该臂；同字段卡住投影链按参考版语义逐槽比，异头非
  flex 判败）"。纯注释，无行为面。
- 同族核对：`L09/bump:1895/:1757` 同款措辞**保留不动**——L09 参考版确实
  无 (Obj,Obj) 臂（该臂是 L10 相对 L09 的恢复项，见 L10 README），其
  注释为真。

## 补-2 [P2·裂缝·已修] 可达性记录顺序 + checked_ret 键型（L13 2a0eb6e 同族）
- 同源位置核对结论：**L09/L10 均存在同源机制**（模式编译器 Compiler 的
  reachable/checked_ret 直接继承自 L08；"再定基层"丢的是 pm 特化状态
  pm_defs/pm_solvable，不在 Compiler 内），A5 的两项修复均适用，但 L09
  因结构差异只有一项适用：
- **L10（参考版 + 快版，A5 同款贴齐）**：
  - 可达性先记：`self.reachable.insert` 从 check_pm_final 之后移到之前
    （`L10/pattern_match.rs:234`、`L10/bump:5911`），注释与 A5 逐字同源；
  - checked_ret 键型 `HashSet<Raw>`→`HashSet<usize>`（ref :67、快版
    FxHashSet :5672），`contains/insert` 全部改按 `idx`（:245/:258、
    :5923/:5937）。修前两个模式相同的不同臂共用一份"已检查"备忘——
    第二臂复用第一臂的检查结果、自身体漏检。
- **L09（参考版 + 快版，键型贴齐、顺序不适用）**：
  - checked_ret 键型 `HashMap<Raw, Tm>`→`HashMap<usize, Tm>`（ref :90、
    快版 FxHashMap :4984），get/insert 改按 idx（:257/:270、:5216/:5230）
    ——L09 的备忘还缓存检查体（ret），Raw 撞键时第二臂直接复用第一臂的
    检查体并带上自己的臂号，属同族错检，修法同 A5；
  - 可达性先记**不适用**：L09 的叶臂 `check_pm_final(...).unwrap()`（参考
    版）/`.expect("check_pm_final 失败（参考版 unwrap）")`（快版
    `:5211-5214`）——**无可恢复 Err 路径**，A5 修的"Err 早退跳过记录 →
    误报 unreachable"缺陷类在 L09 结构性不存在（要么记录、要么双版同
    崩），insert 位置无观测差异，不动（最小 diff）。
- 行为面：L10 双版（参考↔快版）warning 集合同步变化（误报 unreachable
  消失），parity 判据不变；无任何测试钉 warning 字面。L09 无行为面。

## 补-3 口径核对：窄快照（L09/L10）vs A5 恢复的克隆隔离（L11/L12）
- 现状矩阵：
  | 层 | 参考版探测隔离 | 快版探测隔离 | 层内 ref↔快版 |
  |---|---|---|---|
  | L09/L10（本轮） | meta 窄快照、整循环一回滚（:184/:164） | `run_pure_probe` metas 窄快照、整循环一回滚 | **逐字节同机制同粒度** |
  | L11/L12（A5） | 整 Infer 克隆、逐构造器（恢复 165313d 前形态） | `run_pure_probe` metas 窄快照、整循环一回滚 | 隔离语义一致，粒度不同（该层快版先于参考版存在，属 A5 切片的既有偏差） |
- **判定：未产生需要处置的新分叉**。理由：
  1. 快版孪生链（继承性的主锚）在 L09–L13 统一为 `run_pure_probe`
     （8a79162 抽取），本轮改动让 L09/L10 的参考版**精确贴齐自己层快版**
     与 L13 参考版（移植判据的母本，`docs/l13-to-low-layer-port-audit.md`）；
  2. L09/L10↔L11/L12 参考版的机制差（窄快照 vs 整克隆）源自 A5 的
     所有权内选择（A7 §2.4 对 L11/L12 的建议同样是"恢复隔离+用窄快照"，
     A5 取了保守的整克隆并已在修复注释中写明理由），两侧都满足"隔离
     不泄漏"这一继承不变量，属可登记的演进/owner 决策而非裂缝；
  3. 不可反向"收敛"：把 L09/L10 退回整克隆会丢掉 A7 转交的 perf 目标，
     且在层内重新制造 ref↔快版粒度偏差（比现状更差）。
- FYI（转 A5，不动其文件）：A5 修复注释中的括号说明"L09/L10 参考版
  亦克隆"在本轮窄快照落地后已过期，可改为"L09/L10 参考版用 meta 窄
  快照（a4-r2 §2-3）"。

## 补遗改动清单

- `src/L10_typeclass/bump_spine_iter.rs` → lockstep 注释校准（补-1）；
  checked_ret 键型 + 可达性先记（补-2）
- `src/L10_typeclass/pattern_match.rs` → 同上（补-2）
- `src/L09_mltt/bump_spine_iter.rs` → checked_ret 键型（补-2）
- `src/L09_mltt/pattern_match.rs` → 同上（补-2）
- tests 无新增：可达性/备忘行为由既有 parity 套件（警告集合同步变化）
  与决策树路径覆盖；无字面锚需要挪动

## 补遗 verdict 增量

- P0: 0  P1: 0  P2: 1（补-2 已修）  P3: 1（补-1 已修）  needs-verify: 0（新增项）
- 本切片（含补遗）0 个未处置 P0–P2：**是**；无新增跨层分叉。

---

# Round 3 补救（终门禁：显示贴齐方向反了，回滚 + golden 重钉）

终门禁证据链：`L12 mod.rs:1479/1591` 与 `L13 legacy_tests.rs:604/716` 均锁定
`Vec[Bool]::cons(1, Bool::false, Vec[Bool]::cons(0, Bool::true, Vec[Bool]::nil))`
——**逗号分隔**（且头名携带类型参数）。即 L09 起"再定基层"重写的显示血统
已是 comma 格式并延续到 L13；L08 的 `.mk` 去重 + 空格连接是 L08 本地形态。
A4-R2 §2-4 把 L08 显示臂移植进 L09/L10 属方向贴反，本轮整体回滚。

## R3-1 回滚（已修）
- `src/L09_mltt/pretty.rs`、`src/L10_typeclass/pretty.rs` 的 SumCase 显示臂
  恢复 R2 前形态（`{头名}::{分支名}({实参, …})`，实参逗号连接、按外层
  prec 打印；struct 值显示为 `Point::Point.mk(...)`——`.mk` 分支名带 `头::`
  前缀是本层血统内的既定形态）。用 `git diff master -- …pretty.rs` 核对：
  现存差异仅剩 R1 的 go_ix 越界退化、`sum_head_name` 头降级（含注释）与
  L10 的 go_app_pruning 注释贴齐，**无任何格式改动残留**。
- 说明：血统终点的 `Vec[Bool]::cons` 头名携带类型参数是 L12/L13 打印机的
  自身演进；L09/L10 的血统段为 comma + 裸头名（`sum_head_name`），即本层
  改动前的原生形态——本次只回滚到"改动前"，不做新的格式发明。

## R3-2 golden 重钉（已修 4 条、维持 5 条；1 项转 needs-verify）
逐条手工展开程序语义后重钉（回滚后字节为准）：
1. `golden_nat_and_bool_ctor_values`（l09）：**修正推导错误**——`two` 是
   两层 succ，实为 `Nat::succ(Nat::succ(Nat::zero))`（R2 误钉单层，即终
   门禁实测值）。其余三行（zero / not true / not false）语义与格式均无关，
   维持。
2. `golden_recursion_add_and_even`（l09）：**修正程序语义错误**——原程序
   的 `even` 无 `odd` 中转（`even n → even (n-1) → … → even 0` 恒 true），
   `even (succ zero)` 实为 `Bool::true`（门禁实测一致）。改写为互递归
   even/odd（`tests/l09_fast_parity.rs::parity_recursive_defs` 同族已验证
   形态）：`even four`（4→3→2→1→0，五步）= true、`even (succ zero)` =
   false，数学与代码双重正确。`four` = add two two = 4 层 succ + zero。
3. `golden_struct_mk_value_and_projection`（l09）：**裁掉链式投影**——门禁
   实测 `println l.a.x` 输出 `Point.mk(...)` 整值，与 eval 的字段直取语义
   （`mod.rs` eval `Tm::Obj` 对 SumCase datas 按名 find）矛盾，静态无法
   裁决是显示、求值还是程序形态问题 → 按任务授权不钉，转 needs-verify
   （见下）。重写为单级投影 `p.x`（`l10 parity_basics` 已验证形态）：
   `p` → `Point::Point.mk(Nat::succ(Nat::zero), Nat::zero)`、`p.x` →
   `Nat::succ(Nat::zero)`。
4. `golden_outparam_add_chain`（l10）：程序补 `def outParam[A](a: A): A = a`
   （对齐 parity_trait_full_demo 顶部形态，排除裁剪导致的 Err 风险）；
   pin 不变（`two.add two` = 4 层 succ + zero）。
5. `golden_l09_heritage_values`（l10）：`p` 行重钉为回滚后字节
   `Point::Point.mk(Nat::succ(Nat::zero), Nat::zero)`；`Bool::false` 与
   `p.x` 行维持。
- 维持不动（格式无关且门禁未列失败）：l09 string_literals /
  universe_and_stuck_prim / stuck_match_display；l10 tostring_say /
  universe_levels。
- 两个测试文件的 golden 头注释同步改写（删除"与 L08 钉值同族"表述，
  改引 L09-L13 comma 血统锚点）。

## R3-3 README 登记（已修）
- `src/L09_mltt/README.md`、`src/L10_typeclass/README.md` "显示形态"条目
  改写：SumCase 打印为 **L09-L13 comma 血统**（锚点
  `src/L13_namespace/legacy_tests.rs:604`、`src/L12_canonical/mod.rs:1479`）；
  L08 的 `.mk` 去重 + 空格连接是本地形态、不随继承；注明 A4-R2 曾向 L08
  对齐、终门禁证明方向反了、已回滚。

## R3-4 对 R2 其它收敛项的影响核对
- **不受影响**：R2 §2-1 alloc_xname（名字生成，非显示）、§2-3 探测窄快照
  （meta 表换入换出，非渲染）、补-2 reachable/checked_ret（记录顺序与
  键型，非渲染）、§2-2 fuel 论证（纯论证）——均不经过 SumCase 渲染臂。
- 受影响并已同步：§2-4 本体作废（见该节顶部裁决框）；两层 README 显示
  形态条目、两份 golden 头注释、`Point.mk` 相关期望字节。

## R3 verdict 增量
- P0: 0  P1: 0  P2: 0  P3: 0（回滚/重钉均闭环） needs-verify: 1（新增，见下）
- 本切片（含 R3）0 个未处置 P0–P2：**是**。

## R3 needs-verify 新增
1. **链式值级投影的字节/语义**：`println l.a.x` 门禁实测输出 `Point.mk(...)`
   整值，而 `mod.rs` eval `Tm::Obj` 的 SumCase 臂是 datas 按名 find 直取
   字段——两者矛盾。静态无法定位属显示、求值还是程序形态（`def l` 的
   datas 命名等）问题；已从 golden 剔除链式投影形态，待 orchestrator 跑
   测时以最小程序（`def p = new Point(...)` 之上的 `p.a.x` 式链）实测定
   位后处置：若为求值缺陷按 BRIEF §5.3 给触发例立 P1，若为显示则登记。

## R3 终微修（门禁复测后最后两条，均已闭环）
1. `golden_struct_mk_value_and_projection`（:1151）：门禁实测两行均正确
   （comma 血统 ✓、单级投影化简到字段 ✓），差在期望又少写一层 succ——
   程序里 x 槽就是 `two`（两层 succ）。已按实测逐字节重钉：
   `Point::Point.mk(Nat::succ(Nat::succ(Nat::zero)), Nat::zero)` /
   `Nat::succ(Nat::succ(Nat::zero))`，并附手工展开说明（two =
   succ(succ(zero))，p 与 p.x 的 succ 链均为两层）。
2. `golden_recursion_add_and_even`（:1222）：互递归 even/odd 在 L09 报
   `error name not in scope: odd`——**L09（与低层同）不支持 def 前向
   引用**（此前引用的 parity_recursive_defs 同族只有自引用 + 后向引用，
   无前向）。已改单 def 自递归 + 嵌套 match（`even n = match n { zero =>
   true; succ(m) => match m { zero => false; succ(k) => even k } }`，只引
   用自身，fake_bind 自递归封口），期望逐行手工展开：four = succ⁴(zero)、
   even 4 = true（4→2→0）、even 1 = false；断言字节不变
   （`…succ⁴…\nBool::true\nBool::false\n`），程序与注释已更新。
- 本轮登记的经验锚：**golden 程序不得使用本层不存在/未验证的机制**
  （前向引用、链式值级投影），每条期望须逐行手工展开后才能钉。

## R3 终微修补遗（l10 漏网，收敛前最后一项）
- `golden_l09_heritage_values`（`tests/l10_fast_parity.rs:923` 断言）：与
  l09 struct golden 同款问题——期望少写一层 succ。已按门禁实测逐字节重
  钉：`Bool::false` / `Point::Point.mk(Nat::succ(Nat::succ(Nat::zero)), Nat::zero)` /
  `Nat::succ(Nat::succ(Nat::zero))`（第 2、3 行改两层 succ；语义已于上轮
  手工展开证明正确：two = succ(succ(zero))，p 的 x 槽即 two 的值），并附
  同款展开说明。仅改 `tests/l10_fast_parity.rs` 一处断言。
