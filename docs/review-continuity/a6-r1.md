# A6 Round1 — L12→L13（终点章 L13：namespace/HDL/LSP + 性能版快版孪生深审）

> 评审对象：`src/L13_namespace/**`（参考版 + `bump_spine_iter.rs` 14044 行 + `compact.rs`）、
> `tests/l13_*`、`tests/twin_engine_*`。方法：`git diff --no-index src/L12_canonical/X.rs
> src/L13_namespace/X.rs` 逐 hunk 分类 + `git log --oneline -60 -- src/L13_namespace`
> 逐 commit 同族核对 + 静态推理（未运行 cargo，遵守 BRIEF §5）。

## 1. 连贯性判定（verdict）

- **L12→L13 参考版**：连贯。L12 全部上轮修复中适用于 L13 的项，除 FINAL §3.3 点名的
  四族外全部已在位（η 守卫 `is_appliable`、trait 单向一阶匹配 `val_match`、
  `LiteralType` 臂优雅退化、`solve_trait` 错误可恢复化）。FINAL §3.3 四族本轮已补齐
  （见 §2），另有 u64 字面量同族裂缝一并修复。
- **L12→L13 快版孪生**：结构连贯。L13 孪生由 L12 冠军配方移植（9ad15d7），打包字
  编码/显式栈/fuel/`is_flex`/`Entry.hk` 等共享机制逐点在位；差异均为 L13 演进
  （观察面三表、原生 Nat、`Val::Call`/`XCell::Call`、常驻检查点、压实、prelude 重放、
  导出层）。FINAL §3.3 的 `XCell` 对齐裂缝已修。
- **L13 特有的两个开放引擎级分叉**（前轮已定性、带安全闸，本轮维持不盲改）：
  18-utils flex-flex 带 spine 合一（孪生 unify 核心分叉）与 ns-method hover 集合分叉
  （孪生观察面问题，非引擎语义裂缝），见 §2 末两条。
- P0: 0（已清零，本轮修复 2 项 P0）  P1: 1（开放：18-utils unify 分叉，已文档化+闸）
  P2: 1（开放：ns-method hover 分叉，已登记偏差+降级闸）  P3: 0
  needs-verify: 3（全部为 L12 侧同族，走 §4 转交）
- **本切片是否收敛（0 个未处置 P0–P2）**：否（条件收敛）——余 1 P1 + 1 P2，均为
  前轮已定性"非局部修补/已登记偏差"的开放项，修复需专项验证（18-utils 作复现），
  盲改违反 BRIEF §5.3/§5.4。

## 2. 裂缝/分歧清单

FINAL §3.3 逐项现状（本次全部核对 master 后处置）：

| # | 项 | 位置（L13） | 现状→分类→处置 |
|---|---|---|---|
| 1 | 宏展开无深度上限（P0） | `parser/mod.rs` 三处同构副本（p_raw 宏调用 ~:1267、braced 块 ~:1603、decl 级分派 ~:3151，修后行号 +46/+58） | 仍开放→裂缝→**已修**：按 L12 f51a0e4 同款贴齐 `MAX_MACRO_EXPANSION_DEPTH=256` + `thread_local` + RAII `MacroDepthGuard`；三处调用形态逐一对齐 L12（decl 级超限退化遵循本函数既有 `is_cut → Decl::Package{path:[]}` 分支，非 L12 的 Println(Hole)——两函数 Err 分支本就不同）。双版共用本 parser，一处修复两版生效 |
| 2 | `XCell` 无 `#[repr(align(8))]`（P0，wasm UB） | `bump_spine_iter.rs`（`XCell` ~:543、`CloCell` ~:730、解引用 `v_*_of` ~:496-520） | 仍开放→裂缝→**已修**：`XCell`/`CloCell` 加 `#[repr(align(8))]` + `align_of ≥8` 编译期断言块（含 `EnvCons`）+ `v_clo_of`/`v_pi_of`/`v_xcell_of` SAFETY 注释，逐字对齐 L12 6c53097/b621789。64 位布局逐字节不变；`compact.rs` 经 `b.alloc(XCell::…)` 分配，自动继承新对齐（压实产物同样满足 packed 解码前提） |
| 3 | `intersect_go` 长度失配 `unreachable!()`（P1） | `unification.rs:862`（`intersect` 回落路径） | 仍开放→裂缝→**已修**：改 `None`（回落 `unify_sp` 判失败），对齐 L12 同函数及快版 `intersect_bump` 的 `n1 != n2 → false`（孪生侧本就正确，:3366-3368） |
| 4 | `prune_ty` 未反转 pruning 掩码（P1） | `unification.rs:197-232` | 仍开放→裂缝→**已修**：`prune_ty` 先 `collect + reverse` 再走 `prune_ty_go(&[Option<Icit>])`（L12 同形；保留 L13 的 `ren: Rc::new(HashMap::new())` 差异）。孪生 `prune_ty_bump` 本就 `mask_inner_first.iter().rev()`（:5239），修复后两版口径一致。全 Some/单槽掩码行为不变（`prune_ty_go` 不读 Some 槽的 icit 值），仅多层混合掩码不再错位 |
| 5 | 全局哨兵 `>` vs `>= GLOBAL_BASE` | — | **不适用（刻意分歧，非裂缝）**：L12/L13 全局引用是名字制 `Tm::Decl(Span<SmolStr>)`（mod.rs:380），无数值哨兵可下溢；孪生用 `Machine::decl` 脊下标。`1919810` 仅存于 L12 bump_spine_iter.rs:36 注释。L09/L10 的 off-by-one 族在 L13 无同源位置 |
| 6 | u64 字面量 `parse::<u64>().unwrap()`（P1，FINAL §2.2 同族） | `parser/mod.rs` p_atom1（修前 :554） | 仍开放→裂缝→**已修**：超限推 IError `integer literal does not fit in u64` 并退化 `Raw::Hole`（闭包式 parser 臂，与 L12 :599-620 同构） |

已确认在位（无需动作）：η 臂 applicability 守卫（参考 `unification.rs:44` `is_appliable`、
孪生 :3233/:4023/:4038）；trait 实例单向匹配（参考+孪生共用 `typeclass::Synth`/`val_match`，
d4c05ea 后 L12/L13 口径一致——L12 `typeclass.rs:123/246/265` 与 L13 `:271/444/472` 同构）；
`solve_multi_trait` 无 unwrap（bcaaea5 已修）；`LiteralType`/`to_typ` 族（L13 无 `to_typ`，
对应臂全部 `None` 退化）。

其余 diff 分类结论：
- **cxt.rs**：`PrimFunc` 签名 `&[Rc<Val>]` 化、`vconn_builtin`、ns 别名注册、
  `change_mutable` 家族均为 L13 演进；未发现 L12 修复在 L13 缺位。
- **pretty.rs**：`go_ix` 越界退化、`go_app_pruning` 掩码槽退化在位（L13 是该修复的源头层）。
- **elaboration.rs / typeclass.rs / pattern_match.rs**：+2000 行级差异均为 namespace/
  HDL/class 两阶段/Raw::Tm 复用/观察面 push 演进；`no_metas` 为值图遍历版（L13 mod.rs
  `no_metas_seen`，71e11ae 同族已修态）。
- **mod.rs**：5 元组→7 元组 decl 行、`Tm::Prim/Val::Prim` 移除（592f47b）、`Pattern::Bind`
  等为演进；`//TODO:revPruning` 陈旧注释 3 处（unification.rs ×2、elaboration.rs ×1）
  随本次修复删除（L12 上轮已删的同族清理）。
- **快版孪生**：观察面（hover/completion/inlay 三表 + push 站点）、`XCell::Nat`、
  `Call` 值与 `CallAsm` quote、常驻检查点/压实接线、prelude 重放、导出层、
  `REPLAY_GLOBAL_OPS` 重放 = 演进；无 L12 守卫缺失（`is_flex`/fuel/`Entry.hk`/
  显式栈/水位克隆全部在位）。
- **canonical.rs**：L13 在初始化时（f1392ad）把 `create_global`/`change_mutable`/
  `string_concat`/`get_global` 等 6 名从 IDDFS 候选过滤名单中移除（L12 canonical.rs:67
  仍有）→ needs-verify 刻意分歧（见 §5-3）。

### L13-only fix/perf 的低层同族扫描（git log -60）

| commit | 内容 | 低层同源可触发？ | 结论 |
|---|---|---|---|
| 2a0eb6e | 孪生 pattern 可达性记录顺序 + checked_ret 按臂下标 | **是**：L12 参考 `pattern_match.rs:206-216`（先 `check_pm_final` 后 `reachable.insert`）、`:68/:217/:239`（`checked_ret: HashSet<Raw>` 按体 Raw 键）；L12 孪生 `bump_spine_iter.rs:6203-6212` 同序 + 按 Raw 键 + 传原始 `arm.raw`；L09-L11 参考 `:214/:222/:239` 同族 | 裂缝（跨边界）→ §4 转交 A5/A4（13-adder-tree 即触发模板：索引化和的递归 match） |
| 40199ef | 孪生 trait 求解 flex 判据 `v_tag==5` → `is_flex` | **是（仅 L12）**：L12 孪生 `bump_spine_iter.rs:4120` 仍 `v_tag(*f) == 5`（漏 meta 头链 `?m x`）；L10/L11 孪生走 Typ 级 `val_to_typ` 短路，结构不同不受影响 | 裂缝（跨边界）→ §4 转交 A5 |
| 71e11ae | 孪生 `no_metas` 改值图遍历（quote 版挂死） | 部分：L11/L12 参考+孪生的 `no_metas` 均为 quote 版（L12 孪生 `:7540-7545` 已解 meta quote 后续查、`cxt.lvl` 引出）。触发需"解自引用"（L13 模块/bundle 链产物）与深上下文解（L13 NM_QUOTE_LVL 修复的 panic 族）——L12 有 create_global/change_mutable 但无模块宏摊平，无已知触发 | needs-verify → §4 转交（记录 + 建议对齐 NM_QUOTE_LVL/visited-set，无实测触发不强制） |
| d3df88e | 孪生 `solve_multi_trait` 改用 meta 创建处上下文 | L12 参考（unification.rs:488）与孪生（:4056 透传调用方 cxt）**自洽地**都用调用方上下文；L13 两侧都改为创建处快照。属 L13 演进还是 L12 待修，取决于浅上下文求解在 L12 是否可达（rename 越界需 trait meta 创建层深于求解层） | needs-verify → §4 转交 A5 |
| ff38c1a / d711fde / 534378e | 孪生 FORCE_MEMO、arena 压实、常驻检查点 | 否（见 §4 下沉评估） | L13 twin 引擎专属 |
| 9518d53 / 7a44738 / 8127357 | nat_step_value tag 守卫、卡住 Call quote 实参序、Obj 剥 mk 链 | 否：`XCell::Nat` 原生 Nat、`Val::Call`、mk 链均为 L13-only 值形态（L12 parser 注释明言数字脱糖 Church 编号；L11/L12 `Val` 无 Call 变体） | 不适用（记录） |
| 44fc23d | `filter_accessible_constrs` 探测整体快照回滚 | L09-L12 同名函数存在；该修复与 L13 的 meta 快照换入换出形态耦合，未证实低层有同形 AV | needs-verify（低优先，随 §4 转交备注） |

## 3. 本轮改动清单

- `src/L13_namespace/parser/mod.rs`：宏展开深度守卫（常量 + thread_local + RAII +
  `macro_depth_error` + 三处调用点）；u64 字面量可恢复化（推 IError + Hole）。
- `src/L13_namespace/bump_spine_iter.rs`：`XCell`/`CloCell` `#[repr(align(8))]` +
  4 项 `align_of ≥8` 编译期断言 + 3 处 packed 解引用 SAFETY 注释。
- `src/L13_namespace/unification.rs`：`prune_ty` 掩码反转（`prune_ty_go` 改吃
  `&[Option<Icit>]` 逆序切片）；`intersect_go` 长度失配 `unreachable!()` → `None`；
  删 2 处陈旧 `//TODO:revPruning`。
- `src/L13_namespace/elaboration.rs`：删 1 处陈旧 `//TODO:revPruning`（仅注释）。
- `tests/l13_fast_parity.rs`：新增 2 枚解析护栏探针
  （`probe_big_int_literal_recoverable`、`probe_macro_self_recursion_depth_limit`，
  形态与 `tests/l12_fast_parity.rs:365-392` 同款）。

行为契约：以上修复全部是"panic/UB/栈溢出 → 既有 Err 路径"或与快版/L05-L12 口径
对齐；Ok 输出与既有 Err 判定不变（全 Some 掩码反转恒等、长度失配原本 panic、
超限字面量原本 panic、宏超限原本栈溢出）。无测试断言锁定被改行为（panic 不可能
被 parity 测试锁定）。

## 4. 需 orchestrator 转交邻居章节的事项

跨层移植/修复（本轮按授权不实施，交对应 owner）：

1. **[P1] L12 pattern 可达性记录顺序 + checked_ret 键型（2a0eb6e 同族）**——
   `src/L12_canonical/pattern_match.rs:206-230`（参考）与
   `src/L12_canonical/bump_spine_iter.rs:6185-6212`（孪生）：`reachable.insert` 移到
   `check_pm_final` 之前；`checked_ret` 由 `HashSet<Raw>` 改按臂下标；`check_pm_final`
   传 `patcon.to_raw()` 而非原始用户模式。修法模板 = L13 `pattern_match.rs:352-396` +
   孪生 `:10570-10600`（2a0eb6e）。L09-L11 参考 `pattern_match.rs:214/222/239`
   同族顺序问题一并列评估（同修或记录）。
2. **[P1] L12 孪生 trait 求解 flex 短路过弱（40199ef 同族）**——
   `src/L12_canonical/bump_spine_iter.rs:4120`：`v_tag(*f) == 5` →
   `is_flex(&self.spine, *f)`（裸 meta 之外的 meta 头链同样推迟求解）。
3. **[needs-verify] L12 `solve_multi_trait` 上下文策略**——L12 参考/孪生用调用方
   cxt，L13 两侧用 meta 创建处快照（d3df88e）；若 L12 存在"创建层深于求解层"的
   trait meta，rename/quote 会越界。需 A5 判定可达性后对齐或记录刻意分歧。
4. **[needs-verify] L11/L12 `no_metas` quote 版**——已解 meta 无 visited-set/
   深度上限（挂死类）且 quote 用 `cxt.lvl`（深上下文解出时 debug 下溢类，L13 修复
   = `NM_QUOTE_LVL`/值图遍历）。无已知低层触发（模块链为 L13-only），建议低成本
   对齐或记录"无可触发类"。
5. **[下沉评估结论，无需实施] L13 快版机制的_sink 判定**：
   - `compact.rs` arena 压实 / 常驻检查点（prime_resident/observe_user）：**L13 twin
     引擎专属，不下沉**——前提是"bump 跨 kick 常驻"（LSP 阶段 3b），L02-L12 孪生
     一次性口径（`run_fast` 每轮 `bump.reset()`），不可达垃圾随 arena 整体释放，
     压实无对象；且压实器按 L13 的 14 个 bump 类型清单逐类型手写，低层类型集不同。
   - FORCE_MEMO（力记忆化，参考+孪生）：机制通用，但收益来自 prelude 跨 def 共享
     值图（实测 3400× 指针冗余）；低层无常驻 prelude、单文件负载指针冗余趋 1，
     且 taint/版本/epoch 卫生成本真实（round 16.3 的批量 drop 教训）→ 暂不下沉，
     留作低层 bench 出现同类风暴时的候选。
   - 显式栈求值 / packed 布局 / unify/force fuel / FxHash：L06-L12 孪生已在位
     （L13 孪生即其冠军配方后代），无下沉项。原生 Nat（`XCell::Nat`）、`Val::Call`、
     观察面三表：L13-only 语言面/产品面，低层无对应语义。
   - mimalloc 全局分配器：Cargo.toml 级全局生效，已惠及全链（A7/共享文件范畴）。

## 5. 刻意分歧登记（锚点）

1. **L13 无数值全局哨兵**：全局 = 名字制 `Tm::Decl`（`mod.rs:380`）；L09/L10 的
   `GLOBAL_BASE` 边界纪律不适用。锚点：本报告 §2-5；L12 bump_spine_iter.rs:36 注释。
2. **18-utils flex-flex 带 spine 合一（P1，开放）**：goal `LetNamed[?m₁[r], ?m₁[r]]`
   vs 实例 `LetNamed[?m₂[r], ?m₂[r]]`，孪生 unify 判 Stuck、参考版成立——unify 核心分叉，
   ffc4a83 定性"非局部修补"；现由 twin_elaborate 两道闸兜底（遇错回落参考版 + 模块
   文件 check 警告为空则回落，`lsp-twin-wiring-2026-09.md` 2026-09-10 节）。
   复现：`examples/hdl/18-utils.typort`。处置：保留闸 + 专项立项（需以该文件为回归
   闸逐 hunk 验证 unify_iter），本轮不盲改。
3. **canonical.rs 候选过滤名单收窄（needs-verify）**：L13 `canonical.rs`（f1392ad 起）
   不再排除 `create_global`/`change_mutable`/`string_concat`/`get_global` 等 6 名
   （L12 `canonical.rs:64-70` 有）。方向为 L13 演进（CANONICAL 定理模式在 L13 不
   搜索全局副作用 builtin 的判定需复核），锚点：`git diff --no-index` 两层 canonical.rs。
   交 A5/L13 后续对齐。
4. **ns-method hover 集合分叉（P2，开放）**：孪生在方法 token 上多推未实例化声明 π
   （`App(Var(TypeHead.at), receiver)` 的 `push_hover(r.1)`）且缺参考版宽 span 条目；
   根因在 ns-method 分派/实例化层，但**只影响 hover 观察面，非引擎语义**
   （Ok 输出逐字节一致，`l13_fast_parity` 绿；twin_elaborate 出错即回落）。已登记
   偏差（a99972a，`lsp-twin-wiring-2026-09.md`）+ curated 守卫把关。修复属 LSP
   twin 接线子工程（修复后可放宽模块警告闸），非本轮继承连贯性范畴。
5. **L13 孪生 `mach_ptr` 重建（18 处）**：FINAL §3.1 Stacked Borrows 别名 UB 家族
   延伸至 L13 孪生（`flex_flex_bump`/`unify_iter` 的 `unsafe { &mut *mach_ptr }`）。
   与 L10-L12 同一立项（Miri + 自由函数化重构），本轮不动。
