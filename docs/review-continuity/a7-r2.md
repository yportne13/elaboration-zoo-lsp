# A7 Round2 — 横切性能审计 + bench 基础设施（收尾轮）

> 切片：`src/bin/l09bench.rs`/`l10bench.rs`/`l11bench.rs`/`l12bench.rs`、
> `Cargo.toml`（仅 [[bin]]）、本报告。其余仓库只读。
> 输入转交：A5-r1 §4-3（church 排除理由被绿测反例推翻）、A5-r1 §4-5（fuel 矩阵
> L12 行订正）、A4-r2 §2-2（L09/L10 fuel 不适用裁决）。
> 方法：静态推理 + 只读 bash（grep/sed/git diff --stat/git status），禁 cargo、
> 禁 git 写操作，全程遵守。

## 1. 连贯性判定（verdict）

- **fuel 横切矩阵订正完成**（r1 §2.3 的"L09–L12 无 fuel / 参考版 fuel 只在
  L07/L08 与 L13"表述失实，按 A5-r1 §2-9 订正）：L12 参考版 `unify` **实有
  fuel**（仅服务 Decl 头展开臂 + SumCase 重锚两处，见 §2.1 矩阵）；L09/L10/L11
  参考版与孪生确认双向无 fuel，三层孪生头注"参考版无 fuel"**属实**。订正后
  矩阵见 §2.1——该横切项就此闭环（L09/L10 不适用已由 A4-r2 §2-2 六类论证 +
  README 登记；L12 孪生 Decl 展开臂缺口已由 A5-r1 §5-6 登记已知分歧）。
- **bench 补负载完成**：采纳 A5-r1 §4-3 的反驳，`church` 负载补回
  l11bench/l12bench（§2.2）；`gadt` 排除理由成立（锚 0242720），维持排除。
- **复验（任务 3）**：A3/A4/A5 wave-1 对 L07–L12 的大量修改（alloc_xname、
  (Obj,Obj) 臂、is_flex、可达性、窄快照、探测克隆、pretty 降级）**不触碰 4 个
  bench bin 依赖的任何 `pub(crate)` 符号与模块结构**——逐符号复查全部在位，
  仅行号平移（对照表 §2.3）；`git diff --stat HEAD` 证实改动全为函数体/注释级。
- **r1 §4 转交清单闭环**：11 项中 已修/闭环 7、论证不修 2、仍开放 P3 2
  （均不阻断收敛，见 §4 逐条标注）。
- **本轮新发现 1 项 P2**（转交 A5）：L11/L12 参考版探测回滚粒度
  （逐构造器全克隆）与同层孪生 `run_pure_probe`（整循环 metas 快照一回滚）
  不一致，且与 A4-r2 修后的 L09/L10/L13 口径断链（§2.4）。
- P0: 0  P1: 0  P2: 1（转交 A5）  P3: 2（开放备案，归 A5）  needs-verify: 1
  （church 在 L11/L12 的实际判型需 orchestrator `cargo run` 实测）
- **本切片（A7 文件）是否收敛（0 个未处置 P0–P2）：是**——bin 修改为最小
  diff、复用既有闸门路径；P2 属邻居切片，已进 §4 转交。

## 2. 裂缝/分歧清单

### 2.1 【矩阵订正】unify/force fuel 逐层现状（r1 §2.2 表 fuel 行 + §2.3 的订正）

以当前工作树代码为准（本轮逐一 grep/读码核对）：

| 层 | 参考版 fuel | 快版孪生 fuel | 孪生头注准确性 | 处置与锚点 |
|---|---|---|---|---|
| L07/L08 | ✓（mod.rs 烧点 + unify 递归烧点） | ✓ | — | 基线（A4-r2 §2-2 列 L08 烧点锚 `L08/mod.rs:376/388/397/420/435/448`、`unification.rs:581-593`） |
| L09 | ✗（grep 非孪生文件 0 命中） | ✗ | "无燃料（参考版无 fuel）"**属实**（`L09/bump_spine_iter.rs:1957`） | **不适用、不补**：A4-r2 §2-2 六类失控面逐一论证不存在；已登记 `L09/README.md`。r1 P2 销项 |
| L10 | ✗ | ✗ | 属实（`L10/bump_spine_iter.rs:2022`） | 同上（`L10/README.md`） |
| L11 | ✗ | ✗ | 属实（`L11/bump_spine_iter.rs:2139`；`L11/README.md:19-21` 已由 A5 登记，并注明 L12 不同） | 参考版↔孪生双向一致，无缺口；不移植论证同 L09/L10 六类（同架构族：无 pm_defs、force 仅 Flex 臂、decl 表存终值） |
| L12 | **✓ unify fuel**（`unification.rs:555/:646` fuel 参数；`:676-687` Decl 头展开臂 quote+eval fuel-1 重试；`:749-756` SumCase 重锚 100 配额；入口 `elaboration.rs:264`、`mod.rs:687` 均 fuel=100。`force`（mod.rs:394）无 fuel） | ✗ | r1 所引"无燃料（参考版无 fuel）"**失实，已由 A5 订正**（`L12/bump_spine_iter.rs:2190-2197`，含订正说明） | 孪生无 Decl 头展开臂 = 已登记机制缺口（A5-r1 §5-6，`L12/README.md:19`）；`constraints` Stuck 挂账已移植。A7 无动作 |
| L13 | ✓ | ✓ | — | 基线（r1 §2.2 已核） |

**订正结论**：r1 §2.3 的缺口主张对 L09/L10 成立但被 A4 论证为不适用（死护栏 +
fuel 耗尽有 Ok→Err 行为变更风险），对 L11 双向一致无缺口，对 L12 的
"参考版无 fuel"前提失实（缺口只剩"孪生缺 Decl 头展开臂"一项，A5 已登记）。
**r1 该 P2 项全部销项，无遗留。**

### 2.2 【采纳 A5 反驳】church 负载补回 l11bench/l12bench

- r1 排除理由（"参考版对 λ 体 + `Type N` 注解判型失败"，引 natadd_src 注释
  `L11 twin:7682-7684`）被 A5-r1 §4-3 绿测反例推翻：
  - Pi 体 + `Type N` 注解（church 的 `def Nat : Type 1 = (N : Type 0) -> …`）：
    `tests/l11_fast_parity.rs:402`（`def String : Type 0 = Type 0`）、
    `:640`/`:649`（`def Eq [A : Type 0] … : Type 0 = (P : A -> Type 0) -> P x -> P y`）、
    `tests/l12_fast_parity.rs:441`/`:449`（同款，本轮实测行号）均绿。
  - church 的 λ 体 def（`add`/`p0`）注解为完整类型（`Nat -> Nat -> Nat`/`Nat`），
    非 natadd 注释所指的 `Type N` 注解组合。
- 静态复核：L11/L12 参考版 `check` 入口先 `force(&cxt.decl, a)`
  （`L11/elaboration.rs:196`），λ 对命名类型 `Nat`（Decl 值为 Pi）的检查路径与
  L09/L10 church 同构（L09/L10 church 为绿负载）。
- 补回方式（最小 diff，6 处/文件）：header 文档、use 加 `church_src`（L11
  twin:7689 / L12 twin:7827，`pub(crate)`）、`--workload` 帮助与 usage 行、
  workload 分派表（`"church"` 单选 + all 列表头位，照 l09/l10 惯例）、src 分派臂
  `"church" => church_src(k)`。默认 workload 仍为 natadd。
- **口径**：沿用 l11/l12 bin 既有闸门路径（与 natadd 同）：`fast_nodes > 0` +
  memo/非 memo 节点数互检 + 两版全流程输出 parity（`run` vs `run_fast` 逐字节
  /Ok-Err 判定一致）。**不设 `2n+4` 闭式断言**——L09/L10 的钉值
  （`tests/l09_fast_parity.rs:921`、`l10:551`）不跨层，本层无 parity 钉值
  （见 §5 分歧登记）；首跑可实测后钉入。
- **gadt 维持排除**：A5-r1 §4-3 论证排除理由成立（commit `0242720` test9/test10
  基线即挂 + `tests/l11_fast_parity.rs:474-476` 快版 walk 缺陷区注释）；缺陷
  修复后补入（§4 备案）。

### 2.3 【复验·任务 3】4 个 bench bin 的 use/签名核对结论 wave-1 后仍成立

`git diff --stat HEAD -- src/` 证实 wave-1 改动全为函数体/注释级（无模块结构、
无 `pub` 项增删）；逐符号复查（当前行号，r1 行号→现行号）：

| 符号（bin 用途） | L09 | L10 | L11 | L12 |
|---|---|---|---|---|
| 孪生 `Tycker` / `new` | :6249/:6255 | :7031/:7037 | :7576/:7582 | :7714/:7720 |
| 孪生 `bench_check` / `bench_check_nf` / `bench_check_nf_memo` | :6300/:6309/:6314 | :7082/:7091/:7096 | :7627/:7636/:7641 | :7765/:7774/:7779 |
| 孪生 `parse` | :6346 | :7128 | :7673 | :7811 |
| 孪生生成器 `church_src`/`strchain_src`/`match_src`/`enum_src`/`struct_src` | :6362/:6377/:6389/:6404/:6463 | :7144/:7159/:7171/:7186/:7245 | :7689/:7769/:7781/:7796/:7855 | :7827/:7907/:7919/:7934/:7993 |
| 孪生 `run_fast` | — | — | :7666 | :7804 |
| 参考版入口 | `bench_check_nf` mod.rs:618（bench_check :604） | :644（:630） | `run` mod.rs:583 | `run` mod.rs:730 |

- 波及面核对：alloc_xname（产串等价）、(Obj,Obj) 臂位次、`is_flex`、可达性
  先记/checked_ret 键型、探测窄快照（L09/L10）/temp_infer 恢复（L11/L12）、
  pretty `go_ix`/`sum_head_name` 降级——均为行为/性能修复，不改 bin 依赖的
  签名与生成器（`church_src`/`natadd_src`/… 函数体本轮逐一比对未变）。
- A4-r2 §2-4 的 L09/L10 SumCase 显示格式变更**不影响 bin**：bin 闸门比较
  节点数与两版互检输出（参考版与孪生共用 pretty.rs，parity 不破），无字面
  golden 断言依赖旧格式。
- Cargo.toml 的 4 个 [[bin]] 条目在位（:47-60），本轮未动。

### 2.4 【P2·新发现·转交 A5】L11/L12 参考版探测回滚粒度与同层孪生不一致

- **现状**（本轮实测）：
  - L11/L12 参考版：`temp_infer = infer.clone()` 在构造器循环**内**
    （`L11_macro/pattern_match.rs:177-179`、`L12:179-181`）——逐构造器整
    Infer 克隆、逐构造器回滚，**探测 k 的解对探测 k+1 不可见**。
  - L11/L12 孪生：`run_pure_probe`（`L11/bump_spine_iter.rs:4700-4705`）
    metas 整表快照一次、`f` 返回后无条件还原，**整个构造器循环包在一个
    probe 调用里**（`:5977` 闭包含 `for constr_name in all_constrs`）——
    探测 k 的解对探测 k+1 **可见**。
  - L09/L10/L13 参考版（A4-r2 §2-3 修后）：整循环 metas 窄快照 + 循环后
    无条件回滚（`L09/pattern_match.rs:184`、`L10:164`、`L13:266-335`）——
    与各自孪生同粒度。
- **分类**：裂缝（继承裂缝 + 参考↔孪生分叉）。A5-r1 §2-2 恢复的是 165313d
  修前形态（逐构造器克隆），但同轮 A4-r2 已把 L09/L10 升级为 L13 同款
  **整循环**窄快照，并在 §4-4 明确建议 A5"粒度应为整循环一回滚，而非逐
  构造器"——L11/L12 未跟上，成为全链唯一粒度断点。风险面：探测 k 的解
  影响 k+1 可达性判定时，参考版与孪生对"可达构造子集合"判定可分叉
  （潜在 parity 裂缝；`?N` 归一化不掩盖此类，因它不改可达集合）。
- **处置**：仅报告，转交 A5。修法建议照 A4 样板（`L09/pattern_match.rs:
  184-186` 快照 + 闭包包循环 + 循环后无条件回滚）；L11/L12 `Infer` 字段
  可分性需 A5 自行论证（其字段含 hover_table/constraints/trait_solver 等，
  多于 L09/L10——meta 是唯一探测面的论证要重做），或按 BRIEF §2 论证后
  登记刻意分歧。

## 3. 本轮改动清单

| 文件 | 改动 |
|---|---|
| `src/bin/l11bench.rs` | 补回 `church` 负载（header 文档订正排除理由并引绿测锚、use +`church_src`、workload 分派 +src 臂 +usage/help 行）；默认 workload 不变 |
| `src/bin/l12bench.rs` | 同上（绿测锚引 `tests/l12_fast_parity.rs:441/:449`） |
| `Cargo.toml` | **无改动**（4 个 [[bin]] 条目 r1 已在位） |
| `docs/review-continuity/a7-r2.md` | 本报告 |

l09bench/l10bench 无需改动（复验通过，§2.3）。所有 bin 改动复用既有闸门
与计时路径，无新依赖、无新入口签名；`church_src` 已核对 `pub(crate)` 可达。

## 4. 需 orchestrator 转交邻居章节的事项（含 r1 §4 转交清单逐条闭环标注）

### 4.1 r1 §4 转交清单处置结果

**转交 A4（3 项）——全部闭环：**
1. [P2] L10 探测整克隆 Infer → **已修**（A4-r2 §2-3：L09/L10 参考版改 L13
   同款整循环 metas 窄快照，与快版 `run_pure_probe` 同粒度）。
2. [P2·needs-verify] L09/L10 无 fuel → **论证不修**（A4-r2 §2-2 六类失控面
   不存在；fuel 耗尽有可见行为、盲加违反 parity；`L09/README.md` 全文论证 +
   `L10/README.md` 指针）。本轮矩阵订正采纳（§2.1）。
3. [P3] L09/L10 golden 套件 → **已补**（A4-r2 §2-5：l09 +6 条、l10 +4 条
   字面 golden；待 orchestrator 集中跑测裁决）。

**转交 A5（6 项）——4 项闭环，2 项 P3 开放：**
1. [P1·needs-verify] 探测隔离丢失 → **已修**（A5-r1 §2-2 恢复 temp_infer
   克隆）；**但粒度残差升级为新 P2**，见 4.2 新转交。
2. [P2·perf] declb_of 无缓存 → **论证不修**（A5-r1 §5-9：缓存条目持有 bump
   分配与 `bump.reset()` 口径耦合、悬空风险、无 A/B 收益数据；A6-r2 §2-C
   对 L13 同判"非低风险不实施"）。双方结论一致，闭环。
3. [P3] L11/L12 参考版缺 bench_check/bench_check_nf → **仍开放（P3）**，
   A5-r1 未处置；bin 侧按 run 同轴工作不受阻，维持转交 A5 下轮（或接受
   现状登记，bin 无需改动）。
4. [needs-verify] church_src 判型失败疑虑 → **已处置**：A5-r1 §4-3 绿测反例
   推翻排除理由（本轮已实测其锚点行号）；bin 侧本轮补回（§2.2）。
   **needs-verify 残余**：church 在 L11/L12 的实际判型/闸门需 orchestrator
   `cargo run --release --bin l11bench --workload church`（及 l12）实测确认。
5. [needs-verify] gadt_src 快版缺陷区 → **排除理由成立**（A5-r1 §4-3，锚
   `0242720`）；维持排除。缺陷修复后由 A5 通知，bin 侧一句分派即可补入。
6. [P3] L11/L12 golden 套件 → **仍开放（P3）**，A5-r1 §6-4 已自行登记
   （与"探测 ?N 序列不归一化探针"一并下轮补齐）。

**转交 A6（2 项）——全部闭环：**
1. [needs-verify] L13 unification.rs unreachable! 族复核 → **已复核**
   （A6-r2 §2-B：0 个用户可达，全部与 L12 逐字同款不变式，维持原样）。
2. [P2·perf] L13 孪生 declb_of 缓存 → **论证不修**（A6-r2 §2-C：失效条件
   含 arena 生命周期，需专项设计），与 A5 判定对齐。闭环。

### 4.2 本轮新转交

1. **→A5 [P2] L11/L12 参考版探测回滚粒度与孪生/邻层断链**（§2.4）——
   A4-r2 §4-4 的建议未落地；请按整循环口径对齐（窄快照需重做字段可分性
   论证）或论证后登记刻意分歧。**本轮唯一未处置 P0–P2 级跨边界项。**
2. **→A5 [备注]** church 已补回 l11bench/l12bench；若 `cargo run` 实测
   church 判型失败（needs-verify），失败输出回贴后由 A5 定性（参考版 bug
   或语法演进），届时 bin 侧再回退该负载。
3. **→A5 [P3 开放备案]** §4.1-A5-3（bench 口径缺口）与 §4.1-A5-6（golden
   套件）维持开放，均 P3，不阻断收敛。
4. **→orchestrator**：本轮 bin 改动 + A4 的 golden/显示改动 + A5/A3/A4 全部
   修复需集中 `cargo check --all-targets` + parity/blackbox 套件裁决；
   church 负载首跑若 assert 失败，失败差异即裁决数据（预期：全绿）。

## 5. 刻意分歧登记（锚点）

| 分歧 | 锚点 |
|---|---|
| `gadt` 不排入 l11bench/l12bench（快版 walk 缺陷区） | commit `0242720`；`tests/l11_fast_parity.rs:474-476`；本轮两 bin header 文档 |
| l11bench/l12bench 的 basic=全流程 `run` 轴（L11/L12 参考版无 bench_check_nf） | `src/bin/l11bench.rs`、`l12bench.rs` header"口径"节；缺口本身 P3 转交（§4.2-3） |
| church 在 l11/l12 不设 `2n+4` 节点数闭式断言（本层无 parity 钉值，与 l09/l10 不同） | 本轮两 bin church 负载注释 + header；对照 `tests/l09_fast_parity.rs:921`、`l10:551` |
| L09/L10/L11 孪生 unify 无 fuel（参考版同无，六类不适用） | 孪生头注 `L09:1957`/`L10:2022`/`L11:2139`；`L09/README.md` 时代缺口第 2 条、`L10/README.md`、`L11/README.md:19-21`；A4-r2 §2-2 |
| L12 孪生 unify 无 Decl 头展开燃料臂（参考版有 unify fuel——r1 表述订正） | `L12/bump_spine_iter.rs:2190-2197`（订正后头注）；`L12/README.md:19`；A5-r1 §5-6 |
| **订正** r1 §5"fuel 缺口本身转交 A4"行：缺口已销项（A4 不适用论证 + L12 订正），本表取代 r1 §5 对应行 | 本报告 §2.1 |
| 其余延续 r1 §5（L09/L10 哨兵 vs L11-L13 decl 表、η 臂序替代、?N 归一化、l12 不移植 canonical/iddfs、l09 值层卡住 match 限制、bench 负载取舍） | 锚点见 a7-r1.md §5（除 fuel 行与本轮 church 行按上文更新） |

## 6. 终裁（Round3 追加，2026-09-11 · Round4 修正）：church 在 l11bench/l12bench 的处置

**最终裁定（Round4，以 orchestrator 参考版探针实测为准）**：`church_src`
负载在 **L11/L12 参考版本身即判型失败**——实测 `L11_macro::run(church_src(9))`
与 `L12_canonical::run(church_src(9))` 均 Err："can't unify expected: N → N,
find: N @ 113,123"。故 A7-r1 头注的原始排除理由（参考版判型失败）**方向与
理由均正确**；A5 r1 的小样例绿测反例（l11_fast_parity:402/:628、
l12_fast_parity:628）所指程序不是 church_src 本身，不构成对它的证伪；
L11/L12 孪生 `bench_check_nf` 返回 false 与参考版 Err **同判一致**，不是
孪生单侧缺陷，也不适用「0242720 快版已知缺陷区」解释（该锚只管 gadt）。

**证据链**：r1 排除（理由：参考版判型失败，引用 natadd_src 注释）→ A5 r1
小样例绿测反例（被误读为对 church_src 的证伪，r2 据此补回默认列表）→
orchestrator debug 实测：孪生闸门 panic（"fast check+nf 未通过 k=9
(church)"，l11bench.rs:207）→ orchestrator 参考版探针实测：参考版同 Err
@113,123（k=9）→ **Round4 终裁：church_src 在 L11/L12 上下文不合法，
无此负载**（对照：l09/l10 同一生成器 k=10 实测绿——`Type 1` 头注解 +
λ 体 def 的组合仅在 L11/L12 参考版的判型路径下失败）。

**处置（Round4 修正，均属 A7 所有权内，恢复 r1 形态 + 终裁注记）**：
1. `src/bin/l11bench.rs`：`--workload church` 显式入口整体移除（workload
   匹配臂、src 臂、`church_src` 导入、Cli/用法文档行）——负载族恢复为
   natadd/strchain/match/enum/struct/macro；文件头订正为上述实测终裁
   （Err @113,123，k=9），并指明小样例合法形态的 parity 锚位。
2. `src/bin/l12bench.rs`：同位同款改动（负载族 natadd/strchain/match/
   enum/struct/traitchain）。
3. 头注保留「无 church/无 gadt」的并列说明：gadt 的排除理由（0242720
   快版缺陷区）不变，church 的排除理由为参考版判型失败——两者不再混同。
4. Round3 版 §6 中「孪生深链缺陷」「0242720 吻合」的结论**作废**（其对
   church 的定性错误；对 gadt 的定性不受影响）。

**闭环更新**：§4.2-2（church 实测定性）→ **已闭环**（church_src 参考
版判型失败=语法/判型演进，非缺陷、非裂缝；负载维持排除，l09/l10 不受
影响）；§4.2-4 的集中 `cargo check` + parity 裁决预期恢复「全绿」（church
已不在任何默认/显式路径）。本轮新增未处置 P0–P2：0。
