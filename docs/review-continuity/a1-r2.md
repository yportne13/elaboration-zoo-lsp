# A1 Round2 — L01→L02、L02→L03 跨章节继承连贯性

- 输入：`BRIEF.md`、本人 `a1-r1.md`、转交来源 `a2-r1.md` §4.3（L03 parser kw/string）
  与 §4 的处置回执要求。
- 方法：`git status`/`git diff`/`git show 7ff008d` 逐 hunk 核对 + L02/L03/L04 parser
  三方 diff + L02 readme 登记撰写 + wave-1 改动面清点（未运行 cargo，硬约束）。

## 1. 连贯性判定（verdict）

| 继承对 | 结论 |
|---|---|
| L01→L02 | **连贯（维持）**。wave-1（本轮各 agent 的工作树改动）与本切片无关：`git diff --stat -- src/L01_nbe` 零改动，r1 全部锚点（对齐断言、memo 表常驻、`ApplyKnown` 等）原样在位。 |
| L02→L03 | **连贯（维持）**。wave-1 对本切片的改动仅 r1 自身两文件（L02 readme、L03 lex.rs）+ 本轮两文件（见 §3）；L02/L03 代码零他人改动，r1 的逐 hunk 分类全部继续成立。 |

- **L03→L04 边界（A2 主审，本轮受托复验）**：无反向波及——A2 的 wave-1 改动在
  L04 侧仅 `src/L04_implicit/readme.md`（diff 内容恰为 a2-r1 §3 记录的两处文档补充：
  「怎么跑」blackbox_v2 行、「已知限制」pretty 越界保留 panic），L04 代码零改动；
  L03 侧除本人改动外零改动。A3/A4 的 r1/r2 修复全部落在 L07–L10（`git show
  d4c05ea --stat`、工作树 status 证实），各层经 `#[path]` 自包含编译，不波及
  L03/L04 源码；共享文件（parser_lib/lib.rs 等）未动；Cargo.toml 仅 A7 的
  [[bin]] 追加。r1 定义的边界锚点复验在位：`env_ext_defs` tip 判定
  `binds.is_none()`（`L03 bump_spine_iter.rs:209`，与 L04:232 同族逐字）、
  `name_map_shadowing_matches_basic`（`L03 bump:2431`）、lex `get_unchecked`
  SAFETY 精确版注释 L02:78/L03:79/L04:87 三层一致（r1 修复未回退）。
- **任务 1 裁决（a2-r1 §4.3 转交）**：L03 `parser/mod.rs` 的 `kw`/`string` 旧形
  属**零行为差异的纯风格漂移**，且为 7ff008d 清扫的**未完成应用**——该 commit
  文案明确将「kw/string 组合子去掉不可达的 `get(1..)` 错误分支，改守卫 match」
  列为 (L02–L05) 四层清扫项，但其 L03 diff 只做了 `TokenNode`→`Token` 改名、
  未动函数体（`git show 7ff008d -- src/L03_holes/parser/mod.rs` 证实）。行为
  等价证明：`input.first() == Some(x)` ⇒ `input.len() ≥ 1` ⇒ `&input[1..]` 恒在
  界内（len==1 时为空切片），`get(1..)` 的 `ok_or_else` 分支不可达；三条路径
  （匹配成功/类别失配/空输入）的返回值与错误值逐项相同。**选择贴齐而非登记
  刻意分歧**，理由：(a) 旧形无任何测试/文档锚点，登记刻意分歧违背 BRIEF §2
  的锚定要求——它是清扫漏网，不是设计决定；(b) 与 r1 对 L03 lex SAFETY 注释
  的同款处置一致；(c) 贴齐后 L02/L03/L04 三层该两函数逐字节一致（已 diff 验证），
  此后 L03↔L04 parser 的任何 diff hunk 均为纯演进，降低后续评审噪声。**未发现
  行为差异混入**，无需升级。
- **任务 2 裁决（r1 遗留的下沉评估项）**：**不实施 L02 下沉 name_map+复合环境，
  正式登记为教学定位刻意分歧**（`L02_tyck/readme.md`「已知限制」新增条目，与
  memo 口径分歧并列）。理由：
  - BRIEF §2.3 代价判据：~300–500 行 × 双版本（参考版 Cxt + 快版 Machine），
    含 trail 撤销轨迹、tip 判定（`binds.is_none()` 必须随初版带上）等紧邻
    unsafe 打包字的机制细节，且需 l02bench 新增 chain 负载专项验证——本轮
    禁 cargo，无法满足「专项验证」前置，属「只记录、不盲改」类。
  - 适用性：二次方只是潜伏上界——L02 现有负载（church/conv/dup 族）变量引用
    均落链头附近（O(1)），readme 各表实测严格线性（每翻倍 ×2），无任何负载面
    或文档承诺「长链引用最老名字」形态（r1 §2.3 证据继续成立）。
  - 与用户判据不矛盾：「下一章完整继承上一章」约束的是**向上一侧的完备性**——
    L03 = L02 全部代码 + 已文档化演进（成立）；继承方向 L02→L03，低层保留
    朴素实现是 elaboration-zoo 的教学分层（每章一个特性 + 该章够用的机制），
    性能机制向低层回灌是 BRIEF §0 的「评估项」而非继承义务。登记条目已把
    该论证、重启条件（L02 负载面出现链式引用形态时）与移植蓝本
    （L03 readme「名字解析 O(1)」节 + 6efa7dc/ccf943d/985390b/a483331）写明。
- 计数：**P0: 0  P1: 0  P2: 0  P3: 2（均已处置：1 已修、1 登记裁决）  needs-verify: 0**
- 本切片是否收敛（0 个未处置 P0–P2）：**是**

## 2. 裂缝/分歧清单（逐条：位置、证据、分类、处置）

| # | 位置 | 证据 | 分类 | 处置 |
|---|---|---|---|---|
| 1 | `src/L03_holes/parser/mod.rs:113-151`（`kw`/`string` 旧形） | 7ff008d 文案将守卫 match 列为四层清扫项，但其 L03 diff 只改签名类型别名未动函数体；L02:112-135 / L04:131-154 均为新形 | 裂缝（风格级，P3；零行为差异已证明） | **已修**：两函数体贴齐 L02/L04 守卫 match 形（kw 带不变量注释，与 L02/L04 逐字节一致，diff 验证） |
| 2 | `src/L02_tyck/bump_spine_iter.rs`（无 name_map/复合环境） | r1 §2.3/§4.1；本轮正式裁决 | 刻意分歧（教学定位，本轮补正式登记） | **已登记**：L02 readme「已知限制」新增条目（含不回灌论证、重启条件、移植蓝本） |
| 3 | `src/L04_implicit/parser/lex.rs` 相对 L03 缺两条注释行（关键字表注释、ws 的 main.hs 引注） | diff 84d91/157c 证实；L03 保留的注释对 L03 自身正确（关键字表 let/in/λ/U 未变），L04 是删减文档非行为差 | 注释漂移（L04 侧，P3 以下） | 不处置（对方切片、非缺口方向、不制造伪问题） |
| 4 | L03↔L04 `parser/mod.rs` 其余 diff（`parser()` 入口位置、`Icit`/`Either`/花括号 token） | 逐 hunk 核对均为隐式参数特性演进 | 演进 | 无需处置 |

r1 清单第 #8 条（同一 kw/string 漂移，当时登记「仅记录」）由本轮 #1 的贴齐取代，
登记作废。

## 3. 本轮改动清单（文件 → 一句话）

- `src/L03_holes/parser/mod.rs` → `kw`/`string` 函数体贴齐 L02/L04 守卫 match 形
  （补「first() 为 Some ⇒ [1..] 恒在界内」不变量注释；零行为变化，三条路径
  返回/错误值逐项等价）。
- `src/L02_tyck/readme.md` → 「已知限制」新增「环境机制的低层朴素形态（刻意，
  下沉评估已裁决）」条目：教学定位论证 + 不回灌理由 + 重启条件与移植蓝本。
- `docs/review-continuity/a1-r2.md` → 本报告。

（代码改动仅 parser 两函数体等价重写，无 parity/行为影响；黑盒契约不受 touch。）

## 4. 需 orchestrator 转交邻居章节的事项

**无。** a2-r1 §4.3 已闭环（本轮 #1 贴齐）；r1 §4.1（下沉评估）已由本轮任务 2
正式裁决闭环；r1 §4.2（l02bench 口径行）随「不翻转 memo 默认」的裁决失去前提，
一并关闭。复验未发现新的跨边界事项（§1 的 L04 注释漂移属对方切片内部文档
取舍，非缺口，不转交）。

## 5. 刻意分歧登记（锚点：测试名/文档行）

r1 §5 全表继续有效，本轮增补/变更：

| 分歧 | 锚点 |
|---|---|
| L02 朴素环境（线性 types 链 + EnvCons 链）vs L03 name_map+平坦区 | **本轮新增** `src/L02_tyck/readme.md`「已知限制·环境机制的低层朴素形态」条目；L03 readme「名字解析 O(1)」节；commits 6efa7dc/ccf943d/985390b/a483331 |
| （撤销）L02/L03 kw|string 组合子形态分歧 | 原 r1 §2.8 登记——本轮已贴齐消除，不再存在 |
