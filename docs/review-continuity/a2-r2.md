# A2 Round2 — L03→L04 / L04→L05 / L05→L06（复验 + 转交闭环核对）

输入：BRIEF、本人 a2-r1、A3 的 a3-r1/a3-r2、A4 的 a4-r2、A1 的 a1-r1（只读）。
方法：只读 git diff/show/grep + 提取函数体逐字 diff；禁 cargo、禁 git 写操作，
全程遵守。本轮以复验为主。

## 1. 连贯性判定（verdict）

- **转交闭环 1（→A3，a806ff0 alloc_xname）：已闭环**。代码复验：L06:2593 /
  L07:3987 / L08:4079 三方 helper 15 行函数体**逐字节一致**（A3 r2 §2.1
  "md5 相同、仅来源注释按层改名"与实物相符），调用臂
  `if name == "_" { alloc_xname(bump, lp) }` 三层同构（L06:2636 /
  L07:4034 / L08:4126）；A3 报的修前行号 :4079→修后 :4106 漂移已由其自注。
- **转交闭环 2（→A4，L09 侧）：按报告核对通过**。a4-r2 §2-1 所报现位
  L09:3122/3164、L10:3267/3309 与实物逐字吻合，两层 `format!("x{}", lp)`
  0 残留；L09/L10 helper 主体亦与 L04–L08 六方同码（未改文件，仅核对）。
  至此 a806ff0 链 **L04–L10 七层全闭环**，余段 L11/L12/L13 在 A4 r2 §4 /
  A3 r2 §4-2、3 的转交清单中（非我发起，不重复转）。
- **FYI 复核（A3 对 L06 (String,Decl) 宽松臂 vs L07 loose_string 的核对）：
  认可**。证据逐条复验见 §2.2。
- **任务 3（L06 侧快版残留漂移）：无**。L05↔L06 `lams_from_ty` 全函数
  diff 仅剩三类已知演进：文档措辞（L05 版注 vs L06 精简）、`decls/mmap`
  参数穿线（L06 decl 表特性）、尾逗号/换行格式；命名臂两侧同用
  `alloc_xname`，无任何回退到 `alloc_str(&format!` 的残留（L04–L08 快版
  grep 0 命中）。A3 的孪生 L07↔L08 收敛（1070→625 行）未在 L06 侧产生
  反向漂移。
- **任务 4（切片 verdict 复验）：三条边界维持连贯**。wave-1 进入本切片
  源码的改动仅我 r1 的 L06 alloc_xname 区（git diff 证实该文件只有此一个
  hunk）与三个 readme；L04/L05 源码零改动。r1 锚点抽查全部在位：
  `env_ext_defs` tip 判定（`binds.is_none() && flat_base+flat_len ==
  defs.len()`）L03:209/L04:233/L05:254/L06:283 四层逐字一致；η 守卫
  L04 mod 5+bump 5、L05 同、L06 unification 3+bump 5；prune 掩码
  `mask_inner_first.iter().rev()` 消费 L05:1855/L06:2547。A1 对 L03 的
  唯一代码动作是 lex.rs `ident` SAFETY 注释对齐 L02 精确版（纯注释 +
3-1），核对其边界时发现 **L04/L05 同位点未跟进**（本轮唯一新发现，
  P3，已修，见 §2.1）；A1 r1 §2-8 对 `kw`/`string` 组合子旧形的裁决
  （行为等价、刻意分歧、不顺手重排）闭环我 r1 §4-3 转交项。A1 r2 尚未
  落盘——若其 r2 对 L03 parser 另有动作，留待下轮核边界。
- 计数：**P0: 0  P1: 0  P2: 0  P3: 1（已修）+ r1 的 4 项 P3 登记维持
  needs-verify: 0**
- 本切片是否收敛（0 个未处置 P0–P2）：**是**。

## 2. 裂缝/分歧清单

### 2.1 [P3·裂缝·已修] A1 对齐 L03 ident SAFETY 注释后，L04/L05 同位点未跟进
- 位置：`src/L04_implicit/parser/lex.rs:87`、`src/L05_pruning/parser/lex.rs:87`
  （修前）。
- 证据：6c53097 的精确版注释只落 L02；A1 r1（§2-1）本轮把 L03 对齐为三行
  精确版（`L03/parser/lex.rs:79-82`），而 L04/L05 的**同一 unsafe 位点**
  （同 `ident_len` 形态、同变量名 head/after_head/tail）仍留短版单行
  "head/tail 均为 input.data 的前缀"——论证欠精确（tail 是 after_head 的
  前缀而非 input 的），构成 L03→L04→L05 的注释级正向继承缺口。L06 起
  `ident` 已是组合子链式改形（`head.len()+tail_len` 内联），自带三行
  UTF-8 完整性 SAFETY 论证（`L06/parser/lex.rs:150-152` 区），非同源
  位点，不涉、不移植。
- 处置：**已修**。L03 的三行注释逐字贴入 L04/L05（含来源括注"6c53097
  只落了 L02，此处对齐"——该句对任意层为真）。零行为变化，parity 不受
  影响；修后 `L04↔L05 lex.rs` 逐字节一致（git diff --no-index exit 0）。

### 2.2 FYI 复核：认可 A3 的"同构、vt→ty 同源演进、注释在案"
- **把关面同构**：L06 `unification.rs:497-506` `(LiteralType, Decl)` 臂走
  `self.decls.get(&x.data)`——`None => Ok(())`（未登记放行）/
  `Some(entry) => unify(LiteralType, entry.va)`；L07 `unification.rs:557-571`
  `loose_string` 走 `decl.get(n)`——同构的 None 放行 / Some 把关，且
  L07 的 `(LiteralType, Decl)` 与 `(LiteralType, Prim)` 双臂（:696-698、
  :693-695）都汇入 `loose_string`。结构确为同构；Prim 臂是 L07 的内建
  头演进，非 L06 侧缺失。
- **把关内容差异 = vt→ty 同源演进**：L06 `cxt.rs:88-91`
  `string_to_global_type` 返回登记**值** `e.vt`；L07 `mod.rs:528-531`
  同函数返回登记**类型** `e.ty`（注释"登记名给登记类型"在案）。宽松臂
  分别比 `entry.va`/`e.ty`，与各自 `string_to_global_type` 的返回轨完全
  一致——即 A3 所述"与 vt→ty 重设计同源同向"，非裂缝。
- **注释在案**：L07 `unification.rs:686-692` 宽松臂上注明"L06 同款把关"
  "已登记名按登记类型把关"，锚点与 A3 所引（:689-692）相符。

### 2.3 转交项闭环登记（对应 a2-r1 §4）
| 转交 | 闭环依据 | 本轮复核 |
|---|---|---|
| §4-1 →A3（L07:4014/L08:4079 alloc_xname） | a3-r2 §2.1 已修 | 代码复验通过（§1） |
| §4-2 →A4（L09:3127 侧） | a4-r2 §2-1 已修 | 报告与实物一致（§1，只读核对） |
| §4-3 →A1（L03 parser kw/string 旧形） | a1-r1 §2-8 裁决：行为等价，刻意分歧仅记录 | 现状复核吻合（L03 mod.rs:113-121 旧防御形 vs L04:131-138 清扫形） |
| §4-4 FYI →A3（宽松臂把关面） | a3-r2「FYI 核对」结论 | 本轮 §2.2 复核认可 |

## 3. 本轮改动清单

- `src/L04_implicit/parser/lex.rs` → `ident` SAFETY 注释贴齐 L03/L02
  精确版三行（P3，零行为）。
- `src/L05_pruning/parser/lex.rs` → 同上。
- `docs/review-continuity/a2-r2.md` → 本报告。

（源码改动仅注释 +3-1 ×2 文件；`tests/` 无改动。）

## 4. 需 orchestrator 转交邻居章节的事项

- **无新增**。alloc_xname 链余段（L11:3443、L12:3508、L13:5301）已在
  A4 r2 §4-1 / A3 r2 §4-2、3 的转交清单中，由 A5/A6 处置。
- 提示：A1 r2 尚未落盘；本轮 L03→L04 边界按 a1-r1 现状核对完毕。若
  A1 r2 对 L03 parser 另有动作，下轮再核边界。

## 5. 刻意分歧登记

r1 §5 全部条目维持不变（锚点见 a2-r1 §5），本轮新增/更新一条：

| 分歧 | 锚点 |
|---|---|
| L03 `kw`/`string` 保留 7ff008d 清扫前旧防御形（`input.get(1..).map` 三层嵌套），L04+ 为清扫形 | A1 r1 §2-8（行为等价，不顺手重排）；`src/L03_holes/parser/mod.rs:113-121` vs `src/L04_implicit/parser/mod.rs:131-138`；`src/L05_pruning/parser/mod.rs:131-138` 同 L04 |
| L06+ `ident` 为组合子链式改形 + 自带 UTF-8 完整性 SAFETY 论证（与 L02-L05 的 `ident_len` 形态非同源位点） | `src/L06_string/parser/lex.rs:137-165`；7ff008d 重构 |
| L04/L05 ident 注释含"6c53097 只落了 L02，此处对齐"来源括注（本轮贴齐 L03 措辞） | `src/L04_implicit/parser/lex.rs:87-89`、`src/L05_pruning/parser/lex.rs:87-89` |
