# A5 Round2 — L11/L12（Round 3 最终轮：探针编译修复收口 + 探测粒度链上收敛）

> 切片：`src/L11_macro/**`、`src/L12_canonical/**`、`tests/l11_*`、`tests/l12_*`。
> 输入转交：orchestrator（l12_fast_parity E0308 阻塞项）、A4-r2 §2-3/补-3
> （探测粒度收敛）、A4-r2 补-3 FYI（过期注释）。硬约束遵守：未运行 cargo、
> 未动 git 状态、只改本切片文件、最小 diff。

## 1. 连贯性判定（verdict）

- **E0277/E0308 探针编译错全部清零**（本切片内 grep + 静态自查；编译由
  orchestrator 复跑裁决）。
- **L11/L12 参考版探测粒度与快版/L09/L10/L13 收敛为同款**：meta 整表
  快照、整循环一回滚——快版 `run_pure_probe`（8a79162）、L09:184、L10:164、
  L13 pattern_match.rs 同构，链条闭合。
- P0: 0  P1: 0  P2: 0  P3: 1（过期注释，本轮已修）  needs-verify: 0（新增项）
- **本切片是否收敛（0 个未处置 P0–P2）：是**

## 2. 本轮处置清单

### R3-1 【阻塞门禁·已修】L12 pretty.rs 两处 E0308（r2 逐字搬运 SumCase 臂的
L12 SmolStr 演进适配遗漏）

- `pretty.rs:244`（修前）：`format!("{head}::{}", case_name.data)` 返回
  `String`，与 then 臂 `case_name.data.clone()`（`SmolStr`）不兼容。
  **修**：else 臂改 `SmolStr::new(format!("{head}::{}", case_name.data))` ——
  贴齐本层既有构造习惯（`unification.rs:394` `SmolStr::new(format!(...))`
  同款；本文件 `SmolStr::new(&x)` 同族），并加注释说明与 L10 String 族的
  差异来源。
- `pretty.rs:311`（修前）：`sum_head_name` 的 `name.data.clone()` 返回
  `SmolStr`，签名 `-> String`。**修**：`name.data.to_string()`（编译器建议；
  `sum_head_name` 仅被 SumCase 臂以 `format!` Display 消费，String 签名与
  L08-L11 保持同型）。
- **L11 侧静态自查：无此问题**——L11 `Tm::SumCase.case_name: Span<String>`、
  `Sum.name: Span<String>`（String 族），then/else 两臂同为 `String`、
  `name.data.clone()` 即 `String`，与签名一致（mod.rs:88-95 枚举定义佐证）。

### R3-2 【A7→A5·P2·已修】L11/L12 参考版探测改 meta 整表换入换出（贴齐
A4-r2 §2-3 的 L09/L10 同款；收敛 r1 的逐构造器整克隆）

- 位置：`L11_macro/pattern_match.rs` filter_accessible_constrs（修前 :174-179
  的 `temp_infer = infer.clone()` + `temp_infer.check_pm`）、
  `L12_canonical/pattern_match.rs` 同构（:176-181）。
- 修法 = A4 的 L10 模板逐字贴齐（L11/L12 仅 `&cxt.decl` 参数与
  `Raw::Hole(name.to_span())` 差异）：`let meta_snapshot = infer.meta.clone();`
  → 整个构造器循环（含 params 逐层 infer_expr）包进立即执行的
  `(|| -> Result<Vec<_>, Error> {...})()` → 循环后**无条件**
  `infer.meta = meta_snapshot;`（Success/Error 皆然）→ `result`。原
  temp_infer 删除，check_pm 直接跑主 infer（在快照保护内）。
- **与快版口径一致性**：快版 `run_pure_probe` 本就是 metas 整表快照、
  **整循环一回滚**（`L11 twin:5977`、`L12 twin:6126`，params 循环亦在探测
  内——"探测整体回滚，meta 分配不外泄"）；修后参考版与快版**同机制同
  粒度**，也与 L09:184/L10:164/L13 pattern_match.rs 同构，探测粒度链闭合。
  r1 的逐构造器克隆（每构造器各自回滚，探测 1 的解对探测 2 不可见）与
  快版的整循环粒度此前存在层内 ref↔fast 偏差（a4-r2 补-3 矩阵末行），
  本轮消除。
- **字段可分性论证**（写进两文件代码注释；L12 多出两项，逐项：
  - `meta`：唯一探测面（params 循环 Hole 精化的 fresh 分配 + check_pm 的
    insert/求解）→ 快照回滚；
  - `trait_solver: Synth`：每次真查询先 `clean()`（L11 unification.rs:504、
    L12 :517），瞬态残留不跨查询生效（同 A4 对 L10 的论证）；
  - `trait_definition`/`trait_out_param`：trait/impl decl 登记表，探测只做
    表达式级 infer/check，只读；
  - `mutable_map: Rc<RwLock<…>>`：Rc 共享——r1 的整 `Infer::clone()` 本就
    共享同一锁，语义不变；探测路径无 create/change_global 登记；
  - `hover_table`/`completion_table`：仅在 decl elaboration 追加，探测只读；
  - **L12 特有 `meta_contrains: Vec<(Rc<Val>, Rc<Val>)>`**：Flex 求解遇
    Stuck 的挂账（unification.rs:721/:732）。`unify_catch`（mod.rs:682-727）
    **入口清空 + 出口无条件清空**，且 check_pm 的 unify_pm 递归回落臂
    `(_, _)` 恰经 `unify_catch`（elaboration.rs:137-139）——探测期挂账
    要么就地消费、要么同调用内被清，**不持久**，无需回滚（这是 L12 相对
    L09/L10 唯一的新状态面，且是自清型，不影响"只回滚 meta"的充分性）。
- **行为面（如实登记，向快版/链上口径收敛）**：a) 探测期 meta 变化（含
  params 循环 fresh 分配）现随整循环回滚；`?N` 编号差异属已文档化归一化
  偏差，parity 不受影响（A4-r2 §2-3 同款结论）；b) 回滚粒度从"逐构造器"
  变"整循环"（探测 1 的解对探测 2 可见）——修前快版即此行为且 parity
  全绿，参考版向其收敛不翻转任何 parity 判定；c) r1 整 `Infer::clone()`
  每构造器深拷贝 meta+trait_solver+三张注册表+观察表的开销随之消失，
  与 A4-r2 §2-3 的 perf 意图（165313d 的正确落地方向）在 L11/L12 对齐。

### R3-3 【A4-r2 补-3 FYI·已修】过期注释订正

- `L11/L12 pattern_match.rs`："……L09/L10 参考版亦克隆"——随 R3-2 的注释
  块整体重写（新注释已改为"L13/L09/L10 同款窄快照"），不再存在。
- `L11/L12 bump_spine_iter.rs` 孪生 `filter_accessible_constrs` 外层文档
  （修前"构造子类型链逐层推断（**真实 infer**——meta 分配保留）……参考版
  克隆 `temp_infer` 的对应物"）：两处均已过期（孪生实际是整循环
  run_pure_probe 含 params 循环；参考版已窄快照），改为"逐构造子的
  infer_expr/check_pm 都可能分配 fresh meta……整循环包进 run_pure_probe
  （参考版同款 meta 整表快照、整循环一回滚，a4-r2 §2-3 口径）"。
- 复核：两切片内 grep `temp_infer` = 0 命中，"亦克隆"措辞无残留。

## 3. 本轮改动清单

- `src/L12_canonical/pretty.rs` → SumCase 臂 else 分支 `SmolStr::new` 包装 +
  `sum_head_name` 返回 `.to_string()`（R3-1，E0308 清零）
- `src/L11_macro/pattern_match.rs` → 探测改 meta 整表快照/整循环回滚
  （R3-2，含字段可分性注释）
- `src/L12_canonical/pattern_match.rs` → 同上 + meta_contrains 自清论证
  （R3-2）
- `src/L11_macro/bump_spine_iter.rs`、`src/L12_canonical/bump_spine_iter.rs`
  → 孪生 filter_accessible_constrs 外层文档订正（R3-3）
- 测试：无改动（r2 已修的 E0277 探针不受影响；断言零变更）

## 4. 转交 / 同步

- 无新增转交。同步信息：a5-r1.md §2-2（探测隔离"恢复克隆"的处置）与
  §5-7/§5-8 的登记自本轮起以本报告为准（粒度已升级为整循环窄快照）；
  a4-r2 补-3 矩阵"L11/L12 参考版 = 整 Infer 克隆、逐构造器"一行已过时，
  现状为 meta 窄快照、整循环一回滚（与 L09/L10/L13/各层快版同口径）。

## 5. 静态自查记录（编译由 orchestrator 裁决）

- IIFE 闭包捕获：`infer: &mut Infer`（独占、立即调用后借用结束）、
  `accessible: &mut`（闭包内 move 出）、`cxt/forced_type/all_constrs` 共享；
  与 A4 已落地的 L10 模板同构。
- 平衡自查：本轮触碰的 5 个文件相对 HEAD 的 `{}`/`()` 计数 delta = 0。
- L12 `SmolStr` 导入在 pretty.rs 已存在（:140 等既有用法），无新导入。

## 6. Round 3 补救（终门禁：显示贴齐方向反了，回滚）

> 证据链（orchestrator 提供 + 本轮复核）：`src/L12_canonical/mod.rs:1048/
> :1373/:1799`（`Point[Nat]::Point.mk(4, 6)`）、`:1479/:1591`（
> `Vec[Bool]::cons(1, Bool::false, Vec[Bool]::cons(0, Bool::true,
> Vec[Bool]::nil)`）与 `src/L13_namespace/legacy_tests.rs:604/:716`（同
> Vec 串）都锁定 **comma + 头类型参数格式**——这是 L09 起的血统格式。
> r2 的 R2-3 把 A4 给 L09/L10 误移植的 L08 空格格式贴齐进 L11/L12，破坏
> L11→L12→L13 显示血统，l12 侧 test0/test2/test7/test8/test_trait 共
> 5 条失败。A4 并行回滚 L09/L10 同一移植。

### 补-1 回滚结论（已执行）

- `src/L11_macro/pretty.rs`、`src/L12_canonical/pretty.rs` 的 SumCase 显示
  臂恢复 HEAD 原格式：`{头}[{隐式实参 逗号}]::{分支}({显式实参 逗号})`
  （L12 的 Nat 字面守卫臂 `pretty_nat` 在通用臂之前，原样保留）。
- **保留的改动**（与回滚不冲突）：r1 的 `go_ix` 越界退化（`@{ix}`）、
  SumCase 非 Sum 头的 `sum_head_name` panic 降级（仅把 `_ => panic!`
  换成降级调用，格式本体未动）、L12 `sum_head_name` 的 `.to_string()`
  （String 签名所需）。**r3 的两处 E0308 修复随回滚自然消失**——旧臂
  本就编译通过（`ctor` 中间变量与 `SmolStr` 包装一并移除），回滚后静态
  自查（括号配平 delta=0、对照 HEAD diff 仅剩降级/helper 增量 +21/-4
  行）确认无需任何 SmolStr 适配。
- r2 报告 §R2-3 与 §R3-1 中"显示贴齐/SmolStr 修复"相应条目由本节废止；
  旧行为面评估（parity 双 oracle 共用 pretty、无字面 golden）反向适用：
  回滚同样不翻 transfer parity 判定。

### 补-2 L12 内置 5 条断言静态对齐（恢复后渲染路径逐字符核对）

- `Point[Nat]::Point.mk(4, 6)`（mod.rs:1048/:1373/:1799）：头 `Point` +
  隐式参数 `Nat` → `{head}[{impls}]` = `Point[Nat]`；`::` + `Point.mk`；
  显式实参 `4`/`6` 逗号连接 → `(4, 6)`。逐字符一致 ✓
- `Vec[Bool]::cons(1, Bool::false, Vec[Bool]::cons(0, Bool::true,
  Vec[Bool]::nil)`（mod.rs:1479/:1591，contains 前缀）：外层
  `Vec[Bool]::cons` + 实参 `(1, Bool::false, <内层 cons 同臂递归渲染>)`，
  最内 `nil` 无实参 → 无括号后缀（`unwrap_or("")`）。前缀逐字符一致 ✓
- 上述即 l12 侧失败的 5 条（test0/test2/test7/test8/test_trait）与
  bits_adder 断言的唯一渲染路径；orchestrator 复跑 l12_fast_parity/
  --lib 裁决。

### 补-3 其它 r1/r3 改动不受影响的确认

- 探测隔离/窄快照（r1 恢复 + r3 升级 meta 整表快照整循环回滚，含孪生
  文档订正）：不在 pretty.rs，未触碰。
- (Obj,Obj) 合同臂位次（r1，L11/L12 双版）：未触碰。
- `is_flex` 求解短路、`v_to_ref_val` 宽放、`vals_eq_ground` Match case 表、
  可达性先记/checked_ret 键型、`alloc_xname`、u64/宏探针 E0277 修复：
  均未触碰。
- 本轮回滚文件仅 `pretty.rs` ×2 + README 登记 ×2。

### 补-4 README 登记新增（两份 README 已更新）

- **SumCase 显示格式分层**：L11/L12（及 L13）为 comma + 头类型参数族
  （`Vec[Bool]::cons(1, …)`、`Point[Nat]::Point.mk(4, 6)`），L09 起血统，
  被本章内置测试与 L13 `legacy_tests.rs:604/:716` 字面锁定；与 L08 的
  空格 + `[名]` 隐式格式**分层，不得互贴**（a4-r2 误移植、终门禁回滚，
  本条即为防复发的锚点）。锚点：`src/L12_canonical/mod.rs:1048/:1373/
  :1479/:1591/:1799`、`src/L13_namespace/legacy_tests.rs:604/:716`、
  `src/L11_macro/README.md`、`src/L12_canonical/README.md`。
