# A1 Round1 — L01→L02、L02→L03 跨章节继承连贯性

- 评审对象：`src/L01_nbe`（22 变体研究层）、`src/L02_tyck`、`src/L03_holes`
  （各含参考版 `mod.rs` + 快版孪生 `bump_spine_iter.rs`）、`tests/l02_*`、`tests/l03_*`
- 方法：`git diff --no-index` 逐 hunk 分类（快版与参考版都做）+
  `git log --oneline --stat` 单层 commit 扫描 + readme 选型/移植表逐行对码 +
  `tests/l02_*/l03_*` 契约锚点清点。未运行 cargo（硬约束）。

## 1. 连贯性判定（verdict）

| 继承对 | 结论 |
|---|---|
| L01→L02 | **连贯**。快版配方（bump arena、3 位 tag 打包值、spine 栈 len/base、ChainRun 流式 quote、双栈 eval + 右链快速路径、Machine 稳态、quote/conv 记忆化）逐项在 `L02/bump_spine_iter.rs` 落地，与 L02 readme「性能版移植清单（L01→L02）」逐行相符；L01 readme 选型结论（默认 `bump_spine_iter` + 长驻进程 Machine 稳态、memo 按"重复强制负载"开启）在 L02/L03 如实延续。L02 后续改进（`ApplyKnown` β 直送、`ChainWrap(0)` 消除）是超出 L01 配方的演进，已由 L02 readme 记录。 |
| L02→L03 | **连贯**。快版/参考版全部差异 hunk 可归类为：元变量机制的演进（force/solve/invert/rename/flex 头探测/AppBds）、L03 专属性能演进（name_map、复合环境、常驻工作栈、受控内联环、unify 判等记忆化）、以及有测试/文档锚点的刻意分歧（见 §5）。L02 侧的全部修复（71c7fec `EvalCod2`+消融开关 `is_ok_and` 语义、7303d3b fail-fast 移除、a483331 注释勘误、c7d5164 轮清 spine、92e7039 memo 表常驻、9f15a4a↔2eee834 防御性回灌、113ac2c bench mem::forget、6c53097 对齐+SAFETY）经核对全部跨层在位。 |

- 上轮（review-l01l12）修复在本切片的落地核验：**全部在位**——
  - `#[repr(align(8))]` + `align_of ≥ 8` 断言：L02/L03 快版各 3 处（CloCell/PiCell/EnvCons，
    `bump_spine_iter.rs:131-153` / `:164-242`）；两章参考版 `mod.rs` 无 ptr|tag 编码类型
    （enum Val + Box/Rc），无适用点。L01 侧：spine 族 5 变体全部 `use super::bump_spine::`
    共享同一个已加固的 `CloCell`（`bump_spine.rs:56-90`，2 位 tag 断言 ≥4），`native_clo.rs`
    独立断言在位；`bytes_env_arena.rs` 的指针使用是 `write_unaligned` 字节缓冲，无 ptr|tag
    解码——**fa03e14 覆盖完整**。
  - fa03e14 的 `bench_cek_deep` 早退分支 `mem::forget(check)`（`bench.rs:884-893`）两条
    退出路径都已封堵；`bench_size` 经上轮复核对称析构无风险。
  - η applicability 守卫 / 全局哨兵 `>= GLOBAL_BASE` / `prune_ty` 掩码反转 / `intersect_go`
    / MacroDepthGuard / trait 单向匹配 / `to_typ` u64：**对 L01–L03 均不适用**（无全局 decl、
    无 pruning、无 Sum、无宏、无 trait、无字面量；L02/L03 的 η 臂是纯比较形态，
    `spine.push(非λ, fresh)` 可表示、无 panic 类）。
- P0: 0  P1: 0  P2: 0（未处置）  P3: 3（1 已修、1 已补文档、1 仅记录）  needs-verify: 0
- 下沉候选（非缺陷，转交评估）：2（见 §4）
- 本切片是否收敛（0 个未处置 P0–P2）：**是**

## 2. 裂缝/分歧清单（逐条）

| # | 位置 | 证据 | 分类 | 处置 |
|---|---|---|---|---|
| 1 | `src/L03_holes/parser/lex.rs:79`（`ident` 的 get_unchecked） | 6c53097 给 L02 写了精确版 SAFETY 注释（head/tail 前缀论证），L03 只留短版——同源位置注释未同步 | 裂缝（注释级，P3） | **已修**：L03 对齐 L02 精确版 |
| 2 | `src/L02_tyck/bump_spine_iter.rs`（`Tycker::run` 默认普通口径）vs `src/L03_holes/bump_spine_iter.rs:2045`（`run`=memo 默认，L04/L05 同款） | e8289cb 只在 L03 把 quote_memo 提为生产默认；L02 保持 `run`/`run_memo` 双口径，无文档说明该分层分歧；两口径输出逐字节一致（`church_memo_matches_basic` 等互检），分歧仅在默认值 | 刻意分歧（补文档后成立） | **已补**：L02 readme「已知限制」加一条（含不回灌理由：翻转需同步 `src/bin/l02bench.rs` 口径行，bin 非本章所有） |
| 3 | L02 快版无 name_map（O(1) 名字解析）/复合环境（平坦 def 区域），L03 快版有（`6efa7dc`/`ccf943d`） | L02 的 `Raw::Var` 解析线性走 types 链、`nth` 沿 EnvCons 链；现有 L02 负载（church/conv/dup 每层 def 引用最近名字，链头 O(1)）不触发二次方，且 L02 readme 无"链式负载线性"的声明 | 演进+下沉候选（P2 候选，非缺陷） | 仅报告，转交评估（§4.1） |
| 4 | `tests/l03_blackbox_v2.rs` 走 `run`（memo）+ 模块内测试走 `run_no_memo`；L02 恰好相反（blackbox 走普通 + 模块内 `run_memo`） | grep 核对：两章的**两个口径都有测试行使**，覆盖对称，无缺口 | 演进（默认值差异的镜像） | 无需处置 |
| 5 | L03 unify 无 `NO_BITEQ` 消融开关（L02 conv 有 `L02_NO_BITEQ`） | L03 的位相等剪枝受 `L03_NO_CONV_MEMO` 之外的开关统辖缺失；但结构路径独立成立（`(3,3)`/`(0,0)` 显式分支在位，L02 教训 3 的不变量仍满足） | 刻意分歧（bench 工具性，P3） | 仅记录 |
| 6 | 参考版 `pretty` 的 `Tm::Let` 臂排版：L02 `"\n    = "`+`"\n;\n"` vs L03 `"\n  = "`+`";\n\n"`（`L02 mod.rs:440-465` / `L03 mod.rs:803-824`） | L02 的 nf/type 输出永不产生 `Tm::Let`（eval 消除 let）——该臂是死代码；L03 经 elab 模式可达且与上游一致。各层输出被各自 blackbox 逐字节锁定 | 刻意分歧（死代码排版漂移，P3） | 仅记录（改了反而无判据） |
| 7 | L03 mod.rs 快版/参考版部分条目 `pub` vs L02 `pub(crate)` | tests/bins 全部经 `#[path]` 直接编译层源码（`tests/l02_blackbox.rs:23-30`、`src/bin/l0{2,3}bench.rs`），可见性无影响 | 刻意分歧（ba4617e 只收紧了 L02，P3） | 仅记录 |
| 8 | `parser/mod.rs` 的 `kw`/`string` 组合子：L02 用 `&input[1..]`+不变量注释，L03 用防御性 `input.get(1..)` | 行为等价（`first()==Some ⇒ len≥1`），两种写法各自自洽 | 刻意分歧（形态漂移，P3） | 仅记录（不顺手重排） |
| 9 | L02 的 `ApplyKnown`/`ChainWrap(0)` 消除未下沉回 L01 的 `bump_spine_iter.rs` | L01 是 22 变体封闭对比研究，其 readme 消融表/实测数字锚定在各自变体形态上；L02 readme「移植清单」已把该刀记为 L02 演进 | 刻意分歧 | 仅记录（改 L01 变体会作废其基准叙事） |
| 10 | L01 `Machine` 小栈 `with_capacity(64)`（afe9809）未出现在 L02（`Vec::new()`） | L02 的 `Machine` 头注释（f7496d9）实测论证了"新建小缓冲更热"，L03 侧工作栈已常驻（f9046fc）——两章各有实测锚点 | 刻意分歧 | 仅记录 |
| 11 | 单层 commit 全量核对：`253ab40`(L01 rpn 混头) / `985390b`(L03 平坦区 λ 体内 define) / `e541de0`(L03 同号 flex-flex) / `62f7740`(L03 受控环) / `a806ff0`(L03 lams 栈上格式化，L02 无 solve) / `f9046fc`/`b415bad`/`f9e7505`(L03 scratch，L02 有 f7496d9 负结果存档) | 逐一核对病灶是否存在于其余层：rpn 变体 L02/L03 没有；平坦 def 区/同号 flex-flex/lams 仅 L03 机制；受控环的 L02 对应物（纯 conv 无求解副作用）经修订论证保持非受控环。**均无跨层裂缝** | 演进 | 无需处置 |

## 3. 本轮改动清单

- `src/L03_holes/parser/lex.rs` → `ident` 的 get_unchecked SAFETY 注释对齐 L02 精确版（6c53097 只落了 L02）。
- `src/L02_tyck/readme.md` → 「已知限制」补一条：quote 记忆化默认口径的分层分歧（L03+ 默认 memo，L02 双口径默认普通）及不回灌理由。

（均为注释/文档级，零行为变化；parity 不受影响。）

## 4. 需 orchestrator 转交邻居章节的事项

1. **L02 下沉 L03 的 name_map + 复合环境（评估项，非缺陷）**
   - 现状：L02 快版 `Raw::Var` 线性走链解析、`nth` 沿 EnvCons 链取值；"长 let 链引用最老名字"
     形态在 L02 是 O(n²)（L03 readme chain 负载实测 383ms→5.93ms 的同一病灶）。L02 现有
     负载与文档均不触发/不承诺该形态，故本轮不算裂缝、不盲改。
   - 若实施：按 `6efa7dc`（Machine 持 `name_map` + `Cxt.mark` 撤销轨迹）与 `ccf943d`/`985390b`/
     `a483331`（defs 平坦区 + `env_nth`/`env_ext_defs` tip 判定）移植到 `src/L02_tyck/bump_spine_iter.rs`
     与 `mod.rs` 的 Cxt；注意 a483331 的 tip 判定（`binds.is_none()`）必须随初版带上。
     属 ~300-500 行机制移植，需 l02bench 新增 chain 负载验证（建议下一轮由 A1 带 cargo 验证执行）。
2. **`src/bin/l02bench.rs`（A7/共享侧配合）**：若采纳"L02 run 默认翻转为 memo"以对齐 L03+ 惯例，
   需同步把 bin 的 `fast`/`fast_memo` 口径行改为调用普通口径方法（或将 `run_memo` 更名
   `run_no_memo`）并更新 readme 测量方法论——bin 不属 A1 所有权，单独转交。

## 5. 刻意分歧登记（锚点）

| 分歧 | 锚点 |
|---|---|
| L02 conv 非受控内联环（无求解副作用故健全）vs L03 受控内联环 | `tests/l02_blackbox.rs:843` `conv_eta_absorption_accepted_by_both`；`tests/l03_blackbox.rs:1230/1237` `unify_eta_absorption_matches_basic`/`unify_meta_shorter_side_solvable`；L02 readme「后续提速」修订节+教训 5、L03 readme「移植清单」末行 |
| L02 小栈每调用新建 vs L03 常驻 | L02 `bump_spine_iter.rs` `Machine` 头注释（f7496d9 实测负结果存档）；L03 `Machine` `workbuf` 字段注释（"别把这条往 L02 回灌"） |
| L02 quote memo 非默认 vs L03+ 默认 | 本轮新增 `L02_tyck/readme.md`「已知限制」条目；L03 readme「实测结果」"现已成为生产默认" |
| L02 报错文案 `variable out of scope`/`type mismatch` vs L03 `Name not in scope`/`Cannot unify` | `tests/l03_blackbox.rs` 头注「与 L02 的语义差别」；上游 02/03 各自忠实 |
| L02 `_` 项位置是惰性垃圾 vs L03 是 hole | `tests/l02_blackbox.rs` 头注「已知怪癖」+ `underscore_in_term_position_is_inert_junk`；`tests/l03_blackbox.rs` `underscore_in_term_position_is_a_hole` |
| L02 无 name_map/复合环境（二次方潜伏） | L02 readme 无 chain 负载承诺；L03 readme「名字解析 O(1)」节为本轮 §4.1 的移植蓝本 |
| L01 变体形态冻结（不含 ApplyKnown 等后继改进） | `L01_nbe/readme.md` 消融表与实测数字；L02 readme「移植清单」末两行 |
| L03 unify 无独立 NO_BITEQ 消融开关 | L03 readme「打包值上的元变量机制」第 4 条（位相等 force 前后各查一次，消融只提 NO_CONV_MEMO） |
