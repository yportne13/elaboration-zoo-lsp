# A3 Round1 — L07/L08/L09 章节继承连续性审计与修复

- 工作树：`F:/projects/hermes/elaboration-zoo-lsp-cont`（分支 `review/l01l13-continuity`，基于 master a99972a）。
- 方法：`git diff --no-index` 逐对相邻章 + 函数级清单（`grep 'fn '` 集合比对）+ 定点源码核读。静态审计，未运行 cargo（按规则 3）。
- 注意：本 worktree 有其他子 agent 并行工作（diff 里 L01/L10-L13/README 等非本切片改动与他们相关）；本切片只动了下表所列文件。

## 1. 结论（verdict）

- **P0: 0  P1: 0  P2: 5（3 已修 / 2 报告路由）  P3: 4  needs-verify（承接上轮）: 3**
- **本轮收敛判定：CONVERGED（自审切片内）**——全部可安全修复项已清零；剩余 2 条 P2
  是需要编译+测试验证的**耦合语义移植**（L09 Obj 剥链精确化 + (Obj,Obj) 合同臂），
  在禁跑 cargo 的条件下盲改风险高于收益，已给出精确移植方案交协调者决策。

## 2. 上轮修复在位验证（fd25b74 / b7b4225 已入 HEAD，逐项核实）

| 项 | 状态 | 证据 |
|---|---|---|
| L07/L08/L09 `XCell`/`CloCell` `#[repr(align(8))]` + 编译期断言 | ✅ | L07 bump:265/390/409、L08 bump:275/400、L09 bump:268/406/425 |
| L09 全局哨兵 `>= GLOBAL_BASE` 8 处（eval/quote/rename/lvl2ix/Raw::Var） | ✅ | L09 bump:888/997/1333/1484/2690/2735/4208/4321 |
| L09 `intersect_go` 长度失配 `_ => None` | ✅ | L09 unification.rs:486/492 |
| L08/L09 `prune_ty` 掩码反转（`rev.reverse()`） | ✅ | L08 unification.rs:152、L09 unification.rs:128 |
| L09 `pretty::go_app_pruning` 非 panic 实现 | ✅ | L09 pretty.rs（L06 同款） |
| L09 `v_app` 卡住 match 限制的模块头记录 | ✅ | L09 mod.rs:1-7 `//!` 已知限制段 |
| L09 `check_universe` meta→U(0) 的 TODO/needs-verify 状态 | ✅ 未变 | L09 elaboration.rs:157 `//TODO:x?`；上一轮决定保持 |

## 3. 发现清单

### [P2 · 已修] L08 丢失 L07 的「enum 隐式无标注域钉 U」修复（继承回退）
- 位置：`src/L08_product_type/elaboration.rs`（Decl::Enum 臂，修复前无钉 U 逻辑）、
  `src/L08_product_type/bump_spine_iter.rs` Decl::Enum 臂（同）。
- 证据：L07 elaboration.rs:241-262 与 L07 bump:5573-5587 均有「`[A, B]` 无标注隐式域
  钉为 `U`」的黑盒三轮修复（L07 README:271 表格记录）；`git diff L07→L08 elaboration.rs`
  显示该 hunk 在 L08 **整块删除**。触发形态（L07 tests/l07_blackbox_v3.rs:1329 的
  `pack_unannotated_no_index`）：`enum P1[A, B] { p1[A, B](a: A, b: B) -> P1[A][B] }` +
  `p1[Nat][Bool] zero true` —— 第 2+ 个参数的域 meta 带 pruning，显式供参时 invert
  无法倒序 Decl 头 spine → 误报 can't unify。**struct 脱糖（`p_struct`）走的正是
  Decl::Enum 臂**（L09 前所有层同构），故 `struct P[A, B]` 同样受害。
- 处置：**已修**——把 L07 的钉 U 代码逐字回移到 L08 参考版与快版（两版同步，parity
  不受影响），并加 4 源 parity 探针 `tests/l08_fast_parity.rs::parity_enum_struct_impl_hole_pinned_u`
  （enum 显式实例化 / 全显式构造子 / struct 多参数投影 / 显式标注不动）。

### [P2 · 已修] L07/L09 的 Pi/Pi 臂 `quote(cxt.lvl)` η 下探层级越界（L08 已修、前驱/后继均未跟进）
- 位置：`src/L07_sum_type/unification.rs:611`（修前）、`src/L09_mltt/unification.rs:528`（修前）。
- 证据：L08 unification.rs:612-620 已用当前层级 `l` quote 并注释「L07 同码但无触发用例」；
  L08 README §5 明言「**L07 同码潜伏（其测试集恰好无触发用例），可择机回合**」。机制：
  Lam 的 η/Lam-Lam 递归臂 `unify(l+1, …)` 不推进 cxt，随后 Pi/Pi 臂用 `cxt.lvl` quote
  域值，域含 `Rigid(l)` 时 `lvl2ix` u32 下溢（debug panic / release 错误索引）。L09 参考版
  同型（L09 快版结构不同无此路径，与 L08 快版同理）。这是「优秀修复只落在部分章节」的
  标准形态：修复落在 L08，L07（前驱）与 L09（后继，twin 移植晚于 L08 修复两天）都没拿到。
- 处置：**已修**——两处 `quote(cxt.lvl, …)` → `quote(l, …)`，附 L08 同款注释。对
  `l == cxt.lvl` 的全部既有路径逐字节等价；仅原本会越界的路径变为正确 quote。

### [P2 · 已修（加固）] η 臂缺可应用性守卫（任务点名线索，核实后定性为「同型路径存在、已知触发关闭」）
- 位置（修前）：L07/L08 参考版 unification.rs η 两臂 + 快版 bump η 两臂；L09 同（参考
  unification.rs / 快 bump:2220/2235）。共 6 站点。
- 核实结论（与任务线索的差异点，重要）：
  1. **同型 panic 路径存在**：η 臂把非 λ 一侧交 `v_app`/`vapp1`，两侧均为
     `panic!("impossible apply")`（参考版 mod.rs v_app / 快版 vapp1），U/Π/字面量/
     LiteralType/Sum/SumCase 与 λ 相遇即崩，且参考/快版同崩（parity 一致）。
  2. **L06 的触发机制在本切片不存在**：L06/L11/L12/L13 的 `string_to_global_type`
     返回 def **登记值**（可为 λ，"动态类型"逃逸舱口）；**L07/L08 返回 `e.ty`（声明处
     check_ty 过 U 的登记类型）**，λ 进类型位被类型检查关闭；**L09/L10 根本没有
     st2g/get_global**（L09 builtin 仅 string_concat，模块注释 bump:24-27 明示）。
     故 L06 fix commit（1f40085）里「跨章嫌疑：L07/L08…待各章 owner 回灌」的触发前提
     在这几章不成立——这是**语义分叉**（L06/L11/L12/L13 = VALUE 语义 vs L07/L08 =
     TYPE 语义），不是同 bug 漏修。
  3. 尽管如此，守卫为**零行为变化加固**（守卫集 = v_app 可应用集，真值路径照旧；唯一
     变化是原本 panic 的分支改判 unify 失败 Err），且 L11/L12 同款守卫已合入、L13 有
     `is_appliable`——补齐后 L04-L09/L11-L13 全链一致。
- 处置：**已修**。L07/L08 参考版加 `v_applicable`（Flex|Rigid|Decl|Obj|Prim|Match =
  v_app 可应用集减 Lam）；L09 参考版（Flex|Rigid|Obj，按 L09 v_app 裁剪）；快版加
  `vapp_ok`（按各自 vapp1 的 panic 集取反）并守卫 η 两臂。被守卫拒掉的对落空后
  参考版走 `_ => Err(UnifyError)`、快版 `return false`，同判。

### [P2 · 报告路由，未修] L09 Obj 剥链以 `U(0)` 占位实例化显式依赖字段（L08 评审修复未传播）
- 位置：`src/L09_mltt/elaboration.rs:461`（`//TODO:not Val::U(0)`）、快版
  `src/L09_mltt/bump_spine_iter.rs:4343` 起（注释明示「U(0) 占位怪癖保留——参考版
  TODO 注释原样」，两版 parity 一致）。
- 证据：L08 的 Raw::Obj 剥链在评审中修过（commit 8988f7c「前字段 binder 以接收者
  **卡住投影**实例化（旧 U 占位令 Exists.proof 在检查位假拒）」）；L09 保留 struct/
  投影语言面（p_struct、Raw::Obj 都在），twin（09-06）移植自 L08 冠军配方（09-04，
  已含修复），但剥链仍按 L09 参考版旧语义保留 U(0)。后果：`struct Exists (P : ...) {
  witness : ..., proof : P witness }` 这类**依赖在前字段的字段**在检查位假拒
  （`P e.witness` vs `P (U 0)` 合一失败）——L08 通过、L09 误拒，用户可见完备性缺口。
- 未修原因：这是**耦合语义移植**——剥链精确化会让 `e.witness ≡ e.witness` 形态进入
  unify，必须同批补 (Obj,Obj) 合同臂（见下条），且参考版+快版四处同步；无编译/测试
  验证条件下盲改风险高。**建议交协调者立项**：按 L08 elaboration.rs Raw::Obj 臂
  （`eval(Obj(接收者项, 前字段名))` 实例化）+ L08 unification.rs (Obj,Obj) 臂 + L08
  快版 HK_OBJ 统一臂三件套移植，L09 的 eval 签名（无 decl 表）需相应适配。

### [P2 · 报告路由，未修] L09 unify 缺 (Obj,Obj) 合同臂（同上，L08 修复未传播）
- 位置：`src/L09_mltt/unification.rs`（无 `Val::Obj` 合一臂，只有 rename:263）、
  快版 unify_iter（无 HK_OBJ 合同臂；HK_OBJ 仅用于头分类）。
- 证据：L08 unification.rs:560-566 的 `(Val::Obj(o1,f1,sp1), Val::Obj(o2,f2,sp2))
  if f1.data == f2.data` 合同臂（字段名同比接收者+实参）是 8988f7c 评审修复；
  L09 两版均无——目前 `Obj` 对 `Obj` 落兜底 Err。与上一条共生（L09 剥链不精确化时
  该形态难达，精确化后必需）。
- 处置：**仅报告**，与上一条合并立项。

### [P3 · 仅报告] L07 无 (Obj,Obj) 合同臂
- L08 README §2 已论证 L07 该形态不可达（投影类型走剥链直接出结果）；属「后继章
  因特性演进才需要的新臂」，非继承缺口。不动。

### [P3 · 仅报告] L09 无「隐式无标注域钉 U」
- L09 的 enum elaboration 无钉 U，但 L09 有宇宙分层——钉 `U(0)` 会把 Type 1 参数
  误钉到 0 层，L09 的 `universe_lvl` 推断循环（elaboration.rs:317-325）是另一套处理。
  机械回移不正确，属 L09 特有设计问题；如 L09 用户报告同类 can't unify 再议。

### [P3 · 仅报告] L07 `Spine::clear` 在 L08 被内联为 `self.spine.stack.clear()`
- 纯风格分叉（功能等价，clear_round 语义一致）。不动。

### [P3 · 调查结论，无需动作] L09 无 tests.rs / l09_blackbox / l09bench —— **有意时代约定，非遗漏**
- 证据：blackbox 套件只到 L08（tests/ 目录 l02-l08_blackbox*；L09-L12 均无）；
  lib 内嵌 tests.rs 只到 L08（L09-L12 目录均无该文件）；bench 二进制为 l01-l08 +
  l13（Cargo.toml:15-48，无 l09-l12）。L09 起的章节测试约定 = `l09_fast_parity.rs`
  式双 oracle 互检（16 例，头部注释明示「tests/l08_fast_parity.rs 同款」）。
  L09 的 twin commit（2a949ef）只承诺 parity 套件。**结论：与 L10-L12 一致的分层
  约定，不补**；亦无需动 Cargo.toml/src/bin（超出我的路径，且无事实需求）。

### needs-verify（承接上轮，状态不变，本轮复核）
1. **L09 `v_app` 对卡住 match 应用 panic**：模块头限制段在位（mod.rs:1-7）；本轮
   顺带核了「安全修法」——无：需值层 splice + 两版同步大改，保持记录。
2. **L09 `check_universe` 把 meta 解成 U(0)**：elaboration.rs:157 附近 TODO 原样；
   可达路径上 mty 恒为 U(0)（上轮已论证），保持记录。
3. **L09 unification 无 fuel**：本轮确认 L07/L08 参考+快版均有 `UNIFY_FUEL=4096`
   （mod.rs:245/251 + 快版 Fuel），L09 两版皆无——时代缺口保持记录（快版 force 注释
   明示「无燃料…参考版同款裸递归」，属有意对齐 L09 参考版）。

## 4. L08→L09 bump_spine_iter.rs 754 行缩水的逐块解释（7210 → 6456）

结论：**合理重构/裁剪 + 三处 L08 评审修复未传播**（后者见 §3 后两条 P2 + 已修的
quote 层级）。函数清单比对：L08→L09 消失 33 个函数、新增 22 个，逐块对应：

| 块 | 行数效应 | 定性 |
|---|---|---|
| `prim_reduce` 内建族（file_exists/report_check_issue/HDL 族等 ~15 个 prim） | ≈ −350 | 有意：L09 builtin 仅 `string_concat`（模块注释 bump:24-27、commit 2a949ef） |
| `struct_eq` 族 7 函数 + Match/Match struct_eq 快路径 | ≈ −150 | 有意：L09 Match/Match 走中性重求值，无 struct_eq 快路径（与 L09 参考版一致，参考版也无 struct_eq.rs） |
| Match `pending`（卡住 match 实参吸收）+ v_app Match 臂 + unify/quote 的 pending 对比 | ≈ −100 | 有意但为**特性缺口**：即 mod.rs 头注的 v_app 已知限制（L07 起才有 splice） |
| fuel（`burn`/`spend`/`Fuel`/充值点） | ≈ −40 | 有意：L09 无燃料（needs-verify #3） |
| pm_defs 机制（`pm_solve`/`pm_mark`/`pm_restore`/`pm_solvable_contains`/`pm_defs`） | ≈ −200 | 架构替换：`check_pm`/`unify_pm`/`update_cxt`/`refresh`/`check_pm_final` 全链（commit 2a949ef「模式特化不走 pm_defs」） |
| decl 表平铺（`decl_insert`/`simpl_decl`） | ≈ −60 | 架构替换：global 表 + `GLOBAL_BASE` 大下标 + `fake_bind` |
| 新增：`check_universe` 族、`eval_neutral`、debug 族 8 函数、`strconv_quote`、`chain_env`、`run_pure_probe` 等 | ≈ +150 | L09 特性（宇宙检查/调试/纯探测） |

pattern_match.rs 演进链：**L07 ≡ L08 逐字节相同**（struct 是纯解析器糖，正确——
`p_struct` 直接产 `Decl::Enum`，模式匹配机制零改动）；L09 是**决策树架构重写**
（DecisionTree/PatConstructor + 上下文精化，622 行 vs 563，保留 Unmatched/Unreachable
警告），commit 明示逐句移植自其参考版，两版一致，非丢功能。

参考版其余文件链：L07→L08 的 mod.rs（env var 改名+示例）、pretty.rs（struct 打印
去重）、cxt.rs（1 行）、syntax.rs/struct_eq.rs（零改动）全部为 struct 特性差异，
无未解释分叉。

## 5. 本轮改动清单（7 文件）

| 文件 | 改动 |
|---|---|
| `src/L08_product_type/elaboration.rs` | Decl::Enum 臂回移「隐式无标注域钉 U」（L07 黑盒三轮修复，含出处注释） |
| `src/L08_product_type/bump_spine_iter.rs` | 同上（快版同步）+ `vapp_ok` 守卫函数 + η 两臂加守卫 |
| `src/L07_sum_type/unification.rs` | `v_applicable` 守卫 + η 两臂加守卫；Pi/Pi 臂 `quote(cxt.lvl)`→`quote(l)`（L08 §5 修复回移） |
| `src/L07_sum_type/bump_spine_iter.rs` | `vapp_ok` 守卫函数 + η 两臂加守卫 |
| `src/L08_product_type/unification.rs` | `v_applicable` 守卫 + η 两臂加守卫 |
| `src/L09_mltt/unification.rs` | `v_applicable`（按 L09 可应用集裁剪）+ η 两臂加守卫；Pi/Pi 臂 quote 层级回移 |
| `src/L09_mltt/bump_spine_iter.rs` | `vapp_ok`（L09 裁剪）+ η 两臂加守卫 |
| `tests/l08_fast_parity.rs` | 新增 `parity_enum_struct_impl_hole_pinned_u`（4 源探针） |

行为影响边界：钉 U 只作用于「无标注隐式 enum/struct 参数」（既有测试不覆盖该形态的
输入零变化，覆盖该形态的输入从误拒变通过，两版同步）；quote 层级对 `l==cxt.lvl` 的
全部既有路径逐字节等价；η 守卫对可应用值零变化、原本 panic 的分支变 Err——均无既有
Ok/Err 判定翻转风险。**未运行 cargo（规则约束），需 orchestrator 集中 `cargo check
--all-targets` + L07-L09 集成测试验证。**

## 6. 需协调者处置

1. **[P2 立项] L09 Obj 剥链精确化 + (Obj,Obj) 合同臂**（§3 两条报告项）：按 L08
   8988f7c 三件套移植到 L09 两版；建议连同 L09 黑盒样式依赖字段探针一起做。
   若接受为「L09 时代简化」，请在 L09 mod.rs 头注补记（目前只有 v_app 一条）。
2. **[路由] η 守卫的 L10 站点**：任务线索点名 L10 也缺守卫——L10 不在我的路径，
   L10 的 st2g 语义需其 owner 按本报告 §3 第 3 条的方法核实（L09/L10 均无 st2g，
   同为「同型路径、触发关闭」）。
3. **[验证] 本轮 7 文件改动**需集中编译 + 测试（重点：`--test l08_fast_parity` 新
   探针、L07/L09 全套）。

## 7. 收敛判定

**CONVERGED**（自审切片）：L07→L08→L09 的继承审计完成，754 行缩水逐块解释清楚
（合理裁剪 + 三处修复未传播，其中一处已回移、两处定性路由）；任务点名的 η 守卫
线索核实并补齐；L09 测试设施缺失查明为时代约定非遗漏；上轮修复与 needs-verify
记录全部在位。剩余开放项均为「需编译验证的耦合语义移植」或「已文档化时代缺口」，
无本切片内可进一步安全推进的动项。
