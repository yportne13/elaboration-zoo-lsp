# A6 第 1 轮：性能版链 L01→L13 修复签名单调性审计 + 基础设施/文档一致性

- 审计人：A6（全局单调性 + 基础设施）
- 工作树：`F:/projects/hermes/elaboration-zoo-lsp-cont`，分支 `review/l01l13-continuity`，基线 master `a99972a`
- 方法：纯静态（Read/Grep/git show），未运行任何构建
- **重要口径说明**：审计期间 A5 在同一工作树上有**未提交**的 L13 修改（`src/L13_namespace/{bump_spine_iter,elaboration,parser/mod,unification}.rs`）。L13 的判定给出两列：`基线 a99972a`（提交态）与 `工作树当前`（含 A5 未提交修复）。

## 1. 修复签名存在性矩阵（13 章 × 11 模式）

图例：`有` = 双版（参考版+性能版）均存在且口径一致；`有*` = 仅部分版本/形态有（见表下注）；`无路径` = 该章特性集确实无此代码路径（合法）；`遗漏` = 有代码路径但缺修复；`〔A5已修〕` = 基线缺失、工作树已由 A5 未提交变更补上。

| 章 | P1 align8+断言 | P2 MacroDepthGuard | P3 η可应用守卫 | P4 GLOBAL_BASE≥ | P5 prune .rev() | P6 match单向 | P7 to_typ→None | P8 intersect→None | P9 u64可恢复 | P10 solve_multi可恢复 | P11 unify_catch标签 |
|----|----|----|----|----|----|----|----|----|----|----|----|
| L01 | 有¹ | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 |
| L02 | 有 | 无路径 | 无路径² | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 |
| L03 | 有 | 无路径 | 无路径² | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 |
| L04 | 有 | 无路径 | 有 | 无路径 | 无路径³ | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 | 无路径 |
| L05 | 有 | 无路径 | 有 | 无路径 | 有 | 无路径 | 无路径 | 有 | 无路径 | 无路径 | 有⁴ |
| L06 | 有 | 无路径 | 有 | 无路径 | 有 | 无路径 | 无路径 | 有 | 无路径⁵ | 无路径 | 有⁴ |
| L07 | 有 | 无路径 | **遗漏** | 无路径 | 有 | 无路径 | 无路径 | 有 | 无路径 | 无路径 | 有⁴ |
| L08 | 有 | 无路径 | **遗漏** | 无路径 | 有 | 无路径 | 无路径 | 有 | 无路径 | 无路径 | 有⁴ |
| L09 | 有 | 无路径 | **遗漏**(防御) | 有 | 有 | 无路径 | 无路径 | 有 | 无路径 | 无路径 | 有⁶ |
| L10 | 有 | 无路径 | **遗漏**(防御) | 有 | 有 | 有 | 有 | 有 | 无路径 | **遗漏** | 有 |
| L11 | 有 | 有 | 有 | 无路径⁷ | 有 | 有 | 有 | 有 | 有 | 有 | 有 |
| L12 | 有 | 有 | 有 | 无路径⁷ | 有 | 有 | 无路径⁸ | 有 | 有 | 有 | 有 |
| L13 | 基线遗漏〔A5已修〕 | 基线遗漏〔A5已修〕 | 有(is_appliable) | 无路径⁷ | 基线遗漏〔A5已修〕 | 有(val_match) | 无路径⁸ | 有 | 基线遗漏〔A5已修〕 | 有 | 有 |

注：
1. L01 的 tag 机制在 `bump_spine.rs`（2-bit，`& !3`），修复形态为 `align_of::<CloCell>() >= 4` 断言（`src/L01_nbe/bump_spine.rs:78`）——与 2-bit 编码自洽，等价于其余章的 align8 口径。
2. L02/L03 无 decl/builtin，无法制造"λ 值当类型"种子（b621789 在 L04 的注释原话）；且修复前向传播约定不要求回填 L04 之前的章。η 守卫无需存在。
3. L04 无 pruning 机制（`grep -c prune = 0`）；prune_ty 自 L05 引入。
4. L05-L08 的 unify_catch 消息为对称格式（`can't unify X == Y` / `Cannot unify expected type X with inferred type Y`），无 expected/find 双标签，不存在标签方向问题——修复签名不适用（合法）。
5. L06 的 Num token 不产生 Nat 字面量（无 Raw::Nat；数字仅在 L11+ 存在且 L11/L12 脱糖为 Church 链）。u64 unwrap 签名自 L11 才有路径。
6. L09 参考版+快版**一致地**使用 `find: {t} / expected: {t_prime}`（`mod.rs:478`、`bump_spine_iter.rs` 同款）——双版 parity 成立，只是语义标签与 L10+ 相反（调用点 `unify_catch(cxt, expected, inferred)` 把 expected 打在 "find" 标签下）。见 R8。
7. L11/L12/L13 的全局机制改为名字键 `Cxt.decls` 表，`Infer.global` 大下标 + `GLOBAL_BASE` 哨兵被整体移除（`L11 bump_spine_iter.rs:3465-3468` 注释明示"无相应下溢边界"）——机制不存在，off-by-one 修复随之不适用（合法）。
8. L12/L13 的 typeclass 求解器移除了 `Typ` 桥（`Val` 直达 `val_match`），`to_typ` 不存在；L12 快版头注（`bump_spine_iter.rs:20-24`）明示该重设计。Literal 不可解值在 `val_match` 中天然不匹配（合法）。

## 2. 遗漏清单与路由（不直接改 src，按章归属路由）

### R1〔P1 级 · A3 · L07〕η 可应用守卫缺失（可触发）
- 位置：`src/L07_sum_type/unification.rs:662-675`（参考版 η 臂 `(t, Val::Lam(..)) => self.v_app(t, ..)` 无守卫；`v_app` 的 `x => panic!("impossible apply")` 在 `mod.rs:725`）；`src/L07_sum_type/bump_spine_iter.rs:2816/2835`（快版 η 臂直接 `vapp1`，`vapp1` 对 Lit/Sum/SumCase/U/Π/LiteralType panic，见 `:993-1000`）。
- 可达性：L07 继承 L06 的 `get_global` / `string_to_global_type` 逃逸舱口（全章 4+9 处命中）——`def f : U -> U = x => x` 后 `get_global "f"` 即可把 λ 值送入类型位，`unify(λ, 字面量/U)` 命中 η 臂 panic。与 L06 修复（d8e5580）同源同触发。
- 建议补丁形态：参考版在 `unification.rs` 顶部移植 L06 的 `fn v_applicable(v: &Val) -> bool`（`matches!(v, Flex|Rigid|Decl|Prim|Obj|Match 头)`，对齐 L06:16-18 + L07 的 Val 变体集），η 两臂加 `if v_applicable(&t)` / `if v_applicable(&u)`；快版在 η 臂（2816/2835）前加 `vapp_ok(v)`（L11 `bump_spine_iter.rs:721` 同款：tag ∈ {0,1,2,5} 且 tag7 仅 Obj/Decl/Call/Match）。守卫为假时回落 unify 失败（Err/false），两版判定保持一致。

### R2〔P1 级 · A4 · L08〕同 R1
- 位置：`src/L08_product_type/unification.rs:672-685`；`src/L08_product_type/bump_spine_iter.rs:2873/2892`（vapp1 panic 臂 `:996/:1002`）。
- 可达性与补丁形态同 R1（L08 同样有 get_global/string_to_global_type）。

### R3〔P2/P3 级 · A3 · L09〕η 守卫缺失（本层不可达，防御性）
- 位置：`src/L09_mltt/unification.rs:548-560`；`src/L09_mltt/bump_spine_iter.rs:2205/2220`（快版注释自述"字面量等形态的应用 panic——参考版 v_app 同款"）。
- L09 无字符串/get_global（已核实 0 命中），当前无源码级触发；但链条约定要求 L04 落地的守卫存在于 L05..L13，L07-L10 断档。修复与 L04/L05 同性质（纯防御：只把 panic 分支变 Err）。
- 建议补丁形态同 R1（Val 变体集按 L09 裁剪：Flex/Rigid）。

### R4〔P2/P3 级 · A4 · L10〕同 R3
- 位置：`src/L10_typeclass/unification.rs:618-628`；`src/L10_typeclass/bump_spine_iter.rs:2280/2295`。

### R5〔P1 级 · A4 · L10〕solve_multi_trait 仍为 unwrap
- 位置：`src/L10_typeclass/elaboration.rs:287`：`self.solve_multi_trait(&fake_cxt, super::MetaVar(0)).unwrap();`
- f51a0e4 只修了 L11/L12（L11 `elaboration.rs:292-293` 为 `.map_err(|e| Error(...))?`）；L10 同一 Def 臂仍是 unwrap——trait 求解失败即进程 panic（LSP 场景可被用户源码触发）。
- 建议补丁形态：对齐 L11：`.map_err(|e| Error(name.to_span().map(|_| format!("{:?}", e))))?;`

### R6〔需核实 · A4 · L10〕快版 Def 臂缺 solve_multi_trait 调用（parity 观察）
- 位置：`src/L10_typeclass/bump_spine_iter.rs:5098-5133`（infer_decl 的 Def 臂：check→eval→define，**无** solve_multi_trait）；参考版 `elaboration.rs:287` 有。快版的 trait 求解只发生在 unify 的 Flex 求解路径（`:2232/:2347/:2370`）。
- 风险：Def 定型期的 trait 错误在两版的报错时机/文案可能分叉（参考版在 Def 臂即失败，快版可能推迟或漏报）。l10_fast_parity 若已覆盖 def 内 trait 失败用例而通过，则可能是等价路径；请 A4 核实或补探针。

### R7〔记录 · 低优先〕`Type N` 宇宙层级解析静默兜底
- `parse::<u32>().ok().unwrap_or(0)` + `//TODO:do not unwrap`：`src/L09_mltt/parser/mod.rs:330-332`、`src/L10_typeclass/parser/mod.rs:333-335`、`src/L11_macro/parser/mod.rs:548-550`、`src/L12_canonical/parser/mod.rs`（同型）、`src/L13_namespace/parser/mod.rs:597-599`。
- 非 P9 签名本体（不 panic，静默回 0），但属同族可恢复化缺口；归各段 agent 记录，不必本轮修。

### R8〔P3 级 · A3 · L09 观察〕unify_catch 标签语义反置（双版一致）
- `src/L09_mltt/mod.rs:478-495` 与快版同用 `find: {t} / expected: {t_prime}`，而调用点 `elaboration.rs:261` 传 `(expected, inferred)`——expected 值打在 "find" 标签下。双版一致故 parity 无裂；仅跨章消息口径与 L10+ 相反。可选对齐（改双版同序）。

### L13 基线遗漏 → 工作树已闭合（A5 未提交，本轮只验证形态）
- P1：`repr(align(8))`×7 + SAFETY 注释 + align_of 断言（bump_spine_iter.rs）✓ 形态与 L11/L12 同款
- P2：`MacroDepthGuard` + `MACRO_DEPTH` thread_local + 上限 256 + 3 个接线点（parser/mod.rs:137-180, 1329/1670/3222）✓ 与 L11/L12 同款
- P5：参考版 `prune_ty` 掩码反转（unification.rs diff：`pr.iter()` → 预反转 `rev` 传参）✓
- P9：`Raw::Nat` 的 `parse::<u64>().unwrap()` → `match` + IError + `Raw::Hole`（parser/mod.rs:604-618）✓ 与 L11/L12 同款
- 待 A5 提交后由协调者复核 `Type N` u32 兜底（R7）是否顺带处理。

## 3. 基础设施与文档发现

### 已直接修改（我的授权范围）
1. `README.md:130`：L01 "16 representation variants" → **22**（权威依据：`src/L01_nbe/mod.rs:58-79` 共 22 个 `pub(crate) mod` 变体；`src/L01_nbe/readme.md:1` "22 种实现"）。
2. `README.zh.md:102`：同上，"16 个表示变体" → **22 个**。
3. `README.md:63-68`：examples/hdl 特征组枚举补上第 23 组 **verilog compat**（`23-verilog-compat.typort` 存在且已被 `test_examples_hdl_dir` 覆盖，原枚举只列 22 组）。

### 只报告不修改（路由）
- **B1〔中低优先 · 协调者/A3-A5〕bench bin 断档**：Cargo.toml 有 `l01bench`-`l08bench` + `l13bench`，**无 l09-l12bench**。L09/L10 参考版有 `bench_check/bench_check_nf`（`mod.rs:601/615`、`630/644`），L11/L12 性能版 Tycker 有同名方法（`L11:7587`、`L12:7711`），fast_parity 测试在调用它们做正确性 parity——harness 齐备但缺 perf 测量入口。l13bench 头注自称"与 L02-L08 bench 同族"，说明家族在 L09-L12 断档**非有意设计**（更像按需建 bin）。补 bin = 新代码 + 编译验证，不在本轮范围；建议列入后续轮次。
- **B2〔低优先 · 记录〕blackbox 套件分布**：v1/v2 覆盖 L02-L08；v3 仅 L05/L06/L07（L08 无 v3）；`fast_parity` 自 L07 起接棒。v3 是第三轮"攻击面"产物（各章独立撰写，头注明示互补关系），非系统网格——判定为**按轮次有意**而非遗漏；但客观后果是 L09-L12 无黑盒输出断言套件（仅 parity），覆盖缺口记录在案。
- **B3〔无需修改〕lib.rs 模块声明**：`src/lib.rs:21-32` 声明 L02-L13；L01 有意不入 lib（`l01bench.rs:25` 以 `#[path]` 直编，头注明示解耦设计）。与 README 章节表一致。
- **B4〔已验证〕README 测试声明**：`test_examples_hdl_dir` 真实存在（`src/L13_namespace/legacy_tests.rs:1441`，`include_str!` 覆盖全部 23 个 examples/hdl 文件）；examples 表所列 5 个文件全部存在。
- **B5〔记录 · 低优先〕README.zh.md 与 README.md 内容漂移**：zh 版 HDL 特性表缺 en 版的后半行（Reg init、Output reg、regNext、Counter、Memory、Type casts、Inout ports），且整段缺 examples/hdl 介绍 + `test_examples_hdl_dir` 声明。非错误声明（无虚假断言），属翻译滞后；本轮不做大手术，留待协调者决定是否同步。

## 4. 结论

**NOT CONVERGED**（第 1 轮）

- 主阻断项（需 A3/A4 下一轮处理）：R1/R2（L07/L08 η 守卫，可触发 panic）、R5（L10 solve_multi_trait unwrap）、R3/R4（L09/L10 防御性守卫）、R6（L10 快版 Def 臂 parity 待核实）。
- L13 侧：基线 a99972a 上 P1/P2/P5/P9 四项断档均已由 A5 在工作树补齐（形态与 L11/L12 同款，本轮逐项核验），待提交。
- 基础设施：bench bin 断档（B1）为唯一结构性发现，非阻断；文档侧 3 处已修。
- P4/P6/P7/P8/P11 全链无真断档（所有"无"均为机制不存在或双版自洽）。
