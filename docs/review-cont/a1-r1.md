# A1 Round1（连续性审计）— L01_nbe / L02_tyck / L03_holes

> 任务：L01→L02→L03 逐对相邻章节的"继承连续性"审计与修复（第 1 轮）。
> 切片：`src/L01_nbe/**`、`src/L02_tyck/**`、`src/L03_holes/**`、
> `src/bin/l0{1,2,3}bench.rs`、`tests/l0{2,3}_*`。
> 工作树：`F:/projects/hermes/elaboration-zoo-lsp-cont`，分支
> `review/l01l13-continuity`（基于 master `a99972a`）。主线未被触碰。
> 方式：纯静态（Read / git diff --no-index / grep / git log -S），未运行任何
> cargo/git 写操作。注意：工作树内另有并行 agent 的未提交 L13 改动
> （`src/L13_namespace/*` 4 文件），非本 agent 所改、未触碰，协调者汇总时
> 请按作者拆分。

## 1. 结论（verdict）

- P0: 0  P1: 0  P2: 1（已修）  P3: 4（2 已修 / 1 观察项 / 1 文档依据成立）  needs-verify: 1
- 本轮是否收敛：**待 orchestrator 编译复验**。自审切片无未决 P0/P1/P2；
  唯一 P2 与两项 P3 已按最小 diff 修复，但本环境禁跑 cargo，需集中
  `cargo check --lib` + L01 模块测试 + l01bench/l02bench 冒烟确认。

## 2. 审计方法与覆盖面

- 性能版链逐文件全文精读：`L01_nbe/bump_spine_iter.rs`（409 行）、
  `L02_tyck/bump_spine_iter.rs`（1626）、`L03_holes/bump_spine_iter.rs`（2565），
  对共享机制（eval 双栈/右链快速路径、quote 任务栈/ChainRun、Spine 记账、
  memo 屏障、conv/unify 工作表、Machine/Tycker、export/tm_size、负载生成器）
  逐段比对；负载生成器用 sed 切片 diff（church/conv/dup/dup_deep 逐字节
  一致，conv_dup 仅字符串排版差异、输出等价）。
- 参考版：L02/L03 `mod.rs`、`parser/{mod,lex}.rs` 全文 + `git diff --no-index`
  （parser 两对 diff 全量过目）。
- bench 基建：`bench.rs`（992）+ 三个 bin 全文比对。
- 修复沿链条的传播用 `git log -S` 追溯（65269fb / 9f15a4a / 2eee834 /
  fa03e14 / 6c53097）。

## 3. 上一轮修复在位验证（全部通过）

| 修复（来源） | 验证点 | 状态 |
|---|---|---|
| bench 深段早退 `mem::forget`（fa03e14） | `src/L01_nbe/bench.rs:888`（空 rows 早退支）与 `:893`（正常出口）均在 | ✔ |
| L01 packed tag 对齐断言（fa03e14） | `bump_spine.rs:78`（CloCell ≥4）、`:86`（EnvCons ≥8）、`native_clo.rs:42`（Clo ≥4）+ SAFETY 注释 | ✔ |
| L02/L03 `CloCell` `#[repr(align(8))]` + 断言（6c53097） | `L02:130,136`、`L03:226,232`；`PiCell`/`EnvCons` 断言 `L02:146,153`、`L03:242,169` | ✔ |
| v_clo_of/v_pi_of SAFETY（6c53097） | `L02:106-122`、`L03:125-141` 同款在位 | ✔ |
| L02 lexer get_unchecked SAFETY（6c53097） | `L02 parser/lex.rs:78-79` 在位（措辞比 L03 孪生更精确：点明 tail 是 after_head 前缀） | ✔ |
| readme 测试计数（6c53097） | 静态重数：L02 = 10+13+2+1 = **26** ✔；L03 = 9+16+2+1 = **28** ✔ | ✔ |
| L03 Machine/Tycker 头注释（6c53097） | 与 `clear_round`（`L03:1455-1461`）/常驻字段一致 | ✔ |

## 4. 发现列表

### [P2] L01 `bump_spine_iter.rs` 缺 L02–L13 全链都有的 65269fb 防御回灌守卫 —— 已修
- 位置：`src/L01_nbe/bump_spine_iter.rs` quote_iter 的二叉 fallback 与
  ChainRun bail 两处链分解位点（原 `:200-204`、`:241-253`）。
- 证据链：
  1. L05 修复 65269fb（quote 链分解单独引陈旧函数部分 → β-redex 混入输出）
    的提交信息明确点名：「同款链分解代码存在于 **L01~L13** 各章
    bump_spine_iter.rs（跨章嫌疑，L04 同输入实测不触发，潜伏）」。
  2. 防御回灌系列随后落在 L02（9f15a4a）、L03（2eee834）、L04（cda2c0e），
    理由均为「保持各章孪生结构对齐」；本轮 grep 确认 **L02–L13 每章
    bump_spine_iter.rs 两处位点都有该守卫（各 2 处命中）**。
  3. L01 是全链唯一没有守卫的一章。L01 readme / mod.rs / 前轮评审报告均无
    排除 L01 的文档依据 → 按审计规则视为遗漏。
- 机制/影响：L01 无 meta/无 force，spine 槽的 f 压栈时必非闭包（eval 的
  β 岔路先行归尽，`W::Apply`/`W::ChainWrap` 只收非闭包头）且值不可变，
  守卫**恒假、零行为变化**；属与 L02 完全相同的潜伏防御（L02 回灌提交
  自述"守卫恒假，属潜伏"）。不补则 L01 作为链条起点与其后 12 章孪生分叉，
  未来演进（加 meta/惰性解）破坏不变量时 L01 将独自产出 β-redex。
- 处置：**已修**。按 L02 版形状（无 force/env_ext，L01 的 env 是
  `Option<&EnvCons>`）在两处补 `if v_tag(ef/fi) == 1` 分支：函数部分为闭包
  时按 β 语义应用本槽实参再引应用结果（ChainRun 恢复点 `prev:None` 直接
  取该结果），注释说明恒假论证与回灌来源。else 分支与原代码逐字节相同。
  L01 现有 7 个模块测试（含 `interleaved_chains_fallback`/`chain_mixed_heads`
  两个走 fallback 路径的用例）行为不变。

### [P3] `l02bench.rs` 的 `--only` 文档漏列 `fast_memo` —— 已修
- 位置：`src/bin/l02bench.rs:77`（原「逗号分隔：basic,fast,fast_ss」）。
- 证据：代码 `want("fast_memo")` 实际支持（l02bench:197），模块头也把
  `fast_memo` 列为实测行；L03 孪生（`l03bench.rs:83`）文档完整列出
  「basic,fast,fast_ss,fast_memo」。纯文档遗漏。
- 处置：**已修**（对齐 L03 列全四个实现名）。

### [P3] `bench.rs:738` 过期调试注释（`eval_only` 不存在）—— 已修
- 位置：`src/L01_nbe/bench.rs:738`（bench_cek_deep 的 cek_bump 块）。
- 证据：尾注「临时改为 eval_only 排查」自 8593e7c（2026-08-28）残留；
  `eval_only` 在 `cek_bump.rs` 全文不存在，实际调用是 `normalize_imported`
  （完整归一化）。属误导性调试残留（前轮 BRIEF 维度同款：过期注释清理）。
- 处置：**已修**（删除该尾注，代码不动）。

### [P3→观察项，不改] L02/L03 parser `kw`/`string` 的实现风格分叉
- 位置：`src/L02_tyck/parser/mod.rs:112-135` vs `src/L03_holes/parser/mod.rs:115-152`。
- 证据：L02 用 `&input[1..]` 直接索引 + 界内论证注释（`input.first()` 为
  Some ⇒ len ≥ 1 ⇒ `[1..]` 恒界内，成立）；L03 用 `.get(1..).ok_or_else(..)`
  防御式写法（该 err 分支按同一论证不可达）。两者行为等价、各自自洽。
- 处置：**不修**。双向都不构成"丢守卫"：L02 的索引有正确论证注释背书，
  L03 的 get 是冗余防御而非 L02 缺失的修复；强行统一只扩 diff。仅记录。

### [P3→文档依据成立，不改] L03 未继承 L02 的 `NO_BITEQ` 消融开关
- 位置：`src/L02_tyck/bump_spine_iter.rs:589-596`（`L02_NO_BITEQ`）；
  L03 无对应物。
- 证据：消融开关是实验工具而非守卫；L03 readme（:59-60）明示本层消融轴
  只有 `L03_NO_CONV_MEMO`/`L03_NO_NAME_MAP`——章内文档即"有意为之"依据。
  且 L03 的位相等与 force 前后双查交织（`unify_iter` 内两处 `t.0 == u.0`），
  语义上不再是可独立关断的捷径。
- 处置：**不改**（有文档依据）。

### [needs-verify] L01 新增守卫分支无运行时覆盖（结构上不可达）
- 位置：`src/L01_nbe/bump_spine_iter.rs:208,268`（新增两分支）。
- 说明：守卫恒假（§4 P2 的不变量论证），任何公开输入都无法进入分支——
  这与 L02/L03 回灌时"属潜伏、无触发用例"的处境相同（彼处靠既有 blackbox
  全量 parity 兜底 else 路径）。L01 的 7 个模块测试 + l01bench
  `--workload church/dup` 的正确性断言覆盖 else 路径；新分支只能靠
  orchestrator 的 `cargo check`（类型/借用正确性）钉住。
- 处置：needs-verify，请集中编译时确认无 warning（无未用变量/不可达告警）。

## 5. 继承连续性逐对核验摘要（无发现部分）

### 5.1 L01 → L02（性能版）
- eval：右链快速路径/β 岔路/ChainWrap 语义逐行对应；L02 的
  `ApplyKnown`（β 岔路直送）与 `if heads > 0` 守卫是 L02 的**优化演进**
  （自述"不再经 Tm(Var) 重查环境"，语义等价），L01 通用三推不是遗漏——
  非语义变化，不属必须回灌项。
- quote：Q/Lam1/EvalQ/App1/ChainRun 逐行同构；连续性引理注释同源。
  L02 增 Π/Pi1/U 与 memo（MemoStore 屏障），均为本章特性。
- Machine：L01 每调用新建小栈 + spine/vals 复用；L02 同构并加 quote_memo
  常驻（洗白 + 入口 clear，`L02:826-901`）。L02 **未**沿用 L03 的 workbuf
  常驻——L02 `Machine` 头注释（:829-838）实测数据 +「别再往回灌」明示
  有意为之 ✔ 有文档依据。
- 打包值：L01 2 位 tag（`!3`，断言 ≥4）→ L02 3 位 tag（`!7`，CloCell 补
  repr）——差异由 tag 位宽论证背书（a1-r2 §5.1 已证），✔。
- packed 断言覆盖：L01 全目录仅 `bump_spine.rs`/`native_clo.rs` 两处
  packed 表示（grep 复确认），bump_spine_iter.rs 经 `use super::bump_spine`
  复用，被 bump_spine.rs 的断言覆盖 ✔。

### 5.2 L02 → L03（性能版）
- 值/环境：tag 5 Meta 立即数、复合 Env（平坦 def 区 + binder 链）为 L03
  特性；`env_ext_defs` 非 tip 回落有专门回归测试（`define_inside_lambda_
  matches_basic`）。
- eval：L02 全部 work 条目语义保留 + AppBds/AppBdsOne（InsertedMeta 实参
  应用，与参考版 `vAppBDs` 同语义）。
- quote：force 前置、tag 5 → `Tm::Meta`、flex 链头共享 `?m` 节点、flex
  ChainRun idx_node ——本章特性；65269fb 守卫两处在位（force 后判定）。
- conv→unify：L03 的 (2,2) 同头分支把 L02 的无条件链下钻改为**受控内联环**
  （`cont` 门控：两侧实参链顶层 f 与本层 f 同字才下钻）。这不是 L02 丢
  守卫，而是 L03 的求解分派需要（盲下钻会跳过内层头分派、把该走 solve
  的形态逐层 pairwise 误比，L03 注释 :1081-1090 自述实测依据）；L02 无
  meta/solve，所有下钻压出的 Pair 都经完整分派，完备性成立（本轮推导
  复核：ChainWrap/Apply 两种 spine 惯例的 value 分解在 L02 循环下均被
  逐层 Pair 覆盖）。✔ 章节特性差异，非遗漏。
- 判等记忆化：L02 `WItem::Store`（纯 conv）→ L03 `UItem::Store` + 成功
  单调论证（meta 写一次/force 单调），健壮性论证随特性升级 ✔。
- Tycker/export/tm_size/负载生成器：逐段 diff，差异全部为 Meta/Env 的
  本章扩展（`None` → `EMPTY_ENV`、`Tm::Meta(_)` 计数、bds 计数）✔。
- 消融开关：`L03_NO_CONV_MEMO` 继承 `L02_NO_CONV_MEMO` 语义并升级论证；
  `NO_BITEQ` 未继承——见 §4（文档依据成立）。

### 5.3 parser/（L02 → L03）
- `lex.rs`：diff 仅 `Underscore`→`Hole` 重命名 + 注释/测试样例适配；L03
  多一个空行（纯格式）。SAFETY 注释两章均在 ✔。
- `parser/mod.rs`：diff = `Raw::Hole` 新增、`p_atom` 增 Hole 臂、`p_binder`
  改名、kw/string 风格分叉（§4 观察项）、测试样例换洞版。产生式结构
  （p_spine/p_lam/p_pi/fun_or_spine/p_let/p_raw/with_pos）逐行一致 ✔。
- 可见性：L03 `parser`/`pretty_tm`/`display_error`/`Error` 为 `pub`、L02 为
  `pub(crate)`/私有——两者都满足各自 blackbox 测试的 `#[path]` 引用
  （`pub(crate)` 在测试 crate 内同样可见），非缺陷。

### 5.4 bench 基建
- `l01bench`（bench.rs 驱动）→ `l02bench` → `l03bench`：口径一致演进——
  mimalloc、大栈线程（`L0N_STACK_MB`）、计时外解析 + 结果断言、min/median
  输出、`--only` 过滤、参考版行 `basic` 的 mem::forget 均一致。L03 增
  chain/solve 负载与 memo 前置断言（`l03bench.rs:163-171`，比 L02 的
  want 内断言覆盖更广），均为加法。已知小差异：L03 在
  `--workload church --only fast_memo` 下不再断言 memo 节点数（断言移到
  前置块且排除 church）——bench 断言覆盖差异，P3 以下，不修。
- L01 `bench.rs` 变体表：22 变体 + 2 个 `_ss` 口径行 = want() 清单 24 项
  ✔；深段（n>8000）出赛名单（cek/cek_bump/bump_iter/bump_spine_iter/
  bump_spine_slim + iter_ss）与 readme 一致 ✔；`bench_cek_deep` 两条出口
  均 mem::forget ✔。

### 5.5 L01 变体间口径（上轮遗留验证）
- 打包表示仅 2 文件（见 §5.1），断言/SAFETY 均在位 ✔。
- readme 变体表 22 行 = 目录 22 个变体文件 = mod.rs 表 = bench.rs import ✔。
- 上轮 needs-verify 两项维持原判：`rename_iter` 就地 ren 表（L03，无用户
  可达触发，保留）；bench_size 空 rows Drop（已判非 bug，关闭）。

## 6. 本轮改动清单（3 文件，+62/−15）

| 文件 | 改动 |
|---|---|
| `src/L01_nbe/bump_spine_iter.rs` | 二叉 fallback + ChainRun bail 补 65269fb 防御回灌守卫（恒假防御，与 L02–L13 孪生对齐）；else 分支与原代码逐字节一致 |
| `src/L01_nbe/bench.rs` | 删 `:738` 过期调试尾注（`eval_only` 不存在） |
| `src/bin/l02bench.rs` | `--only` 文档补 `fast_memo`（对齐 l03bench） |

均为守卫/注释级最小 diff：不改任何既有路径的行为、输出字节与公共 API。
未动 tests/（新守卫结构上不可达，无可写的黑盒触发用例）。

## 7. 需要 orchestrator 处置的事项

1. **集中编译复验**：`cargo check --lib`（重点 L01 bump_spine_iter 的两处
   新 unsafe-free 分支的借用/类型）+ `cargo test --lib L01_nbe`（7 用例）
   + l01bench/l02bench 冒烟（`--workload church --max-church 8000` /
   `--max-k 9`）。
2. **跨切片提示**：L01 的递归孪生 `bump_spine.rs`（递归 quote 的 App
   fallback）与 `bump_spine_slim.rs` 存在同族链分解位点，65269fb 回灌系列
   历来只覆盖各章 `bump_spine_iter.rs`（L02–L13 均未动章内其它表示），
   本轮按既定家族模式只补链条起点 bump_spine_iter.rs；若后续想消除章内
   潜伏面，应整族统一立项，不宜单点。
3. 工作树含并行 agent 的 L13 未提交改动（`src/L13_namespace/` 4 文件），
   汇总/提交时请注意归属拆分。

## 8. 收敛判定

- 自审切片：P0/P1 = 0，P2 已修，P3 清理 2 项 + 2 项有据不改，needs-verify 1
  项（仅验证覆盖面，非缺陷）。
- 结论：**CONVERGED（待集中编译确认）**——无阻塞项；§7.1 的编译复验是
  流程性确认而非已知风险。
