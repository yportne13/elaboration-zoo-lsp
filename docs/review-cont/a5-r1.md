# A5 R1 — L13 章节继承连续性审计与修复（第 1 轮）

- 范围：`src/L13_namespace/**`（参考版 + 孪生 bump_spine_iter.rs），对照 `src/L12_canonical/**`（上轮评审已合入 L02-L12 的同族补丁形态）。
- worktree：`F:/projects/hermes/elaboration-zoo-lsp-cont`（分支 `review/l01l13-continuity`，基于 master a99972a）。
- 方法：逐项 diff L12 对应文件确认补丁形态 → 逐字移植；函数级清单比对（grep fn）+ 逐文件 diff 审读；未运行任何构建（协调者集中编译）。

## 1. 发现清单

### P0

| # | 位置 | 描述 | 状态 |
|---|---|---|---|
| 1 | `src/L13_namespace/bump_spine_iter.rs:541/730`（修前） | **XCell/CloCell 无 `#[repr(align(8))]`**：packed 编码 `v_clo`/`v_pi`/`v_xcell` 写 `ptr\|tag`、`v_*_of` 用 `& !7` 解码；wasm32 上 `&str` 仅 4 对齐，低 3 位非 0 → 解引用错位指针（真实 UB；Cargo.toml 有 wasm 目标）。L02-L12 均有（FINAL §2.1），L13 没跟上。 | **已修**：XCell/CloCell 加 `#[repr(align(8))]` + 文档说明；`v_clo_of`/`v_pi_of`/`v_xcell_of` 补 SAFETY 注释（L12 逐字）；新增 const 断言块（XCell/CloCell/PiCell/EnvCons ≥8）。64 位布局不变。compact.rs 无 packed 编码，无需改。 |
| 2 | `src/L13_namespace/parser/mod.rs:1267/1603/3151`（修前，与上轮记录行号一致） | **宏展开无深度上限**：`macro_rules m { () => { m } }` + `def x = m` 无界递归栈溢出，LSP 可在任意用户源码触发。L11/L12 有 MacroDepthGuard（FINAL §2.3），L13 三处同构展开点均无守卫。 | **已修**：移植 L12 的 `MAX_MACRO_EXPANSION_DEPTH=256` + thread_local + RAII `MacroDepthGuard` + `macro_depth_error`；三处展开点全盖（p_raw 宏臂 / p_def_stmt_block / p_decl 宏臂；第三处超限退化与现有 Err 分支同款：is_cut → 空 Package，否则试下一条规则）。 |

### P1

| # | 位置 | 描述 | 状态 |
|---|---|---|---|
| 3 | `src/L13_namespace/unification.rs:853`（修前） | **intersect_go 长度失配 `unreachable!()`**：一侧 spine 空另一侧非空时 panic；L09-L12 已改 None 回落 unify_sp。 | **已修**：`_ => None` + 注释（L12 逐字）。快版 `intersect_bump` 本就 `n1 != n2 → false`，无需改。 |
| 4 | `src/L13_namespace/unification.rs:197-232`（修前） | **prune_ty 未反转 pruning 掩码**：Pruning 链头是最内层槽位，meta 类型 Π 从最外层剥起；旧实现直接按链头配对外层 Π，多层 telescope + 混合掩码错位，与快版 `prune_ty_bump`（外→内）parity 分叉。L09-L12 已修，L13 没跟上；且残留 3 处过期 `//TODO:revPruning` 注释（unification.rs ×2、elaboration.rs ×1）。 | **已修**：`prune_ty` 先收集反转成外→内 Vec；`prune_ty_go` 改收 `&[Option<Icit>]` + `split_first` 配对（L12 逐字，ren 字段保留 L13 的 `Rc<HashMap>` 形态）；清 3 处过期 TODO。 |
| 5 | `src/L13_namespace/parser/mod.rs:603`（修前） | **u64 超大整数字面量 `parse::<u64>().unwrap()`**：`99999999999999999999999999` 直接 panic。L11/L12 已改推 IError + Hole。 | **已修**：闭包式 parser（L12 同构），Ok → `Raw::Nat`，Err → `state.push_error`（ErrorStore）+ `Raw::Hole`；保留 L13 原生 Nat 语义（不脱糖 Church 链）。 |

### P2（核实为不适用 / L13 已自带，未改代码）

| # | 对照项 | 结论 |
|---|---|---|
| 6 | GLOBAL_BASE 哨兵 off-by-one（L09/L10 专属 `Infer.global` 大下标机制） | L12/L13 均无此机制（全局走 decl 名字键表，无大下标解码，无下溢边界）——**不适用**。附带发现：L13 twin `Machine` 文档注释（bump_spine_iter.rs:5340）是 L09 误拷（声称走 `Machine::decl` + GLOBAL_BASE 偏移，与实际 `Rc<Decls>` 机制矛盾），**已按 L12 口径订正**（上轮 §2.4 的"Machine 误拷注释"同族）。 |
| 7 | to_typ 对 LiteralType/LiteralIntro/Prim todo!()（L10/L11） | L13 trait 求解已重写：无 `to_typ`/`Typ` 中间层，`val_match`/`vals_eq_ground` 全 `_ => false` 兜底、无 todo!/panic（typeclass.rs:271-352/359+），`"s".foo` 走 head_key=None → 无实例 Err——**结构性消除，无需移植**。 |
| 8 | η 臂 applicability 守卫 v_applicable/vapp_ok（L11/L12） | L13 **已自带**且更宽：参考版 `is_appliable`（unification.rs:44，含 Lam/March）守 η 臂（1029/1036）；twin `is_appliable`（bump_spine_iter.rs:3260）守 η 臂（4050/4065）。语义 = L12 v_applicable/vapp_ok + L13 扩展（v_app 对 Match 是 branch splice 非 panic、对 Lam 是 β）。**无需移植**。 |

### P3（观察项，主线演进区 / 非同族，记录不动）

| # | 位置 | 描述 |
|---|---|---|
| 9 | `src/L13_namespace/typeclass.rs:284` | `val_match` 双向绑定臂 `(_, Val::Rigid(x, sp)) \| (Val::Rigid(x, sp), _) if sp.is_empty()`：第二分支允许 **goal 侧**裸刚性变量 vs 实例 pattern 构造子（List/Sum/Nat…）时自绑定返回 true——与 d4c05ea 在 L10/L11 修掉的"泛型假匹配"同构通道。但 **L12 逐字同款**（L12 typeclass.rs:131-139；d4c05ea commit 即声明"语义对齐 L12/L13 已有的 val_match"），防线在 L12/L13 的上游（trait_wrap 的 can_satisfy 头索引 + out_param + trait_metas 推迟机制，d4c05ea 实测 L13 对该场景报 Err）；且 L13 trait 求解是主线正在演进的分叉区（docs/l13-twin-bench-2026-09.md §10 连修 5 个阻塞点）。**记录为两章共有的理论通道，不在本次移植范围**；若主线后续在 L12 收紧 val_match，需同步 L13。 |
| 10 | `src/L13_namespace/cxt.rs:561-591` | 新增文件 IO prim（file_read/write/append_all_text）IO 失败 `panic!`——L13 HDL/命名空间新特性（主线设计），非同族缺陷；LSP 场景理论上可由 HDL 源码触发，建议主线改可恢复 Err。 |
| 11 | `src/L12_canonical/unification.rs:823` | L12 残留 `//TODO:todo!()`（注释掉的 Sum unify 臂占位），L13 无此注释——L12 侧小瑕疵，超出本 agent 修改白名单，留给 L12 归口 agent。 |

## 2. 继承审计（L12 → L13 逐文件）

方法：`git diff --no-index src/L12_canonical/X src/L13_namespace/X`（X = syntax/cxt/elaboration/unification/pretty/typeclass/canonical/pattern_match/parser/mod + bump_spine_iter）+ 函数名清单集合差。

- **函数级**：L13 只增不减。L13 twin 缺的 3 个函数名均为替代实现：`vapp_ok`→`is_appliable`（η 守卫，已核对语义覆盖）、`err_unsolved_meta`→unify_catch 内联格式化、`eval_aux_case`→pm 重写（infer_expr_pm/unify_pm 在 elaboration.rs）。L12 cxt.rs 的 tm_app/tm_decl/tm_lam/tm_pi 在 L13 cxt.rs:623-640（`pub(super) fn`，清单正则漏报）。
- **差异内容**：全部落在预期特性面——命名空间/模块/包（cxt/pretty/typeclass/parser）、HDL→Verilog（cxt file IO prim、parser Package）、原生 Nat（XCell::Nat、nat_matches_chain）、观察面（push_hover/inlay/typ_pretty/span）、twin 专属（观察面 push、arena 压实 compact.rs、force memo、Call/Match unify 臂）。**未发现其他"L12 有而 L13 丢"的修复/守卫**。
- **twin 关键守卫核对**：`intersect_bump` 长度失配直败 ✓、`prune_ty_bump` 掩码外→内 ✓、`unify_catch` expected/find 标签方向正确（非 L10 旧病）✓、η 臂 is_appliable ✓、packed 类型对齐（本轮补）✓。
- pretty/canonical/pattern_match：L12 的 panic 点在 L13 或同构保留（syntax.rs:50/65、typeclass "Too much effort"）或已降级（pretty Sum-case panic → L13 有 `sumcase_with_non_sum_typ_degrades_instead_of_panicking` 钉子）。canonical.rs 差异仅 fresh_meta 加 span + builtin 过滤名单挪位（L13 演进）。

## 3. 修改文件清单（本 agent 全部改动）

| 文件 | 改动 |
|---|---|
| `src/L13_namespace/bump_spine_iter.rs` | +35/−2：XCell/CloCell `#[repr(align(8))]` + 文档；v_clo_of/v_pi_of/v_xcell_of SAFETY 注释；const 对齐断言（XCell/CloCell/PiCell/EnvCons）；Machine 文档注释 L09 误拷订正 |
| `src/L13_namespace/parser/mod.rs` | +91/−4：MacroDepthGuard 基础设施（上限 256）；三处展开点守卫；u64 字面量可恢复化（IError + Hole） |
| `src/L13_namespace/unification.rs` | +26/−8：prune_ty 掩码反转（prune_ty_go 改收 slice）；intersect_go None；清 2 处 `//TODO:revPruning` |
| `src/L13_namespace/elaboration.rs` | +1/−1：清 1 处 `//TODO:revPruning?` |
| `tests/l13_fast_parity.rs` | +33：`probe_oversized_int_literal_recoverable`、`probe_macro_self_recursion_depth_limit`（L12 同款探针移植；8MB 栈线程跑 parser，断言不 panic + 有解析错误） |

未触碰：`vscode_extension/`、`Cargo.toml`、L01-L12 文件（git status 中 L01/L10/L11/L12 的改动来自并行子 agent，非本 agent）。

## 4. 编译验证需求（移交协调者）

静态自查已做（作用域/类型/导入/trait 方法存在性逐点核对，全部对照 L12 同款形态），但规则禁止本地构建，需协调者集中验证：

1. `cargo check --all-targets`
2. `cargo test --test l13_fast_parity probe_oversized_int_literal_recoverable probe_macro_self_recursion_depth_limit`
3. L13 既有测试全量（prune_ty 反转改变多层非线性 pruning 的行为口径——与快版对齐，参考版/快版 parity 应更一致而非分叉；若既有用例锚定了旧的错位行为需按 L05-L12 同款口径更新断言）
4. wasm32 目标 check（`cargo check --target wasm32-unknown-unknown` 或实际目标 triple，验证新 const 断言通过）

## 5. 结论

**CONVERGED**（本 agent 范围内）：

- 上轮 §3.3 列的 L13 同族缺陷：5 项缺失已全部按 L12 同款补丁逐字移植（P0×2 + P1×3）；3 项核实为不适用或 L13 已自带等价守卫（P2，含 1 处注释订正）。
- 继承审计未发现 §3.3 之外的"L12 有而 L13 丢"项。
- val_match 泛型刚性目标验证：与 L12 逐字同构（d4c05ea 的对齐目标本身），上游防线完整；理论假匹配通道为两章共有，已记录（P3#9），不动主线 trait 演进区。
- 阻塞项：无。唯一未闭环是编译验证（规则禁止本地构建，见 §4）。
