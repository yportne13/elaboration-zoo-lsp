# A2 Round1（continuity）— L04_implicit / L05_pruning / L06_string 章节继承连续性审计

- 工作树：`F:/projects/hermes/elaboration-zoo-lsp-cont`（分支 `review/l01l13-continuity`，基于 master `a99972a`；主线 `F:/projects/hermes/elaboration-zoo-lsp` 未触碰）。
- 审计对象：`src/L04_implicit/**`、`src/L05_pruning/**`、`src/L06_string/**`、`src/bin/l04bench.rs`、`src/bin/l05bench.rs`、`src/bin/l06bench.rs`、`tests/l0[456]_*`。
- 方法：逐对相邻章节 `git diff --no-index` + **函数体级规范化比对**（剥注释/缩进后 unified diff，Python 辅助脚本，临时目录不入围）；L05→L06 重构大 diff 用函数级清单比对。静态审计，未运行任何构建（规则 3）。
- 本轮**未修改任何文件**（零丢失项，见 §3）；本报告为唯一产出。

## 1. 结论（verdict）

- **P0: 0  P1: 0  P2: 0  P3: 4（均仅报告）  路由 A3/A4: 1 项**
- **CONVERGED**：L04→L05→L06 两套实现（参考版 + bump_spine_iter 快版）逐函数比对，
  全部差异均为「本章新特性演进」或「有注释/文档依据的架构重构」，未发现任何
  「前章有而后章丢」的修复/守卫/断言/口径。上轮修复（b621789）全部在位。
- 无阻塞项。

## 2. 上轮修复在位验证（b621789，已合入 master 且为 HEAD 祖先）

`git merge-base --is-ancestor b621789 HEAD` 通过；`git log b621789..HEAD -- <本切片>` 为空
（上轮之后无第三者改动）。逐项核对：

| 修复项 | 位置 | 在位 |
|---|---|---|
| `CloCell`/`PiCell` `#[repr(align(8))]` + `align_of>=8` 断言 | L04 `bump_spine_iter.rs:248,260`；L05 `:273,285`；L06 `:302,314` | 是 |
| `XCell` `#[repr(align(8))]` + 断言 | L06 `bump_spine_iter.rs:209` | 是 |
| `v_*_of` SAFETY 注释（点明 repr 保证含 wasm32） | L04 `:137,148`；L05 `:155,166`；L06 `:172,183,195` | 是 |
| 参考版 `v_applicable` + η 两臂守卫 | L04 `mod.rs:140,437,441`；L05 `mod.rs:180,939,943`；L06 `unification.rs:16,476,481`（先存） | 是 |
| 快版 `v_applicable` + η 两臂守卫 | L04 `bump:163,1008,1020`；L05 `bump:181,1152,1164`；L06 `bump:833,1737,1756`（先存） | 是 |

## 3. 继承审计明细（逐对、逐套实现）

### 3.1 性能版 L04→L05（bump_spine_iter.rs，1289+/603-）

函数清单比对：L05 新增全部属剪枝特性族（`intersect_bump`/`flex_flex_bump`/`invert_bump`/
`solve_with_pren_bump`/`prune_vflex_bump`/`prune_meta_bump`/`prune_ty_bump`/`has_mark`/
`with_big_stack`/`prune_src` 等）；改名仅 3 例且均为特性演进（`solve`→`solve_bump`、
`lams`→`lams_from_ty`、`fresh_meta` 改签名带类型、`AppBds*`→`AppPrun*`）。
规范化函数体比对（剥注释/缩进）结论：

- `env_nth`/`env_ext`/`spine_head`/`collect_args`/`flex_of`/`reset`/`set`/`insert`/
  `insert_t`/`eval`/`quote`/`unify`/`unify_catch`/`clear_round`/`unwind_names`/`run_impl`/
  `bench_nf_impl`/`quote_maybe`/`types_names`：**代码逐字相同**（0 delta）。
- `force`/`meta_val_of`：仅 typed-meta（`Solved(v,_)`/`Unsolved(_)`）——L05 特性。
- `eval_iter`：仅 `AppBds`→`AppPrun`（icit 槽位化）演进；`debug_assert!`（flat_len）**升级为
  release 也查的 `assert!`**（注释给出 u32 wrap 理由）——加强而非丢失。
- `quote_iter`：纯 rustfmt 格式差，语义逐字同构（L05 注释自述「本段与 L04 逐字同构」，核实属实）。
- `unify_iter`：异头 flex 求解重构为 f1/f2 分派 + `intersect_bump`/`flex_flex_bump`/
  `solve_bump`（L05 主特性）；**受控内联环逐语句保留**（含 `cont` 下钻时 `f1!=f2` 压栈、
  位相等后缀免比实参仍压函数部分对——L04:1127-1151 与 L05 同构；diff 碎片曾致误读，
  已直读源码复核）。η 守卫两版同步（b621789）。
- `rename_iter`：+`occ: Option<u32>`（occurs check 参数化）+flex 链走 `prune_vflex_bump`——特性。
- `check`/`infer`/`bind_name`/`new_binder`/`define_name`/`fresh_meta`/`insert_go`/
  `insert_until_name`/`eval_fresh`/`display_metas`/`export`/`tm_size`：全部为 typed-meta/
  locals+pruning 线程化（`BdCons`→`PrCons`/`LCons`、`InsertedMeta`→`AppPruning`）特性演进。
- `debug_assert_eq!(done_icits.len(), 0, …)`（L04:1428）在 L05:1722 **保留**（换行格式化）。

### 3.2 性能版 L05→L06（3591→4430 行）

函数清单比对：L05 全部剪枝族函数在 L06 在位（`intersect_bump`/`flex_flex_bump`/
`invert_bump`/`solve_with_pren_bump`/`solve_bump`/`rename_iter`/`prune_vflex_bump`/
`prune_meta_bump`/`prune_ty_bump`/`lams_from_ty`/`close_tm`/`has_mark`/`has_free_var`/
`bind_prefix_of_telescope` 等）；L06 新增字符串/decl 族（`v_lit_ty`/`v_xcell`/`v_xcell_of`/
`spine_len`/`is_declheaded`/`decl_name`/`prim_arity`/`lit_of`/`prim_fire`/`decl_apply`/
`vapp1`/`force_v`/`ty_precheck`/`check_ty`/`infer_decl`/`str_pi`/`run_input`/`run_fast`/
`strchain_src`/`globals_src`）。规范化比对结论：

- solve/prune/rename 全族：仅 `decls`/`mmap` 参数线程化 + `intersect_bump` 加
  `|| v_tag(a1) == 7`（XCell 实参不得走位相等捷径，须按参考版分派——有注释，语义正确）。
- `eval_iter`/`quote_iter`：+`LiteralType`/`LiteralIntro`/`Decl` 求值与引读臂、
  decl 头链走 `decl_apply`（prim 触发）——特性。
- `unify_iter`：位相等捷径与实参跳过对 tag 7 关闭（`&& v_tag(t) != 7`，必要且正确：
  异指针同内容字面量/Decl 须按名分派）；same_head 扩展 Decl 同名；新增 (6,6)/(6,7)/(7,6)/(7,7)
  四臂与参考版 `unify` 的 LiteralType/Decl 臂逐一对应（`(Lit,Lit)=>false` 注释自述
  「参考版无该臂」= 参考版落 `_=>Err`，两版判定一致——L07/L08 模块头同样记录该设计，
  全 zoo 无 `(Lit,Lit)` 自反臂，属一致设计非分叉）。
- `v_applicable`：L05 同款 + tag 7/XCell::Decl 扩展（分层正确）。
- 错误口径变化（`unify_catch`「can't unify {:?}」、`no named implicit arg`、
  `error name not in scope`、`infer named lambda`、`icit mismatch {:?}`）：参考版同步
  （mod.rs:333 / elaboration.rs:56,233,252,278），`tests/l06_blackbox*.rs` 断言钉死。
- L05 的 `show_val`/`display_metas`/`main_with`/`quote_maybe`/`run_impl` 及
  `conv_src`/`conv_dup_src`/`chain_src`/`dup_src`/`dup_deep_src` 在 L06 移除：均为
  **decl 架构重构的组成部分**（表达式级 mode 入口 → `run`/`run_fast(input,path_id)`；
  elab/nf/type 模式由 println 输出契约取代；错误显示改 Debug 格式；bench 负载族换成
  strchain/global，见 §3.5）。`run_fast` 架构被 L08-L13 完整继承（grep 证实），系正向演进。
- Machine 包装（`eval`/`quote`/`quote_memo`/`unify`）：destructuring 局部化重构 + decls/
  mmap 线程；`'static→'a` 擦除 unsafe 模式逐字保留。

### 3.3 参考版 L04→L05（mod.rs，825+/544-）

- `v_applicable`/`v_app`/`closure_apply`/`quote`/`insert`/`insert_t`/`insert_until_name`：
  逐字相同。
- `unify`：仅臂序微调 + flex-flex/intersect 两臂（L05 特性）；η 守卫、`_ => Err` 兜底保留。
- `rename`：L05 重写为含 occurs check（`pren.occ`）+ `prune_vflex`（L05 主特性），
  L06 逐字继承。
- `eval`/`force`/`check`/`infer`/`bind`/`define`：typed-meta + locals/pruning + src_names
  名字表（L05 特性）；`fresh_meta(cxt, a)` 带类型。
- `go_bds`/`v_app_bds`→`go_pr`/`v_app_pruning`：剪枝掩码化（特性）。

### 3.4 参考版 L05→L06 文件组重构（本次审计重点）

L06 文件组（mod/cxt/elaboration/unification/pretty/syntax + parser/）函数清单 vs L05 mod.rs：

- **unification.rs 完整继承 L05 unify/prune/occurs 语义**（逐函数规范化比对）：
  `invert`/`invert_go`（nlvars 非线性集逻辑保留）、`prune_ty`（**掩码反转 rev 保留**）、
  `prune_ty_go`（四态 None/Some(None)/Some(Some)/Err 保留）、`prune_meta`、`prune_vflex`
  （SpinePruneStatus 三态 + foldr 逆序修正注释保留）、`rename`（occurs + scope check）、
  `rename_sp`、`solve`/`solve_with_pren`（prune_non_linear 验证保留）、`unify_sp`、
  `flex_flex`（长短侧选择保留）、`intersect`/`intersect_go`（**长度失配 `_ => None`
  回落 `unify_sp` 保留**，与快版 `intersect_bump n1!=n2=>false` 同判定）、`lift`/`skip`、
  `lams`（"_"→x{n} 改名保留）。差异仅为 `&Val`→`&Rc<Val>` 装箱与 List 模式匹配重构 +
  字符串臂（`(LiteralType,LiteralType)`/`(LiteralType,Decl)` 宽松合一/同名 Decl 逐参）。
  **臂序与 L05 相同，新臂插在 `_ => Err` 之前——η 守卫、flex 求解臂位置不变。**
- elaboration.rs：`insert`/`insert_go`/`insert_t`/`insert_until_go`/`insert_until_name`/
  `check` 结构逐臂继承；新增 `ty_precheck`/`check_ty`（L13 check_universe 轻量移植）与
  decl 级 `infer`（Def/Println；重定义检查 + 递归占位 fake_bind）；`infer`→`infer_expr` +
  LiteralIntro 臂 + decl 表回退。
  check 的 Lam 首臂守卫由 L05 的 `n.data == x_t.data` 改为
  `(i.clone(), i_t) == (Either::Name(x_t.clone()), &Icit::Impl)`——**语义等价**：
  `Span<T>` 的 PartialEq 是定制实现（`parser_lib.rs:71-77`，只比 `data`），快版同式
  （bump:3223-3228 有对照注释 + data 比较）。
- mod.rs：`Val`/`Tm` + LiteralType/LiteralIntro/Decl 变体；`v_app` + Decl 臂（prim 触发），
  `_ => panic!("impossible")` 保留（由 η 守卫把门）；`eval`/`quote`/`nf`/`force`/
  `close_val`/`fresh_meta`/`v_app_pruning` 结构同 L05；`close_ty`（L05:111→L06 syntax.rs:14）
  与 `Locals` 同构。`Cxt` = L05 五字段 − `pos`（随 `Raw::SrcPos` 变体一起移除——L06 错误
  架构去位置化，两版一致，测试锁定）+ String builtin 注册。
- 入口：`run(input, path_id)` 取代 `main_with`/`display_error`/`display_metas`/
  `help_mode`/`line_col`/`report_at`/`show_icit`/`show_tm`——decl 架构 + 无位置字符串错误
  的整体重构；`bench_check`/`bench_check_nf`（`mem::forget` 防深树 Drop 爆栈，注释自述
  L03/L04/L05 同款处理）+ `tm_size_ref` 对齐快版口径。
- pretty.rs：`pretty_tm`/`fresh`/`go_ix`/`go_app_pruning`/`go_pr_inner` 为 L05 go_lam/
  go_pi/go_pr/go_pr_i 的提取重构 + `List` 化；`fresh` 递归→迭代（等价）；`go_ix` 出界
  退化固定文案（a2-r3 已记录的 L06 行为）。
- cxt.rs：Cxt/builtin 注册表（L13 移植，L06 特色）。

### 3.5 parser/ 链与 bench 基建

- **L04→L05 parser**：`lex.rs` **逐字节相同**；`mod.rs` 仅 2 行 doc 注释差异（章名）。
- **L05→L06 parser**：表达式核（`ident_atom`/`p_binder`/`p_spine_arg`/`p_icit`/`p_app`）
  0 delta；`p_raw`/`p_atom` 仅去 `with_pos`（SrcPos 移除）+ 字符串字面量臂（unescape）；
  新增 decl 层（`p_decl`/`p_def`/`p_print`/`many1_sep`/`square`/`extract_base`）。
- **l04bench→l05bench**：全 L04 负载族保留（church/implicit/conv/conv_dup/chain/solve/
  dup/dup_deep）+ `prune`（L05 特色）；参考版 API `bench_check`/`bench_check_nf` 演进正确。
- **l05bench→l06bench**：保留 church/implicit/prune/solve（源改写为 decl 语法）+ strchain/
  global（L06 特色）；移除 conv/conv_dup/chain/dup/dup_deep——**有文档依据**：bin 头注释
  「全部为 L06 顶层 decl 语法——无尾表达式行、def 无分号，parser 只取 decl 前缀」（旧
  负载以尾表达式为检查目标，decl 语法下无意义）+ readme「负载族（l06bench）」节只列
  新六族；l07bench 同口径（church/strchain/global/…），属链上一致演进。
  L06 还**加强**了互检（nf 负载 inline 断言参考版节点数、basic_too_slow 显式豁免有注释）。

### 3.6 测试连续性

- 套件：l04_blackbox(45)+v2(43)；l05_blackbox(18)+v2(31)+v3(39)；l06_blackbox(28)+v2(10)+v3(68)。
- L05 v3 含继承的隐式参数回归（named_implicit_* / parity_scan_eta_icit_matrix）；L06 含
  implicit+pruning 交互（implicit_insertion_and_pruning / pruning_nonpalindrome_dependent_masks）
  与 η 守卫回归（bug1_eta_*，上轮已确认）。内部 cfg(test) 每章自然演化，不等同继承丢失。
- readme 文件清单漂移为上轮已记录 P3（见 §4-R3），维持不动。

## 4. 发现清单（全部仅报告，无需修复）

### [P3-R1] ty_precheck 错误文案尾部两版格式不同（潜在文案 parity 缝，行为判定一致）
- 位置：参考版 `src/L06_string/elaboration.rs:133`（`format!("expected universe, got {:?}", other)`，
  Debug 参考版 Val）vs 快版 `src/L06_string/bump_spine_iter.rs:3338`
  （`format!("expected universe, got V({})", v.0)`，打包原始字）。
- 证据：`def bad : string_concat = "a"` 类输入两版 Err 判定一致、needle
  "expected universe, got" 均含（tests/l06_blackbox.rs:388-408 用 substring 断言，绿）；
  但尾部一个 `Pi(…)`、一个 `V(…)`。v3 的 `assert_full_text_parity`（全文逐字节）目前
  未覆盖此输入，若未来升级为全文比对会暴露。
- 处置：仅报告。两版内部表示不同（Box 树 vs packed word），Debug 本质不可逐字对齐；
  该消息族的契约就是 substring needle（既有测试形态）。改文案属无收益 churn。

### [P3-R2] l04bench 的 128MB 栈理由注释在 l05/l06 bench 丢失（信息移至 readme，非丢失）
- 位置：`src/bin/l04bench.rs:23`（「参考版（Box/List 递归）在大 k 段仍需要深栈，默认
  128MB 留足余量」）在 l05bench/l06bench 无对应行；但 `L05/L06_STACK_MB` 机制与
  `unwrap_or(128)` 均在，且 L05 readme:138 / L06 readme 均有同等说明。
- 处置：仅报告（信息未丢失，只是位置移动）。

### [P3-R3] 三份 readme 的测试文件清单漂移（上轮 a2-r3 §2 遗留，维持）
- 位置：`src/L04_implicit/readme.md:11,50`、`src/L05_pruning/readme.md:12,50`、
  `src/L06_string/readme.md:16-17,130-131`。
- 处置：仅报告（延续上轮「最小 diff」判定；无计数错误）。

### [P3-R4] L06 eval_iter 的 `assert!` 注释尾句被截（assert 本体在）
- 位置：`src/L06_string/bump_spine_iter.rs:1163` 附近 `assert!(env.flat_len >= b.none_run)`
  —— L05 同行注释「（release 也查：错位时 u32 减会 wrap 成静默越界）」在 L06 只剩
  前半。release 断言本体保留。
- 处置：仅报告（注释不完整，无行为影响；不为半句注释动文件）。

### 【路由 A3/A4】L07-L10 unify η 臂缺 applicability 守卫（同型潜伏 panic 面，与修复前的 L04/L05 同类）
- 位置（参考版）：`src/L07_sum_type/unification.rs:662,669`、
  `src/L08_product_type/unification.rs:672,679`、`src/L09_mltt/unification.rs:548,554`、
  `src/L10_typeclass/unification.rs:618,624`——η 臂对非 λ 侧无条件 `v_app`；
  L07 `v_app` 对 U/Pi/LiteralType/LiteralIntro 走 `panic!("impossible apply")`
  （`src/L07_sum_type/mod.rs:725`）。
- 可达性核实（本轮 grep + 读码）：
  - **L07/L08**：`string_to_global_type` 返回 decl 登记**类型** `e.ty`（L07 mod.rs:534
    `Some(e) => e.ty.clone()`；L08 mod.rs:534 同），非登记**值**——L06 Bug-1 的
    「λ 值当类型」链条在此两层**不可达**（与上轮 a2-r1 §4 判断一致，仍成立）。
    mutable_map 的 get_global 值虽可为 λ，但其**类型**是 st2g（未登记名→卡住 Decl→宽松臂
    放行），λ 值不进入 unify 的类型位。
  - **L09/L10**：grep 证实无 decl 表/st2g/builtin 注册（`add_builtin`/`decl_insert`/
    `string_to_global_type` 均无命中）——无 λ-当-类型种子。
  - 对照：L11/L12 已有 `v_applicable`/`vapp_ok`（参考+快版），L13 有 `is_appliable`。
- 定性：与修复前的 L04/L05 完全同类的**潜伏**（当前不可达）panic 面，非可达 bug。
- 建议：转 A3（L07/L09）与 A4（L08/L10）裁定——若按 b621789 对 L04/L05 的统一化理由
  （消潜伏面、跨层口径一致、守卫只改原 panic 分支、零可达行为变化）补齐，可参照
  L04/L05 的补丁形状（参考版 `matches!` 帮助函数 + 快版 tag 版 `v_applicable`）。
  本切片未代改（超出 A2 允许修改范围）。

## 5. 修改文件清单

- **无**（src/tests/bin 零改动；零丢失项故无修复）。本报告为唯一新增文件。

## 6. 收敛判定

- **CONVERGED**。判定依据：
  1. 上轮 L04-L06 修复（b621789）全部在位且为其后无改动；
  2. 三对相邻章节 × 两套实现的函数级/函数体级比对未发现任何继承丢失（前章的守卫、
     断言、修复、语义臂在后章全部在位或以特性形式增强）；
  3. 全部 P3 为注释/文档级或既有契约内的文案差异，不影响行为、parity 判定与内存安全；
  4. 唯一跨切片事项（L07-L10 η 守卫）已核实定性并路由，不阻塞本切片。
- 给协调者：本轮 A2 无需集中编译验证（零代码改动）；若 A3/A4 采纳路由项，其改动才需门禁。
